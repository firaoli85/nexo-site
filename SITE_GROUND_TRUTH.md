# SITE GROUND TRUTH

**The single source of truth for the Nexo Access marketing site (nexo-site).**
Read this first, every session, before touching anything.

| | |
|---|---|
| **Owner** | Firaoli ("Oli") Seboka, solo founder, FC Nexo LLC dba Nexo Access |
| **Status** | V1 live at nexoaccess.com. V2 rebuild starting: research-first, on the `v2` branch. |
| **Horizon** | Built to hold for 2 to 3 years of solo operation. Oli will be running the business, not rebuilding the site. |
| **Version** | v2.6 — August 21, 2026 |

---

## 0. WHY THIS DOCUMENT EXISTS

V1 was built with real craft and real QA discipline, but its decisions live in code comments and lost chats ("stage reports" cited everywhere, existing nowhere). The site also speaks as the wrong company. This document ends both problems. Every principle and decision lives here or in a doc this file points to. It is law. **If code and this document disagree, this document is right and the code is wrong.** If Oli leaves for two years, this file plus FIXLOG resumes the project.

---

## 1. WHAT NEXO ACCESS IS (positioning law)

**Nexo Access is a medical transportation management organization. Transportation is the business: trips are delivered through our credentialed network of transport providers, on our platform, under our standards, and we take responsibility for every trip. Providers operate the vehicles; Nexo Access manages and answers for the service.**

- First market: **NEMT in the DMV** (DC, Maryland, Virginia). That is the public story.
- The broader ambition (private pay, facility contracts, other transportation management) stays **structural and modular**, not on the site yet. Adding a market later is adding a module, not repositioning.
- The site's job: make an MCO contracting director, a state program officer, or a provider owner conclude within one visit that this is a serious, well-built transportation management company that answers for its network.
- Anti-identity: never consumer taxi language ("book your ride today") in the lead; never framing that disowns transportation ("we don't drive", "we're just software"). We manage the service and we own the outcome.

## 2. AUDIENCES (in priority order)

1. **MCOs and payers** — contracting directors, provider network managers. They evaluate: credibility, compliance posture, network capacity, technology proof, a pilot path.
2. **Transport providers** — owner-operators in the DMV. They evaluate: is joining worth it, what is required, how do claims and payment work, is this company organized. Recruitment surface AND a genuine resource: the site must teach them how to become a compliant provider in each state.
3. **Facilities and case managers** — booking and confirming rides for people in their care.
4. **Members** — reassurance and what to expect. Smallest surface. B2B site; no consumer sign-up.

## 3. VOICE AND MESSAGE PILLARS

Voice: plain, direct, evidence-based. State what the platform enforces; claim nothing the code cannot back. Copy is written last (into COPY_DECK) and is the cheapest layer; the machine underneath is the work.

Pillars (each must be provable from the platform):
1. **We manage, providers drive** — the operating model, stated plainly.
2. **Credential-gated everything** — expired credentials block assignment automatically.
3. **Clean claims by construction** — scrubbing, adjudication, frozen records, no double payment.
4. **Enforced in the database, not a policy binder** — RLS, state machines, audit trails.
5. **Built for the DMV** — regional depth: the states, the programs, the requirements.

## 4. LOCKED DECISIONS LEDGER

Append-only. A decision changes only with a new dated entry stating from where, to where, and why. Nothing is ever deleted; rejected paths stay in the record, marked rejected, with the reason.

| # | Date | Decision |
|---|---|---|
| D1 | 2026-08-17 | **Positioning:** medical transportation management organization. We manage; providers drive. Resolves the "operating model UNDECIDED" note in `src/lib/launch.ts`. |
| D2 | 2026-08-17 | **Public scope:** NEMT-first, DMV-first. Broader markets modular, later. |
| D3 | 2026-08-17 | **Sign-in visible pre-launch.** Un-gate the existing `PORTAL_LIVE` surface. Any attempt: "no account found, contact support." No sign-up path (B2B). Wire to real auth when app.nexoaccess.com deploys. |
| D4 | 2026-08-17 | **Cross-repo design law:** the site's design system is documented as UI guidance for the nexo-access platform (docs/PLATFORM_DESIGN_HANDOFF.md; seed = NEXO_SITE_DESIGN_REPORT.md §7 incl. shadcn token-collision warnings). |
| D5 | 2026-08-17 | **Cross-project checkpoints** between site and platform, relayed by Oli, every exchange documented. |
| D6 | 2026-08-17 | **Skills audited, not assumed.** Registry doc; broken tools stopped and fixed, never silently worked around. |
| D7 | 2026-08-17 | **REVERSAL — git moves to Claude Code.** WAS: Oli runs all git himself; git never inside a Claude Code prompt. NOW: build prompts include the git steps; Claude Code stages, commits (one commit per task, descriptive message), and pushes. WHY: manual git caused costly mistakes; removing the human step removes the mistake class. Guardrails: never force-push, never push with tsc red, never touch `main` except at declared merge gates. |
| D8 | 2026-08-17 | **Branch strategy:** all V2 work on the `v2` branch. CI typechecks and builds every push; the deploy job fires only on `main` (verified in `.github/workflows/ci.yml`), so pushing `v2` never deploys. Merge `v2` → `main` only when the §7 quality gates pass. The live V1 site stays untouched until then. |
| D9 | 2026-08-17 | **Identity: text wordmark, no logo for now.** A logo waits until one actually satisfies the owner. Many serious companies run on a wordmark; so do we. |
| D10 | 2026-08-17 | **Research-first sequencing.** No page is designed before the research phase documents the industry standard (see §6, P1). |
| D11 | 2026-08-17 | **Mobbin + Figma are the design-reference pipeline.** Inspiration is pulled from Mobbin (and Figma), and every adopted reference gets a receipt in docs/DESIGN_RESEARCH.md: screenshot, what we take, what we reject, why. Both Claude (chat, Mobbin connector live) and Claude Code use it. |
| D12 | 2026-08-17 | **Challenge-to-test law (automatic).** Any task that fixes something genuinely hard — a bug that cost time, a rendering defect, a regression, a class of mistake — must add an automated test (QA invariant, unit test, or script check) covering that problem's whole class, in the same task, same commit. Manual verification recipes are test specs: captured as automation, never performed once and discarded. The owner never has to request this; the prompt template enforces it. |
| D13 | 2026-08-17 | **SITE_BOARD — the visual HQ.** `docs/SITE_PROGRESS.json` (committed) is the living status truth; `scripts/board.mjs` renders self-contained `docs/SITE_BOARD.html` (gitignored, local render). Shows: current task ("you are here"), phase bars P0–P6, plan ledger with status dots, decisions ledger with reversal chains, latest FIXLOG entries, gates dashboard, skills registry, discovery inbox, and a graveyard of rejected/deferred items (kept forever, with reasons). Regenerated at the end of every task automatically. Built in P0 Task #3. |
| D14 | 2026-08-17 | **V2 visual target: Mobbin-class animated premium.** Performance comes from technique + measurement, not restraint. P1a gains a technique-teardown workstream (per admired reference: animation library, what is animated, loading strategy, degradation strategy). Research findings no installed skill covers get encoded into new project skills via skill-creator (first candidate: nexo-motion). The static-complete SSR doctrine is retained. |
| D15 | 2026-08-17 | **Positioning language refinement (amends D1's expression, not its substance).** Public framing never negates: no "we do not drive," no "nothing to do with transportation." We deliver transportation through a credentialed provider network and take responsibility for every trip; providers operate the vehicles, Nexo Access manages and answers for the service. §1 carries the canonical wording. |
| D16 | 2026-08-17 | **Handoff-in-parallel protocol (extends D4).** docs/PLATFORM_DESIGN_HANDOFF.md is maintained WHILE building, never retrospectively: every design decision receives a transfer verdict at decision time — TRANSFERS (identity-level) / ADAPTS (platform reworks it) / SITE-ONLY (marketing furniture) — with reason and date. Two different products, one identity: neither wholesale copying nor from-scratch. Cross-project checkpoints (D5) continue. |
| D17 | 2026-08-17 | **Publish boundary for the resource center: requirements, never advice.** The site states what the rules ARE (insurance tiers, driver/vehicle requirements, WMATC authority, workers' comp thresholds) with citation and verification date. NEVER business-formation, ownership, legal, or financial guidance ("form an LLC" and its relatives are banned). Every resource page carries an informational-only disclaimer and its verification date. Nothing UNRESOLVED or merely REPORTED/OWNER-REPORTED is publishable. |
| D18 | 2026-08-17 | **Broad legibility, local operations (refines D2, does not reverse it).** MCO evaluators contract nationally; the site must read as a national-grade medical transportation management organization whose current operating footprint is the DMV, never as a local-only shop. SEO targets category queries alongside geo queries. Provider resource pages remain state-specific (providers are local); payer-facing surfaces speak nationally. Owner insight on record: the evaluator searching from New York or California must not scroll past us. |
| D19 | 2026-08-17 | **Imagery strategy (closes T3).** Primary imagery = animation + custom illustration in our own visual language. Stock people-photography permanently banned (fake patients read as fake trust). Real photography deferred until real operations exist, then added as truth. The hero receives a new signature animated centerpiece; the current hero is on record as hard to grasp (owner + user feedback). Concept proposed by Claude in P3 as rendered comparisons; owner reacts before build. SafeRide-style flow explanations are built as animation, not video, within this ruling. |
| D20 | 2026-08-17 | **P2 technical ratification.** (a) Performance bar adopted as ceiling: ≤600KB transfer, ≤150KB JS, ≤1200ms DCL, ≤60 requests on the default profile; P5 measurement may tighten, never loosen. (b) Stripe's decline-don't-degrade rule is standing law for any canvas/GPU work (failIfMajorPerformanceCaveat: true; never render slowly). (c) motion-safe: architecture adopted for V2: motion is switched ON by capability classes, never patched off. (d) The four nexo-motion candidates (nexo-drift, nexo-shape, nexo-settle, nexo-accent-card) approved for P3 exploration and encoding into the nexo-motion skill. |
| D-pre21 | 2026-08-17 | **Type process rulings (D21 pending finalist pick).** (a) Premium purchase declined for now; ROADMAP door: if at P6 the type feels insufficient on the real site, evaluate ONE premium face, one-time license only, never subscription, bench rerun required. (b) Commit-and-close law: once D21 locks the finalist, type is CLOSED until post-launch; no re-litigating mid-build. (c) J3 contradiction page names no vendors (trade-libel: the pattern is the story); owner veto open. |
| D21 | 2026-08-17 | **Type ruling (W3 closed; commit-and-close in force).** System B: Bricolage Grotesque display at heavy cuts (headings 700, hero 800), Hanken Grotesk body, IBM Plex Mono accent layer (stat lines, eyebrows, codes). Bench-measured tracking is law: hero -0.030em, heading -0.022em, lede -0.008em, mono +0.035em. Owner verdict on two good screens: systems close; B chosen; B is also the zero-retypeset path (bench-disclosed). MODULAR TYPE LAW: type follows the color single-source architecture — one token block (families, weights, tracking), font loading in one file, Tailwind mapped once, zero hardcoded font families in components; a future swap = one block edit + full cube. Type is CLOSED until post-launch. Cheap-panel C3 leg still owed → W4. |
| D22 | 2026-08-17 | **Private evidence tier: OWNER-DOC-VERIFIED.** Facts confirmed from the owner's historical broker/payer documents (Healthway era; PHI-scanned clean) carry internal-verification weight. HANDLING LAW: the source documents and the evidence memo live in Claude project knowledge ONLY; no figures, broker names, or contract terms enter any repo; site publication still requires public primary sources (D17). Chat-side memo creation was the deliberate exception to the Claude-Code-creates-docs law: private material cannot pass through a public repo. |
| D23 | 2026-08-18 | **Perception floors (C3 hardened).** Owner two-device readings (high-end control + standard laptop operative): surfaces A06/B06, borders E1-07/E2-05, glass floors 0.75/0.60 (current 0.90/0.70 held — safety margin recorded), disabled tier exempt, F1+F3 PROPOSED adopted. FLOORS-ONLY-RISE LAW: perception floors may rise with new device readings, never lower; no task may soften a floor by good-screen judgment. Identity unchanged: same jade, hardened deltas. Hex guard live (DISC-20 closed). |
| D24 | 2026-08-19 | **Impact-check law + suggestion register (owner ruling).** (a) Every future build prompt carries an IMPACT CHECK step before implementation: map the planned change against the §4 ledger, FIXLOG AUDIT GUIDANCE lines, and the invariant set; name every collision and how it is resolved; an unresolvable collision is a STOP-and-surface-to-owner fork, never a silent override. (b) All design/feature suggestions from any source (owner, Claude, external AI reviews, user feedback) are captured in `docs/DESIGN_SUGGESTIONS.md` with source, date, disposition (ADOPTED / ALREADY-LAW / TEST-FIRST / LATER / REJECTED+reason / OWNER-DIRECTIVE), and impact notes citing D-numbers and invariants. Nothing is implemented from suggestion lists directly; register first, impact-check at adoption time. |
| D25 | 2026-08-20 | **Colour direction ruled: D2 SYNTHESIS (owner).** Jade remains the identity and owns all interactive, primary and status surfaces; warmth enters as **ATMOSPHERE ONLY** — layered CSS radial glows, background register, **never carrying semantic meaning and never behind the alarms' colour space**. Owner-supplied anchor: rose **#f58f9c** (companion **#f7929a**, same family). Supporting owner picks registered for **benched accent use only**: violet **#8d6fd3** (converges with the existing illustration accent family), deep tones **#332750** and **#522a22** (ink-accent candidates, **unadopted**). **Owner's stated law, on record: warm/pink cannot be an emergency-adjacent signal; incorporate it in backgrounds.** Implementation law: **zero-cost CSS radials** (the GitLab-dissection technique, credited per the owner's take-credit-improve rule); **every text-over-glow pairing AA-verified against PAINTED pixels**, not against assumed composites; **D23 floors bind and floors-only-rise is untouched**; final temperature chosen by the owner from the tuned bench; **site implementation is a separate full-cube task**. |

> **D25 IS REVERSED. Ruled 2026-08-20, shipped in Task #27, reversed 2026-08-21 by the N3-b ruling
> on the Noir Bench.** The fork was rendered as two identical hero compositions differing only in the
> warm dusk, and the owner ruled from the render rather than from prose: ***"the warm dusk is
> officially dead; mint is the only light in the dark."***
>
> **FROM:** rose/violet atmosphere as an ink-register background wash (`--atmo-rose #f58f9c`,
> `--atmo-rose-soft #f7929a`, `--atmo-violet #8d6fd3`, `--atmo-light`, `--atmo-ink`), plus the
> **compensated ink pair** that existed only to pay for the wash's luminance lift
> (`--ink-surface #273a32`, `--on-ink-border #4a6c66`, `--on-ink-border-strong #688f82`).
> **TO:** pure noir per D29 — deep ink, charcoal, stark white, and electric mint as the single light.
> **WHY:** side by side, the warm atmosphere read as dusk rather than as instrumentation, and it put a
> second temperature in a palette whose whole argument is one light in the dark.
>
> **SALVAGE — this ruling cost nothing and produced three things that outlive it.** (1) The
> **painted-pixel method** (Range client rects in document coordinates against a text-hidden full-page
> raster), which is now the standard instrument and which has since caught defects no computed check
> could see. (2) The **register-split discovery**: electric mint measures 1.49:1 on paper, so "electric"
> is an ink-register idea — that finding is the seed of D30. (3) The **compensated-pair math**, which is
> the worked example of what a background change actually costs in border and surface tokens.
>
> **EXECUTION RIDES TASK #33**, not this task. The reversal removes the `.atmo-*` applications and
> their tokens; the pre-#27 global values are intact **outside** the scoped `.atmo-ink` subtree, so #33
> verifies that before deleting rather than assuming it.
| D26 | 2026-08-20 | **Build-environment integrity law.** *(D25 is held for the pending colour-direction ruling per the bench protocol; this row is numbered D26 so the reserved slot stays open.)* **(a) FILE SYNC MUST NEVER TOUCH BUILD EXHAUST.** A committed `.stignore` at the repo root excludes `.next`, `node_modules`, `.git` and `out`. Syncthing reads `.stignore` from the folder root, so committing it protects every machine automatically after `git pull` — the owner syncs the working folder across three machines, which is a live mechanism for stale and mixed builds and is the most likely contaminant of two earlier field rounds. **(b) FIELD PROVENANCE PROTOCOL.** Every field test begins with the build-generation probe. **Screenshots of unproven builds are not evidence.** **PROBE PUBLICATION RULE: any field probe published into `docs/FIELD_OBSERVATIONS.md` must first be EXECUTED by the harness against the running local build, with its output shown in the task report, and must be derived from the actual rendered DOM and CSS — real class names, real property serialisation — never from assumed selectors. Untested probes are banned.** Two chat-authored probes were published blind and both failed: one read the progress variable from `documentElement` when the code sets it on `.route-overlay` and therefore printed `0` on every machine forever; the other crashed on `parseFloat('calc(0.72px)')`, which is Blink's serialisation of `stroke-dashoffset`. Both produced numbers that were cited as evidence and were artifacts. **(c) CONTAINERIZATION** (owner suggestion) registered for post-launch evaluation. |
| D27 | 2026-08-20 | **The Boldness Ruling (owner).** Verdict on the current build: **~85% right** — smooth, sleek, disciplined — **but too quiet**: flat nav, decorative-but-mute background lines, list-shaped navigation, newspaper feel. Directives: **(a) NAV becomes bold and unmistakable** — sleek but heavy presence, liquid-glass weight, **boundary perceivable at a glance**, colour no longer restricted to black/ink; **(b) NAV PANELS follow the boxed-destination pattern** (Railway reference): each destination a **designed box with its own shape/representation**, never a text list; **(c) HERO:** the dispatch console **demotes down-page**; a **big signature animation leads** (activates D19/W2 with priority); **(d) MOBILITY CARDS:** service levels (ambulatory / wheelchair / stretcher; **bariatric & two-person assist as the modifier** per PRODUCT.md) rendered as **beautiful switching cards**, GitLab-card register — **S-001's concrete form**; **(e) AMBIENT MEANING:** background lines must read as an **intentional system model** — aggressive, legible, semantic — **never unexplained decoration**; **(f) BRAIDED ROUTE:** evolve the single route line toward a **braided multi-strand motif** weaving rose/violet (D25 palette) with the jade lead; **(g) EVERY PAGE:** per-page atmosphere/background attention in P4 — **no page ships as bare paper**; **(h) INTENTIONALITY DOCTRINE:** every visual element must be able to **state its purpose**; *"just a nice line"* is **banned**. **Perception supersession:** the FO-2 nav hairline **measures painted (I21 green) but reads invisible to the owner** — recorded as **the subtle approach failing the owner's bar, not as a defect**; **I21 remains the floor**; the bold nav **supersedes the question**. **All directives flow through the Grand Bench** (rendered candidates, owner reaction) **before site application**; **D23 floors, D20 budget, motion-safe architecture, decline-don't-degrade, and status-semantics law bind every candidate.** |
| D28 | 2026-08-21 | **Tech-noir ruling (owner, Grand Bench reading).** Brand thesis verbatim: *"Nexo Access is fundamentally a medical transportation company, but our backbone is pure technology… trust through a clean, highly professional interface… aesthetic like a modern, high-end developer tool or cloud platform (Railway-class)… minimalist, modern, with a very subtle Blade Runner/cyberpunk edge… practical medical transport made effortless through advanced routing tech."* **Rulings:** **B1 → ADOPT B1(i) INK-BOLD nav.** **B2 → ADOPT structure**; active/hover via **glowing neon accent borders** (never heavy fills); **geometric utilitarian icons**; modifier-tier treatment **ratified**. **B3 → ADOPT braid at ROUTE SCALE** (precision routing, not page-dominating); **strand palette re-ruled**: inactive strands **low-opacity neutral (charcoal family, register-aware)** receding into the field; **ONE saturated neon active line = ELECTRIC MINT** (jade family; **cyan remains rejected per S-007**); **van livery = B** (ink body, mint outline, the "glowing wireframe"); **weave/occlusion/roller-coaster spec unchanged (paint-only recolor)**. **B4 → ADOPT Direction 2** (dispatch-network model; *"tactical data stream"*). **B5 → drift REJECTED** (bench evidence: static read better; B4-d2 makes it redundant); **settle ADOPTED** as the arrival grammar. **Composite → Chanel's rule ratified**: the **braid + network nodes are THE signatures**; surrounding layout stays **stark and minimal**. **GUARDRAIL (load-bearing): "subtle."** Noir owns the **signature and chrome registers only**; **D18 deep-reading surfaces (provider/payer content) stay clean and legible**; **neon never carries or neighbours alarm semantics**. **OPEN FORK:** D25 warm atmosphere on ink is **RULED FROM THE NOIR BENCH (N3)**, not in chat. |
| D29 | 2026-08-21 | **Monochrome + One Light (owner research ruling).** **PALETTE LAW: deep ink, charcoal, stark white, and exactly ONE accent.** **Accent law, verbatim:** ***"mint only where blood is pumping through the system."*** The accent marks life in the system — active route, live state, the one thing moving — and nowhere else; an accent that appears where nothing is live is a violation of this law even when it is legible. **The accent is ELECTRIC MINT `#2fe89a`** (hue 155°, S80 V91, 11.62:1 on ink). The maximum-voltage alternate `#00ffa3` is **REJECTED as gaming-adjacent** — the brand is an instrument, not a console. **REGISTER-SPLIT LAW (arithmetic, not preference): electric mint measures 1.49:1 on paper and is unusable there for text or graphics.** No tuning escapes it; it is a property of white grounds. The light register therefore carries its own active line (`#00a36a`, 3.03:1), and **"electric" is an INK-register idea**: on ink the signature reads as *voltage*, on light the same signature reads as *precision*. **Jade-as-wash is RETIRED; jade SURVIVES as the mint bloodline** — electric mint is hue 155°, inside the jade family, so the identity is continuous rather than replaced, and **cyan stays rejected (S-007 holds by measurement: cyan begins near 175°, this sits 20° clear)**. **Rose and violet are RETIRED**, including the illustration accent; a **noir pass over the line art is scheduled**, so no illustration keeps a warm accent by default. **§5 AMENDMENT (nexo-brand):** neon edges are **1px calibrated RINGS, never halos or blooms** (the N4-A "expensive instrument" reading). The object-glow ban is otherwise **intact** — this amends §5 for the signature and chrome registers only and does not open glow as a general material. **Spring physics ship via CSS `linear()`, zero-JS** (framer-motion stays dead). **Ambient density is pursued through breathing/grain candidates, which are DISTINCT from the rejected drift**: drift was per-cell opacity cycling and read as moving parts; density treatments change the field's texture, not its behaviour (S-028 is not reopened). |
| D30 | 2026-08-21 | **Two-Register Law + Seam Rule (owner, "the Stripe move," confirmed verbatim).** The site runs **two registers**, and each one has a job it must do. **THEATER (noir + neon):** chrome, heroes, the braid and van, the dispatch map, the mobility cards. Its job is to ***prove technical weight***. **PAPER (light, high-legibility):** provider requirements, payer content, compliance, and every D18 deep-reading surface. Its job is to ***pass compliance***. **THE SEAM RULE.** **Chrome is always theater, on every page** — the bar does not change register when the body does. **Any page may open with a theater band.** **The moment content becomes studied — tables, floors, legal text, long-form — the page crosses to paper and STAYS there**; it does not cross back inside one page. **Forms are paper.** **Alarms are register-independent** and keep their own colour space in both (this is the D28 guardrail restated as a boundary rather than a warning). **Each page's crossing point is drawn explicitly in that page's P4 tonal map, homepage first** — the seam is a designed line, not an emergent one. **Owner-approved scope:** the homepage's mid-page bands are **re-cast into theater** per the tonal map. This supersedes tonal map v3's register assignments for those bands, and the map is redrawn in the task that implements it (#37) rather than inferred here. |
| D31 | 2026-08-21 | **R2 rulings + the Mint Restraint Law (owner).** **G1-b ADOPTED: the liquid-glass nav.** Tint at the **0.75 D23 lineage floor**, **blur 16px**, over both registers. **The `@supports` fallback is SOLID INK-BOLD**, and the two-state reality is **accepted by ruling** rather than treated as a defect: the nav must be complete and I21-legal in both states. **The braid's mint smearing through the glass is LEGAL** — it is **transmission of a live element**, not accent application, and this is the interpretive note on D29's accent law: the mint is not being *placed* in the chrome, it is being *seen through* it. **G2 breathing field ADOPTED** for theater fields; the **imperceptible-as-motion bar was confirmed live by the owner** rather than inferred from a still. **G3 grain is PROVEN-UNADOPTED** — a working, measured tool with no current application; it is shelved, not rejected. **G4 spring ADOPTED as the site's arrival and interaction grammar** (ζ 0.72, ωₙ 9.2, 620 ms `linear()` family; zero-JS). **framer-motion is removed in this task and is banned as a dependency.** **THE MINT RESTRAINT LAW.** On the noir register, **mint appears in EXACTLY TWO places: the active route (braid / map) and the primary CTA.** Nowhere else, at any opacity, at any size. **N4-A IS REVERSED, with its receipt:** the mobility cards' active state is **spring physics plus a subtle charcoal elevation**, **not neon borders**. Owner's rationale on record — **neon edges on every card turn the page into a "laser tag arena"**, which is the opposite of the expensive-instrument reading N4-A was adopted for. One ring is an instrument; six rings are a game. **The nav's mint underline is retired under the same law.** The bar's boundary is carried by **glass + structure + the NEUTRAL I21 hairline** — I21's mechanism is preserved, its colour is not. **D29's §5 ring amendment STAYS ON THE BOOKS but is DORMANT:** no sanctioned surface currently uses a neon ring. It is not repealed — if a future surface earns one, the amendment already governs it — but **nothing may cite it as licence today.** |
| D32 | 2026-08-21 | **The signature, implemented (braid + physics van at route scale).** **WEAVE GEOMETRY.** The canonical path becomes the serpentine LEAD of a three-strand weave; two companions sit at phase ±2π/3. **Swing 36px peak-to-peak, wavelength λ = curveStart / round(curveStart/780) = 722px (6 full periods) at both 1440 and 1920**, polyline sampled at 14px. The swing extends **LEFT ONLY** and the lead's rightmost x stays exactly `gutterX`, because the measured room to the right of the gutter is **30px at every width** and the van lane already spends 22 of it — there is no rightward budget at any viewport size. An **integer** number of periods means the serpentine begins and ends at `gutterX`, so the terminus cubic is byte-identical to the one that shipped. A **DEADBAND, not a clamp**: below 12px of available room the swing is 0 and the route degenerates to exactly the straight line that shipped (decline-don't-degrade). A bare clamp produced a 2–7px lateral over a 722px wavelength between roughly VW 1196–1220, which reads as a rendering fault rather than as a braid. **CLEARANCE (the #32 finding binds — text directly on a strand measures 1.39:1):** pointwise strand-to-text minimum measured **28.75px at 1024, 1440 and 1920**, and **N/A below lg where the route is not rendered at all**. That is *identical* to the straight line it replaces, by construction rather than by luck. **These numbers are FLOORS.** **SEAM CROSSING → HARD SWITCH, and the reason is arithmetic rather than taste.** Both were built and shot side by side. A short blend necessarily paints the ink-register colour ONTO the paper register for the width of the blend, and electric mint measures **1.49:1 on paper** — so a blend is not a stylistic alternative, it is a contrast violation with a soft edge. The hard switch also co-locates the colour change with the tonal edge, so the line reads as responding to its ground. **ACTIVE LINE** is register-split per D29: `--route-active-ink #2fe89a` (11.62:1 on ink) / `--route-active-light #00a36a` (3.03:1 on paper). **COMPANIONS** are register-aware in the OPPOSITE direction — darker than paper, lighter than ink — at N1-A's maximum recession, just clear of D23's E1-07 divider floor. **LIVERY B RULING: the mint outline is CONSTANT and does NOT switch register.** A 30px object that changes colour mid-travel reads as a glitch, not as design; and the two registers already carry the silhouette differently — over paper it is the ink body against a light field, over ink it is the mint outline against a dark one. One object, one identity, the whole way down. **DYNAMIC PHYSICS — path-time reparameterization.** `p` (scroll) → `s` (arc length) through a precomputed monotone warp; `--route-progress` carries **s**, so the dash, the van and I20 all keep reading one number that means one thing. **Dial: lateral cap 0.15, K 14.** **The dial is governed by DRIFT, not by ratio**: the van's position differs from linear scroll by `max|warp(p)−p| × pathLength`, and once that approaches a viewport height the van simply leaves the screen. Uncapped, the terminus curve (lateral 0.937) dominates and produces one contiguous slow zone — 3.96:1 ratio but **860px** of drift. Capping makes the terminus saturate at the serpentine's own level so the alternating fast/slow largely cancels. **Shipped, measured on the running build: felt ratio 2.12:1, max drift 52px (1.08% of the route).** **SCROLL-OBEDIENCE IS LAW, RESTATED:** the warp is monotone non-decreasing in scroll and the route NEVER moves on its own. D29's split ruling put autonomous drama in the W2 hero script precisely so the page route could stay obedient. **A warp is a reparameterisation; a route that runs on its own clock is an animation and is a violation.** The route now publishes `data-route-span` so an invariant can recompute p and check this from outside. **THE CANONICAL-PATH LAW.** There remains exactly ONE truth: the lead. Van, LUT, dash and station nodes consume it and nothing else; the companions are decorative siblings that are never sampled, measured or ridden. The lead carries `data-route-canonical` and the invariant asserts there is exactly one. **Any refactor that makes a second strand consumable is a second truth and is the exact class C4/FO-1 cost two tasks to close.** **PAINT COST (D20), measured not asserted:** across 4 interleaved rounds with page animations frozen, the shipped braid, the braid with companions hidden, and the whole overlay hidden all reach the same **16.7ms best-case median — the 60Hz vsync floor**. The strands are static geometry rasterized once, and the per-frame JS gained exactly one array lerp. |
| D33 | 2026-08-21 | **Signature rescue (owner field verdict, full-scroll read — BINDING).** **THE VERDICT:** the shipped signature read as ***"a basic scroll-tracker… the complexity is completely missing."*** Accepted without qualification. **TWO CAUSES, BOTH ON RECORD.** (1) **Amplitude starved by the wrong budget:** the swing was capped at 36px against the 30px of RIGHT-hand clearance, while 122px of LEFT budget at 1440 and 362px at 1920 sat unused — and the swing opens leftward, so the right-hand budget never bound it. (2) **N1-A APPLIED OUT OF ITS REGISTER:** maximum recession was ruled on the NOIR bench, i.e. for ink, and carrying it to light produced 1.69:1 — invisible. **FOUR MANDATES, all implemented:** a **visible braid** (charcoal strands genuinely twisting around the active path); **the footer dead-end fixed**; a **z-axis reflection** (blurred glow twin beneath the van — "gliding over liquid glass"); and **spring physics** (the van decoupled from 1:1 scroll, catching up and decelerating like a vehicle). **TWO LAW-BOUND PUSHBACKS, held rather than negotiated.** **The glow is WHITE-OR-MINT, never cyan** (S-007/D29 stand; the reflection is mint-family). **Recalculation drama stays W2 hero scope** (the owner-ratified split): the route gets complexity and weight, it does not get autonomy. **THE REGISTER-SCOPING CORRECTION, and it is more than a re-tune.** The registers are not symmetric. On ink the active lead measures **11.62:1**, so companions at **1.66:1** have vast headroom to recede into. On light the register-split law leaves the lead at only **3.03:1**, so the companions have a **CEILING they never had on ink**: measured, α.58 reaches 3.41:1 and α.72 reaches 5.00:1, and both make the INACTIVE strands out-contrast the ACTIVE one — inverting D29's accent law, which says the live thing is the thing that reads. **Shipped: α.52 → #8e9297, 2.91:1, present and industrial and still below the lead.** **STRAND PRESENCE IS REGISTER-SCOPED LAW: a recession value ruled on one register may not be reused on the other.** **THE NARRATIVE SYNTHESIS — an infinite system, one completed trip.** The COMPANIONS fade through gradient masks at their emergence and again before the seam: a network does not begin at the top of a marketing page or stop above a footer. The **ACTIVE LEAD IS DELIBERATELY UNMASKED** — it is this trip, it has a beginning, and it arrives. **Masks are companions-only.** The arrival now docks at the terminus **STATION NODE** (82/104 of the motif, not its bounding-box centre — the old aim landed the van ~30px short of the station it was arriving at, which is what read as a dangling stop) and lands on `--spring-settle`. FOOTER_HANDOFF and I15 are unchanged. **WELDED TO THE ROAD, NOT TO THE WHEEL (the spring law).** The line's dashoffset stays **1:1 with warped scroll — the line is the CLAIM** and must never lag what the reader has scrolled past. The **van chases it through a damped-spring integrator** (ζ 0.9, ω 12, fixed 1/60 sub-steps, ≤0.1s of physics per frame, self-sleeping at rest, leashed to 0.12 of the route so it can trail but never vanish). **The van's position remains EXACTLY LUT(s_van) every frame: it rides its own PARAMETER, never its own PATH.** **THIS IS THE ONLY SANCTIONED IMPERATIVE MOTION ON THE ROUTE, and it is legal on two conditions that both hold:** it starts only inside the `!reduce` branch, and it is **scroll-target-driven — it has no clock of its own.** Autonomy remains W2's. **I20 EVOLVES TO A THREE-LEG VAN CONTRACT:** (a) **path adherence at the van's OWN parameter** — the old assertion compared the van to `--route-progress` and would now fail on a correct build during any motion, so the route publishes `--route-van` and adherence is asserted there; (b) **convergence** — a flick must produce a real lag AND the van must arrive within 1200ms of the scroll stopping; (c) **target monotonicity** (the #34 leg). |

> **D27(g) TRANSCRIPTION NORMALISED (v2.1, 2026-08-21).** The directive as first recorded read
> *"per-page atmosphere/背景 attention in P4"* — a stray CJK token from prompt transcription.
> Normalised to **"per-page atmosphere/background attention in P4"**. **Meaning unchanged**
> (背景 *is* "background"); this is a transcription fix, not a re-reading of the ruling.

## 5. SITE FACTS (verified 2026-08-17, recon receipt: docs/SITE_RECON_2026-08-17.md)

- **Stack:** Next.js 14.2.35, React 18, TypeScript strict, Tailwind 3.4, framer-motion, Radix nav, Playwright QA. Single hex-sRGB token file (`globals.css`); no OKLCH anywhere.
- **Deploy:** push to `main` → GitHub Actions (tsc TRUE 0 + build) → arm64 image → ghcr.io → Coolify webhook → live. Server never compiles. Branch pushes run checks only.
- **QA:** 18-invariant cube, 3 engines × 6 profiles, regression law, deploy gate = full green + Oli's real-iPhone checklist. Missing: performance budget, throttled old-device profile, visual screenshot diffing.
- **Staged truth:** `launch.ts` flags (LIVE_OPERATIONS, HIPAA_INFRA_VERIFIED, PORTAL_LIVE, HIPAA_EFFECTIVE_DATE) gate every strong claim; flags flip only on explicit owner instruction. This system is kept.
- **Consistency defects on record:** C1 fonts-optional (slow machines keep system fonts), C2 capability forks (glass, van), C3 **MEASURED + HARDENED (D23)**; further device readings may only raise floors, C4 **OBSERVED (FO-1, 2026-08-18)** — route line and livery van desynchronise on a Yoga-class machine; mechanism named in `docs/FIELD_OBSERVATIONS.md`; fix in W5 with a regression invariant (I20). Measure before fixing (P5 builds the measurement).
- **Known SEO state:** homepage indexing issues open; www→apex 301 redirect planned; canonical/OG currently carry the OLD "NEMT company" positioning and will be rewritten in V2.

## 6. THE V2 PROGRAM (the professional order of work)

Each phase produces documents before code. A phase is done when its documents exist in the repo with a FIXLOG entry. V2 is a reposition + deepen + harden, NOT a teardown: the QA cube, the token system, the motion doctrine, and the signature craft (map landmarks, route van, terminus) carry forward unless a documented decision retires them.

**P0 — The machine.** Task 1: this founding commit (ground truth + FIXLOG + recon receipt on `v2`). Task 2: skills audit — list, test, and register every installed skill and tool (Playwright, agent-browser, screenshot tooling first); fix what is broken; output `docs/SKILLS_REGISTRY.md`. Task 3: the board (D13).

**P1 — Research.** Three documented studies, all with VERIFIED / REPORTED source labels. Timeboxed: research ends when the sitemap can be frozen, not when the internet runs out.
- **P1a Competitor teardown** (`docs/DESIGN_RESEARCH.md` part 1): MTM, Verida, Modivcare, SafeRide Health, and 2 to 3 modern healthcare-infrastructure SaaS sites. Full information-architecture inventory: every page they have, every information category, what an MCO sees, what a provider sees. Copy the good, reject the bad, reasons on record. Includes the SafeRide member-eligibility flow (sign-up → payer check → confirmation email) as a pattern study for the platform. Method: global scope, not NEMT-only — select exemplar companies and dissect them anatomically, section by section from navigation to footer, diagnosing every part (structure, technique, why it works) before any page of ours is designed; Mobbin is the reference tool on both the chat and Claude Code sides.
- **P1b Provider requirements research** (`docs/PROVIDER_REQUIREMENTS_RESEARCH.md`): how a transport company actually becomes a compliant NEMT provider in DC, MD, and VA. Government bodies, portals (ePREP/MPRIME, DC OCP/DHCF, VA DMAS + DMV certificate), inspections, driver requirements, insurance minimums (owner reports a $1.5M figure for at least one payer; strategic plan records VA CSL $300K to $1M — VERIFY both, publish nothing unverified). Feeds the provider resource center: state-by-state guides + downloadable PDFs.
- **P1c SEO/indexing strategy** (`docs/SEO_PLAN.md`): diagnose the homepage indexing issue, www→apex redirect, metadata/JSON-LD rewrite for the new positioning, content strategy for provider-guide pages as the organic engine.

**P2 — Sitemap freeze.** From P1: the full page inventory (how many pages, which pages, what each must contain) frozen into `docs/SITE_BUILD_PLAN.md` as numbered items. Starting hypothesis, to be corrected by research: current 12 public pages + Sign-in + provider resource center (per-state guides + PDFs) + security/trust page + FAQ. The frozen list only shrinks; new ideas go to the roadmap section.

**P3 — Design system.** Mobbin/Figma-driven, component-up: tokens reviewed against C1–C4 findings, then universal primitives (buttons, cards, forms, nav), then page-specific compositions. Every choice receipted in DESIGN_RESEARCH part 2. Consolidate `docs/UI_STANDARDS.md` (from the design report + token file) including the motion budget and the performance budget.

**P4 — Build.** Page by page against the frozen plan, one task at a time, design skills mandatory, each task ending with FIXLOG + progress update + board regeneration + commit + push to `v2` (Claude Code does the git, D7).

**P5 — QA extension + measurement.** Add to the cube: throttled old-device profile with a hard performance budget, and cross-engine visual screenshot baselines with diffing. Then measure C1–C4 and decide each: keep the fork, flatten it, or harden the palette. Decisions become D-entries.

**P6 — Copy + launch.** COPY_DECK written last (fast, per the owner ruling "copy is cheap"). Full-cube green, perf budget green, Oli's real-iPhone checklist, then merge `v2` → `main` = deploy.

## 7. QUALITY GATES (nothing merges to main without all of these)

1. tsc TRUE 0, CI green.
2. Full-cube qa:sweep green for shared-chrome changes (existing law, kept).
3. Performance budget green on the throttled old-device profile (from P5 onward).
4. Reduced-motion path for every animation; every capability fork deliberate and on record.
5. FIXLOG + progress updated in the same commit.
6. Owner's real-device checklist. GREEN IS A FLOOR.

## 8. STANDING LAWS

- **Document-first law:** we communicate through documentation, not chat memory. Every decision, change, fix, and reversal gets a written receipt in the repo. Reversals show from where, to where, and why.
- **Documents are created and edited by Claude Code in the repo.** Chat is for design discussion and sign-off only. The owner is never handed files to move by hand; the owner uploads to project knowledge only by choice.
- **FIXLOG discipline:** append-only, reverse-chronological. Every recon/audit prompt begins "Read FIXLOG.md first — do not re-flag anything recorded there as fixed unless you find evidence of regression." Every build prompt ends with appending its FIXLOG entry in the same task.
- **Discovery capture:** anything found mid-task that is not the task's job is recorded immediately (progress JSON discovery inbox), never acted on mid-task, never lost.
- **Challenge-to-test law:** see D12. Enforced by the prompt template on every fix.
- **Impact-check law (D24):** collisions with locked decisions and shipped fixes are named before building; the suggestion register is the only door for outside design input.
- **Field provenance + probe publication law (D26):** a field test without a build-generation probe is not evidence, and a probe that the harness has not executed against a running build is not published.
- **Writing rules (site-facing):** no em-dashes in site copy/UI/payer-facing documents; "Built for HIPAA compliance" only, "100% HIPAA compliant" banned; no PHI ever; sample data labeled; 12-hour AM/PM time; evidence-based voice.
- **Skills law:** every prompt uses and combines relevant installed skills; visual work always includes the design skills (frontend-design, emil-design-eng, impeccable, ui-ux-pro-max, frontend-a11y, ui-styling, design-system, design-is, huashu-design); code/perf work uses react-patterns, react-performance, security-review, tdd-workflow; never cite ui-design:* namespace, javascript-testing-patterns, auth-implementation-patterns. FIXLOG records SKILLS USED per task.
- **Prompt format law:** every Claude Code prompt is explicit numbered steps (nothing implied), begins with the FIXLOG read-first line, ends with FIXLOG append + progress/board update + the git steps (D7: one commit, descriptive message, push to `v2`, never force-push, never touch `main`).
- **Instruction-to-Oli law:** anything the owner must do by hand arrives as a short numbered list, one action per line.
- **Modular type law (D21):** no component may declare a font family; type flows from the token block only.
- **Publish-boundary law (D17):** resource content states requirements with citations and dates; advice-shaped content is banned; disclaimers and verification dates are mandatory on resource pages.
- **Transfer-verdict law (D16):** every design-decision task appends its verdict to docs/PLATFORM_DESIGN_HANDOFF.md in the same task.
- **Positioning language law (D15):** public copy never negates the transportation role; §1 wording is canonical.
- **GREEN IS A FLOOR:** agent-reported green can miss human-visible defects; the owner's real-device check is the last rung, always.

## 9. CURRENT STATE (update every session)

**Task #35 (2026-08-21) — THE SIGNATURE, RESCUED.** The owner's full-scroll verdict on #34 was correct
and is accepted in full. Swing **36 → 122px at 1440 / 140px at 1920**, wavelength **722 → 433px**
(~4 crossings per viewport, up from ~2.5), strands **2 → 3px**. Clearance **held at 28.4px** at every
desktop profile — the amplitude was never in tension with the text, it was in tension with a budget
that did not apply. Light-register strands corrected **1.69 → 2.91:1**, capped by the active lead
rather than by the divider floor. The van now springs (**ζ 0.9 / ω 12**): measured lag **0.010 on a
slow read, 0.045–0.074 on a flick**, converging exact in **~437ms**, self-sleeping at rest. Paint cost
for the bigger braid **and** the blurred reflection: **nothing separates from the 16.6ms vsync floor**.

**Task #34 (2026-08-21) — THE SIGNATURE IS LIVE.** The route is a three-strand braid and the van has
physics. Clearance is **28.75px at every desktop width, unchanged from the straight line**; the warp is
**monotone with zero backsteps at 2.12:1 felt speed and 52px of drift**; paint cost sits at the **vsync
floor**. The seam ships as a **hard switch** because a blend would paint a 1.49:1 colour onto paper.
An architecture review before implementation caught six real defects in the first draft — the warp was
being built for the *un-braided* narrow lane and was worst there; the LUT could hold a stale warp after
a resize; the zoom leg's curved-sample gate was signed and could never see a weave that runs left; and
three stated receipts did not reproduce. All were verified against the code and fixed.

**Task #33 (2026-08-21) — NOIR CHROME IS LIVE.** The first D31 ruling to reach shipped code.
**The bar** is liquid glass at the **0.75 D23 lineage floor**, `blur(16px) saturate(1.25)`, with the
`@supports` fallback rendering the same bar at full opacity — verified as a **real** negative in
Firefox with `layout.css.backdrop-filter.enabled=false`, not simulated. Over the ink hero the two
states converge by construction; they diverge only where the backdrop does.
**The panels** are boxed destinations: a darker well holding cells at the raised-surface fill,
separated **fill-against-fill** (D23's B06 tier doing the job it was derived for) rather than by
borders. Platform features Dispatch across the top row; Solutions is a deliberately even 2×2 because
its four audiences are peers.
**Mint restraint is enforced and proven, not asserted:** a DOM sweep of every computed `color`,
`background-color`, `border-color`, `fill` and `stroke` in the header returns **exactly one** mint
value — the Apply CTA. The magic line, panel icons, item arrows and focus rings all moved to the
neutral on-ink ramp, and the focus ring **gained** contrast in the trade (16:1 white vs 10:1 mint).
**The #29 keyframes fault is dead:** panel open/close is a transition plus `@starting-style`, and a
mid-flight trigger→trigger swap was captured at opacity 0.598 / scale 0.984 with `getAnimations()`
reporting `CSSTransition` — it retargets instead of restarting.
**D25 is executed-dead**, verify-then-delete, with the ink register re-measured on its returned
values (**B06 1.25:1**). **framer-motion is removed** from package, lockfile and node_modules.
**The sub-44px question is answered for the real nav:** every control in the bar and the mobile
overlay now measures ≥44px (9 desktop, 10 mobile, zero under).

**As of August 18, 2026:** P3-W3 CLOSED (D21, implemented Task #14). **P3-W4 CLOSED (D23, implemented Task #18):** the palette is hardened to the owner's measured perception floors — 12 token values moved, F1 (scrolled-nav edge → card tier) and F3 (15 section/chrome boundary rules → `--border-strong`) adopted, and the hex guard is live so the token block is enforced as the single source (DISC-20 closed). Glass alphas were deliberately NOT changed: their read floors (0.75 / 0.60) sit below the shipped values (0.90 / 0.70), and floors-only-rise gives no mandate to move what already clears. **The closing proof is owed and is the owner's:** these values were derived from readings but have not yet been scrolled by the owner on the standard laptop that set the floor. NEXT: W5 — motion system (nexo-motion skill, the C4 fix + I20, motion-safe architecture).

**As of August 20, 2026 — D25 IS LIVE ON THE SITE (T2, implemented Task #27).** The owner picked
temperature **T2 (rose + violet)** from the tuned bench and it now ships on two surfaces: the
**homepage ink hero** (the flagship) and the **final CTA band** (the one light band). The layers are
the bench values verbatim, tokenised in `globals.css` as `--atmo-light` / `--atmo-ink` with the
owner hexes as `--atmo-rose`, `--atmo-rose-soft` and `--atmo-violet`, applied through two utility
classes (`.atmo-light`, `.atmo-ink`) as a **background-image on the section itself** — zero JS, zero
images, zero new network bytes, nothing animated.

**THE COMPENSATED INK PAIR IS SCOPED, NOT GLOBAL, AND THAT IS A DELIBERATE DEPARTURE FROM THE IMPACT
CHECK'S ASSUMPTION.** The bench proved that any usable glow on ink costs `--ink-surface` its D23 B06
separation. Compensating it globally would have lifted every ink surface on the site — nav dropdown
panels, the footer, Stop 3 — where there is no glow and therefore no deficit, and it would have
invalidated Task #18's `--ink-hover` derivation, which was solved against `#192b24`. Custom
properties inherit, so the compensation rides on `.atmo-ink` and reaches only glowing subtrees.
**Verified by computed style:** inside the hero `--ink-surface #273a32`, `--on-ink-border #4a6c66`,
`--on-ink-border-strong #688f82`; at `:root`, in the nav, in the footer and in the light band all
three keep their Task #18 values, and `--ink-hover #2e4d40` is unchanged everywhere (it is nav-only,
and the nav is chrome — a sibling of `<main>`, never inside a glowing section).

**THREE TIERS MOVE, NOT THE TWO THE BENCH NAMED.** The bench modelled only the card edge and missed
the divider tier: D23 E2-05 hardened `--on-ink-border` to **2.10:1**, and the lifted surface drops
the shipped `#405e58` to **1.70:1** — under its own floor. `#4a6c66` restores **2.09:1**. The card
edge behaves as the bench predicted (`#5b8275` would fall to 2.82 under the 3:1 WCAG 1.4.11 tier;
`#688f82` restores 3.36). No floor was lowered.

**AA RE-VERIFIED ON PAINTED PIXELS ON THE REAL PAGES, NOT ON BENCH FRAGMENTS: 57 text runs, 0 below
floor**, across all four console scenes and both registers. The four pairings the glow measurably
lowered all keep enormous margin (hero lede 9.66→9.27, hero secondary CTA 16.17→15.86, band h2
17.28→16.81, band lede 7.51→7.20; floors 4.5 and 3.0). **VIOLET COLLISION: RULED WEATHER, NOT
MEANING** — rendered and viewed, including a deliberate worst case with the violet sun dragged
directly behind the `--svc-wheel` chip at the owner's own alpha, where the chip still reads as a
chip; a 3x-alpha control confirms the test can fail. Violet therefore stays in the ink register.

**ONE MEASURED DEGRADATION IS ON RECORD AND IS THE OWNER'S TO WEIGH:** the console panel's FILL
separation from the field falls from **1.14 to 1.00** at the brightest point of the rose sun. It
breaches no floor that governs it — B06 governs `--ink-surface`, not the `--ink-glass` composite,
and the panel's separation is carried by its EDGE by documented design (Hero.tsx says so in as many
words), which measures **4.40 vs the painted field and 3.13 vs the panel interior**, both clear of
the 3:1 boundary tier. But at 1.00 the fill contributes literally nothing, so the console now leans
entirely on its edge. Restoring fill separation would mean roughly doubling `--ink-glass`
luminance, which would end the hero's kin-to-kin doctrine; that trade was not taken unilaterally.

**WIDER ROLLOUT IS P4 PER-PAGE WORK.** The interior routes deliberately do not wear the treatment
yet: each needs its own painted-pixel pass first. **FO-3 AND FO-1 ARE NOW FIELD-CLOSED (owner, 2026-08-20)**, on executed probe output from
both machines rather than on screenshots: the Yoga at dpr 1.25 returned `errY -0` / `errX 12` (the
designed outbound lane, not drift) / `ctm [1,1]` / `offsetPath "none"`, and the owner laptop at dpr 2
returned `varP 0.6523 = lineP 0.6523` / `gapPx 0` / build NEW. That is the Task #22 architecture and
the Task #25 units fix both confirmed on real hardware, at the settings that used to break, with
build provenance established. **All three field observations (FO-1, FO-2, FO-3) are closed**, field
confirmation is now a validated gate on the board, and a future line-or-van report begins at the
FO-3 probe plus the D26 provenance protocol rather than at re-diagnosis. NEXT: W5 part 2 —
nexo-motion skill + motion-safe conversion + S-001 card motion.
