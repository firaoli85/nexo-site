# FIXLOG — nexo-site

Permanent, append-only, reverse-chronological ledger of every fix, change, and reversal. Newest entry at the top, directly under this header block. Entry format:

### YYYY-MM-DD — task name (short commit)
- **WRONG / BEFORE:** what was wrong or absent, or the state we moved from.
- **CHANGED:** what this task did.
- **VERIFIED:** how it was verified (tsc, qa:sweep scope, manual check).
- **TESTS ADDED:** the challenge-to-test law (D12) — tests created for any hard problem fixed, or "none — no challenge class in this task."
- **SKILLS USED:** which installed skills were used, or "none."
- **AUDIT GUIDANCE:** what a future recon should NOT re-flag because of this entry.

Rule for every future recon/audit prompt: read this file first; do not re-flag anything recorded here as fixed unless there is evidence of regression.

---

### 2026-08-17 — Task #9: P2 freeze (SITE_BUILD_PLAN.md) + ground truth v1.3 (D18–D20) + P1-C1 gate closed
- **WRONG / BEFORE:** Scope existed only as a **draft** buried inside a research document — `DESIGN_RESEARCH.md` §6e carried a 24-page inventory explicitly marked "DRAFT FOR P2 FREEZE, not frozen", and nothing bound it. There was no per-page content specification, no workstream list, no roadmap, and no mechanism preventing scope growth. Three owner rulings were undocumented: **national legibility** (payer evaluators contract nationally, and the site read as a local-only shop), **imagery strategy** (T3 was an open P2 decision, and the hero was on record as hard to grasp), and the **P2 technical ratifications** (the performance bar, the decline-don't-degrade rule, the motion-safe architecture, and the four `nexo-motion` candidates were all still proposals). The homepage indexing question was open pending an owner reading.
- **CHANGED:** **PART A:** `SITE_GROUND_TRUTH.md` → **v1.3**. Appended **D18** (broad legibility, local operations — refines D2, does not reverse it), **D19** (imagery strategy, closing T3 — animation and custom illustration primary, **stock people-photography permanently banned**, real photography deferred until real operations exist, hero gets a new signature animated centerpiece), and **D20** (technical ratification: perf bar as ceiling, decline-don't-degrade as standing law, motion-safe architecture, four `nexo-motion` candidates approved for P3). §9 CURRENT STATE rewritten. **PART B:** created **`docs/SITE_BUILD_PLAN.md` (FROZEN)** — the FREEZE LAW verbatim, the SafeRide information-model ruling, the gates table, **24 pages B1–B24** each with route / audience / one-line purpose / REQUIRED CONTENT checklist / deliverables / gates / source citations, **10 workstreams W1–W10**, **15 roadmap items**, **6 judgment calls (J1–J6)**, and **12 recorded grounding gaps**. **PART C:** `SITE_PROGRESS.json` — P2 phase done, three P2 items closed, D18–D20 mirrored, W1–W10 added as phased items, `activePhase` → P3, `currentTask` → "P3-W3: type-specimen bench". **ADDENDUM APPLIED MID-TASK — the P1-C1 gate is CLOSED** by the owner's Search Console reading: `SEO_PLAN.md` gained **§2.7** with the verbatim readings, `SITE_GROUND_TRUTH.md` §9 now lists only P1-B1 as open, the build plan's gates table cites P1-C1 historically as CLOSED, and `SITE_PROGRESS.json` marks the gate done and logs the Sitemaps observation as a low-priority discovery.
- **VERIFIED:** **The P1-C1 closure, verbatim (OWNER-RELAYED, URL Inspection, 2026-08-17):** `https://nexoaccess.com/` — **"URL is on Google"**, page **is indexed**, last crawl **Aug 9, 2026, 11:14 PM** (Googlebot smartphone), crawl allowed, fetch successful, indexing allowed, user-declared canonical `https://nexoaccess.com/` and **Google-selected canonical = Inspected URL (Google agrees with the apex)**. `https://nexoaccess.com/platform` — **"URL is on Google"**, indexed, last crawl **Aug 5, 2026, 6:44 PM**, Google-selected canonical = Inspected URL, **referring pages include the www variant, consistent with correct alternate handling**. Minor: homepage inspection showed Sitemaps **"Temporary processing error"**; `/platform` showed **"No referring sitemaps detected"** — neither blocking, both logged. **This confirms the Task #8 adversarial refutation by outcome:** the www rows were canonicalization *succeeding*, and the honest UNRESOLVED verdict was correct precisely because there was no defect to find. **Every one of the 24 page entries cites its research source** by document and section. Ledger append-only order verified **D1 → D20** after the edit. Board validator **green on the first run** (7 phases, 37 items, 57.7 KB). `npx tsc --noEmit` **TRUE 0**. `git status` showed only the intended files.
- **TESTS ADDED:** none — documentation task, no site code touched, no cube run. **`I19` remains specified, not built** (W9 builds it in P5 alongside the redirect it guards).
- **SKILLS USED:** `frontend-design` for the IA judgment — specifically the per-page content architecture, the audience-to-route mapping, and the decision to keep segmented payer sub-audiences inside one page rather than splitting into new routes until there is distinct proof per segment. `nexo-brand` as the lens every content spec was passed through: §7 (the honesty gate, which is why `/security` is scoped to four repo-verifiable facts instead of matching competitors' SOC/HITRUST posture, and why `/providers/faq` can honestly answer only about half its inherited question list), §7.2 (which is why `/about` carries only prohibitions), §7.4 (admin excluded from every public surface), and §5/§8 (the W2 constraints box). **Honest note:** a 5-agent workflow extracted the grounding — category matrix, provider publish map, SEO targets and schema assignments, craft verdicts and themes — plus an adversarial completeness critic; **the critic's findings materially changed the plan** (it caught all six contradictions and the twelve gaps). The judgment calls, the scoping decisions, and the rulings on which pages must not ship yet are mine.
- **AUDIT GUIDANCE:** **THE PLAN IS FROZEN. Any future addition to Section 1 (pages) or Section 2 (workstreams) is a freeze violation and belongs in Section 3 (ROADMAP).** Auditors must flag B-item scope growth as a violation, not as an improvement. **Six judgment calls J1–J6 are recorded with reasons and are reversible only by an owner ruling** — do not silently re-decide them: **J1** the owner-question count is **58**, not the 70 in `PROVIDER_REQUIREMENTS_RESEARCH.md` §7's own summary (row count settles it: DC 24 + MD 16 + VA 18); **J2** the **Maryland 16+ passenger insurance tier is UNPUBLISHABLE** because two cells inside one document are both labelled VERIFIED and disagree, and that document itself calls a wrong insurance minimum "actively harmful"; **J3** the "$100,000 GL / 25-50-25" figure set is attributed to **Maryland by one document and Virginia by another**, so the flagship insurance-contradiction page cannot be written until the jurisdiction is resolved; **J4** the matrix headline arithmetic is wrong and the cells win (8 full / 4 partial / 2 gated / 29 absent); **J5** `gated` is a **deploy state, not a content gap**, so the sign-in surface is a flag flip (W7), never build work; **J6** the three state pages are **provider-only**. **Three routes are deliberately NOT shippable yet and their thinness is not a defect to fix by inventing content:** `/providers/credentialing` carries a "CARRY (priority)" label with **zero publish-map rows** behind it and an unmakeable duration claim in its query targets; `/faq` has **no question list anywhere in the corpus** and waits for real inbound; `/notices` is a **legal instrument with no legal source**, so we lay it out and do not author it. **Two obligations are owed by W8 and must not be lost:** D18's payer half has **no SEO query classes behind it**, and the internal-linking spec stops after five bullets. **Never re-flag "homepage not indexed" without a fresh owner Search Console reading; as of 2026-08-17 both inspected URLs are indexed with agreeing canonicals.** Note that only **2 of 13 routes** were inspected, so the other eleven remain unread — that is an unknown, not a defect. The www→apex 301 stays in W8 as **durability work, never rescue** (`rel=canonical` is a hint Google may override); do not drop it because indexing is fine, and do not re-justify it as an indexing fix.

### 2026-08-17 — Task #8: P1c SEO plan (live audit, indexing diagnosis, D15 rewrite specs) + owner taste receipts (§13)
- **WRONG / BEFORE:** The homepage indexing issue was **undiagnosed** — `SITE_GROUND_TRUTH.md:79` recorded "homepage indexing issues open; www→apex 301 redirect planned; canonical/OG currently carry the OLD 'NEMT company' positioning" with no evidence behind any of the three, and no spec for fixing them. Nobody had fetched the live site to establish what was actually true. The provider resource center was identified as the organic engine in Task #5/#6 but had no target queries, no internal-linking design, and no assessment of whether the incumbents were beatable. Separately, the owner's design taste existed only in voice-to-text messages and would have been lost.
- **CHANGED:** **PART A:** appended **§13 OWNER TASTE RECEIPTS** to `docs/DESIGN_RESEARCH.md` (102 lines) — seven references with the owner's stated reasons in an explicit **OWNER-STATED** evidence tier, five themes **T1–T5** (combination over cloning; fonts are a wound; the imagery gap is real user feedback; borderless full-bleed; keep our creativity), and what P2/P3 owe against each. Added a **P2 ratification agenda** item and **DISC-17** to `docs/SITE_PROGRESS.json`. **PART B:** created **`docs/SEO_PLAN.md`** — live current-state audit, adversarially-reviewed diagnosis, four fix specs (www→apex 301; metadata/JSON-LD rewrite to D1/D15; provider-resource-center organic architecture; E-E-A-T posture), and a measurement plan. **PART C:** `P1-C` done, **`P1-C1` added as a blocking owner gate**, phase **P1 → done**, `activePhase` → P2, `currentTask` → "P2: sitemap freeze + ratification session".
- **VERIFIED:** All audit facts are `curl` readings of the live production site on **2026-08-17**. **Apex and www both return HTTP 200 with IDENTICAL `ETag "13x53aqr78w4r74"` and identical 222,048-byte bodies, with zero redirects between hosts** — a true duplicate-host state. **But the cross-host `rel=canonical` is CORRECT on both hosts, including deep pages** (`www/platform` → `https://nexoaccess.com/platform`), and both serve an apex-consistent `robots.txt` and the same 13-URL apex sitemap. **A redirect layer already exists and deliberately preserves host:** `http://www.nexoaccess.com/` → **302** → `https://www.**nexoaccess**.com/` (to www, not to apex), so host normalisation must be added on purpose. **SSR is GREEN, tested rather than assumed** — every `<script>` block was stripped from each response and 6,056 / 1,851 / 2,019 characters of real body text plus 17 / 10 / 9 genuine headings survived, so the App-Router "content only lives in the RSC flight payload" failure mode does **not** apply; a Googlebot user-agent returned byte-identical markup (no cloaking). **DISC-7 CONFIRMED and worse than recorded:** the stem **"manage" appears ZERO times** across the entire indexed metadata surface (title, description, every og/twitter tag, JSON-LD, h1) of all three audited pages, and **one byte-identical `MedicalBusiness` JSON-LD block asserts "Nexo Access is a technology-first NEMT company built for the DMV" on all 13 routes** — the old framing is stated in machine-readable form site-wide from a single string, contradicting `/solutions/providers`'s own correct human-readable "Join the Nexo Access network". Board validator green after two guard rejections (below); board regenerated 54.1 KB, 7 phases, 26 items. **`npx tsc --noEmit` TRUE 0.** `git status` showed only the four intended files.
- **TESTS ADDED:** none — research and planning task, no site code changed and no challenge class fixed. **A new harness invariant is SPECIFIED but not built: proposed `I19`** (assert `301` on `www` → apex with path and query preserved), which must issue a raw request with an explicit `Host:`/`X-Forwarded-Host` header because the sweep addresses `localhost:3300` by port and cannot otherwise exercise a host rule. It is built in P4 alongside the redirect it guards. **The D12 guard fired twice on real input and was right both times** — first rejecting `status: "scheduled"` on the new gate item (six legal values named in the error), then rejecting `DISC-17` for using `title`/`summary` when the discovery schema requires `text`. Both were my errors, corrected; nothing was written until the JSON was valid.
- **SKILLS USED:** `nexo-brand` §6.8/§7.3 (the query-lexicon law, which is what resolves the central tension in the rewrite spec: "NEMT" stays in titles because it is what searchers type, while the *descriptions* carry the management-organization model — naming the service category is not the banned claim, describing *the company* as the operator is) and §7/§7.2 (the copy honesty gate, which is why `HERO_LEDE` is flagged as a COPY GATE event rather than a metadata edit, and why the whole E-E-A-T section is built from signals we can ship honestly). `frontend-design` for the information architecture of the provider center and the internal-linking spine. `webapp-testing` for the fetch-and-inspect method. **Honest note:** `coding-standards` was consulted for the redirect and schema specs but contributed nothing beyond what `nexo-brand` and the existing code conventions already dictate — recording that rather than padding the list. A 13-agent workflow did the parallel live audit, three-jurisdiction query research, competitor SEO teardown, and E-E-A-T research; **the four adversarial verify agents materially changed the conclusion** (below); the diagnosis, specs and synthesis are mine.
- **AUDIT GUIDANCE:** **NEVER re-flag the five `www` "Alternate page with proper canonical tag" rows in Search Console as a defect.** Google's own documentation defines that status as *"This page correctly points to the canonical page, which is indexed, **so there is nothing you need to do**"* (support.google.com/webmasters/answer/7440203, fetched 2026-08-17). They are the documented signature of canonicalization **succeeding**. **One correction to the brief's own phrasing is recorded in SEO_PLAN §1.6:** the task described the "Validate Fix" click as failing "by design", but the verified substance is that the status is *informational*, sits in the "Why pages aren't indexed" table rather than under Errors, and has **nothing to validate** — a stronger statement than "validation fails". Whether the UI exposes the affordance at all is UNRESOLVED (no Search Console access); the operational instruction is identical either way — **leave those rows alone**. **Do not "fix" the diagnosis into something more confident than it is:** the adversarial pass **REFUTED** the intuitive www-causes-indexing hypothesis (crawl budget does not apply at 13 URLs; the cited evidence is the success signature) and rated the authority hypothesis **OVERSTATED** because site age and backlinks are unmeasured. **The honest verdict is CAUSE UNRESOLVED**, and it collapses to a diagnosis with one owner reading — tracked as **`P1-C1`**, which blocks the P4 SEO work. **The www→apex 301 is still specified and still worth shipping, but for durability, not rescue:** `rel=canonical` is a hint Google may override, and a 301 removes that discretion permanently and cannot be silently lost by a future code change. Do not let a future audit either drop the redirect ("the canonical handles it") or over-claim it ("it will fix indexing") — both were tested and both are wrong. All SEO and competitor facts are dated **2026-08-17** and describe live third-party surfaces that change; **MTM is the one to watch** — it runs genuine state-scoped recruitment SEO and currently has zero DC/MD/VA provider pages, so if it templatises, the §3.3 window narrows. Provider pages remain **BLOCKED** under D17 by `P1-B1` (58 owner questions plus OR-1/OR-2/OR-3). **No rankings are promised anywhere in `SEO_PLAN.md`, and no volume, difficulty, or traffic numbers appear in it** — we have no keyword tool and no Search Console API, and any future edit adding such a number must cite a real source or omit it.

### 2026-08-17 — Task #7: P1a craft lane (Stripe/Linear dissection, performance bar, nexo-motion candidates) + D17 publish boundary
- **WRONG / BEFORE:** The craft half of P1a was unstarted. D14 had committed us to a "Mobbin-class animated premium" V2 and to a technique-teardown workstream (animation library, what is animated, loading strategy, degradation strategy), but nothing in the repo recorded *how* an exemplar site is actually built. The specific open question was **how Stripe's WebGL gradient hero degrades**, and the honest answer in the repo was "nobody has looked." Separately, `docs/PROVIDER_REQUIREMENTS_RESEARCH.md` carried owner-supplied figures with no label distinguishing them from primary-source findings, and no law governed what a resource page may say.
- **CHANGED:** **PART A (law):** appended **D17** to the `SITE_GROUND_TRUTH.md` §4 ledger — the resource center states **requirements, never advice**; business-formation, ownership, legal and financial guidance are banned ("form an LLC" and its relatives); every resource page carries an informational-only disclaimer and its verification date; nothing UNRESOLVED or merely REPORTED/OWNER-REPORTED is publishable. Added the matching §8 standing-law bullet. Added a **§5b OWNER-REPORTED block** to the provider research (OR-1 WMATC scope, OR-2 DC workers' comp above 3 workers, OR-3 DC GL $1.5M–$2M) plus a D17 governance banner on its §6 publish map, so owner recollection is a distinct evidence tier and not silently promoted to VERIFIED. **PART B (research):** appended **part 2** to `docs/DESIGN_RESEARCH.md` (523 lines, §7–§12) — Stripe full anatomy (13 homepage sections, nav system, footer system, `/enterprise` deep page, typography) and Linear **motion only**, each dissected on PURPOSE / STRUCTURE / TECHNIQUE / DEGRADATION / VERDICT; a performance bar; and a four-part synthesis (nav+footer verdicts, typography direction, four `nexo-motion` candidates, Maze and ElevenLabs paragraphs).
- **VERIFIED:** Everything in part 2 is a **machine reading of the live page on 2026-08-17**, not recall: headless Chromium at 1440×900, each subject loaded normally and under `prefers-reduced-motion: reduce`, with a `getContext` interceptor installed before page scripts. **The priority question is answered with evidence, and the premise needed correcting: stripe.com's hero is not WebGL today.** `.hero-wave-animation` contains **0 canvas, 0 svg, 0 video, 1 img** — an art-directed `<picture>` with three width-gated sources whose assets **Stripe itself names `wave-fallback-desktop.png` / `wave-fallback-tablet.png` / `wave-fallback-mobile.png`** (WebP, `q=60`, `alt=""`), inside a wrapper Stripe classes `__static`. The **capability gate** was captured on the page's one real canvas (`squeezy-carousel__canvas`, document Y ≈ 11,676): `getContext("webgl", {…, failIfMajorPerformanceCaveat: true, powerPreference: "high-performance"})` → **null**, then `experimental-webgl` → **null**, then `2d` → **granted**. A **control canvas created in the same document** proves the flag is the discriminator, not WebGL absence: plain `getContext("webgl")` → **true**, with `failIfMajorPerformanceCaveat: true` → **false**. Stripe therefore *declines* WebGL on a software rasterizer rather than rendering slowly. **Four limits are stated in §7 rather than papered over:** the harness has software WebGL only (SwiftShader/ANGLE), so Stripe's accepted-GPU path was never observed; **Stripe's stylesheets are cross-origin and unreadable, so no claim is made about its reduced-motion CSS in either direction**; transfer/request counts are single-run with an uncontrolled cache and are labelled indicative, not measurement; and section enumeration is selector-dependent, so pages that collapsed (`/enterprise`, ElevenLabs) are reported as probe limits, not as one-section pages. Board validator green after a status correction (below); board regenerated 50.8 KB, 7 phases, 24 items. `npx tsc --noEmit` **TRUE 0**. No screenshots committed — `scripts/qa/artifacts/research/` was already gitignored from Task #5 and 0 files staged.
- **TESTS ADDED:** none — research and documentation task, no challenge class fixed. **The D12 guard earned its keep:** the progress update initially used `status: "scheduled"` for the new gate item, `npm run board` **refused to write** and named the offending item and the six legal values, and the status was corrected to `todo` (the owner can act on it now; what it *blocks* is P4, which the item note states). The guard behaved exactly as designed, on real input, unprompted.
- **SKILLS USED:** `nexo-brand` throughout as the measuring stick — §0 (reduced-motion non-negotiable, which is what made Linear's posture a **failure to record** rather than a technique to copy), §2 (sequential headings, which is what made Stripe `/enterprise`'s 20 `<h1>` elements a defect and produced the measure clarification), §5 (motion vocabulary and ceilings, which the four `nexo-motion` candidates extend rather than replace), and §1 (the tonal map, against which Stripe's single dark chapter in 13 sections was read). `webapp-testing` for the Playwright instrumentation pattern. Honest note: no subagents this task — every measurement was taken directly, because the whole value of the priority question was in controlling the experiment (the first two runs produced an ambiguous "animating vs static" result that was a **measurement artifact** of screenshotting an off-viewport, not-yet-painted canvas; it was discarded and re-run properly rather than reported).
- **AUDIT GUIDANCE:** Do not re-flag the WebGL hero question as open — it is answered in §8.0 **with the correction that the hero is not WebGL at all**, and the standing rule for any future canvas/WebGL work on this site is recorded there (gate the GPU request behind `failIfMajorPerformanceCaveat: true`, ship a named art-directed static asset as the resting state, never put the gamble in a hero). **Do not treat part 2 as design decisions:** every `nexo-motion` candidate is a proposal for P2/P3 and the performance bar is proposed for P2 ratification; nothing here is approved or published. **Do not "fix" the divergences from Stripe** — the non-sticky header, the light 85-link footer, and the absent sub-nav on a 14,184px page were all examined and deliberately rejected, with reasons, in §11a and §8.4; they are logged as chosen. **Two exemplar defects are recorded as what-not-to-copy and must not be read as endorsements:** Stripe `/enterprise` ships 20 `<h1>` elements (including the logo and duplicated nav-panel labels) and its homepage hero ships two; Linear ships **no first-party `prefers-reduced-motion` CSS** (1 rule of 1359, belonging to the third-party `sonner` toast library) and still runs **103 animations** under reduced motion — measured on same-origin, fully readable stylesheets, so that is a real absence and not an inspection limit. In both cases **our existing law is stronger than what the exemplar ships**. The ElevenLabs `motion-safe:` inversion (opt **in** to motion at the utility level rather than opting out) is flagged in §11d as the strongest adoption candidate in the sample and is a P2 question. All observations are dated 2026-08-17 and describe live third-party sites that will change; re-observe before relying on any specific measurement.

### 2026-08-17 — Task #6: P1b provider requirements research (primary sources DC/MD/VA, comparison table, publish map)
- **WRONG / BEFORE:** Provider requirements were known only from owner memory and from what competitors publish about themselves. Two specific figures were in play and unverified: a **$1.5M insurance minimum** attributed to a payer/broker, and a **Virginia CSL of $300K–$1M**. Task #5's audit guidance had explicitly blocked both from publication pending primary sourcing. Nothing in the repo distinguished state licensure from Medicaid enrolment from broker credentialing.
- **CHANGED:** Created `docs/PROVIDER_REQUIREMENTS_RESEARCH.md` (814 lines). Three jurisdiction chapters (DC, Maryland, Virginia), each covering program structure, the ordered path to becoming a provider, vehicle requirements by class, driver requirements, insurance minimums, ongoing compliance, and open questions written for the owner to answer from experience. Plus: a dedicated **insurance cross-check** section, **verdicts on both contested figures**, a **cross-state comparison table** (the skeleton of `/providers/requirements`), and a **publish map** marking each content block PUBLISHABLE or BLOCKED with its condition. **351 requirements captured, 58 owner questions, 128 sources consulted, 47 sources recorded as unreachable or blocked.**
- **VERIFIED:** Every requirement carries **VERIFIED** / **REPORTED** / **`UNRESOLVED`** with URL and access date (201 / 68 / 82 occurrences). **$1.5M: CONFIRMED** as MTM Health's published DC broker requirement (its DC NET Provider FAQ, live and text-extracted 2026-08-17, states "no less than $1,500,000 each" for general and automobile liability) — **but kept BLOCKED from our site** because the document is undated in substance and **MTM's own archived DC-titled handbook contradicts it at $500,000 CSL**; two independent alternative origins were also verified (Virginia's 7-to-15-passenger tier at $1.5M; WMATC restricted authority at $1.5M CSL). **VA CSL: CORRECTED** — the actual state floor is **$350,000** for a 1-to-6-passenger vehicle (Code of Virginia § 46.2-2053 via Virginia DMV); $350K falls inside the recalled range but matches neither endpoint, and **no Virginia primary source uses the term "combined single limit"**. Board validator green (7 phases, 22 items, 16 decisions, 16 discoveries), board regenerated 49.9 KB. `npx tsc --noEmit` **TRUE 0**.
- **TESTS ADDED:** none — research and documentation task, no challenge class fixed. The board's JSON validator (the D12 guard) was exercised on the progress update and passed.
- **SKILLS USED:** `nexo-brand` §7 (the copy honesty gate, which is why the DC $1.5M figure is CONFIRMED as a finding yet BLOCKED from publication — a verified quotation from an undated, self-contradicted document is not a publishable claim). Honest note: no design skills were used or needed; nothing visual was produced. Four parallel read-only research subagents did the primary-source fetching (three jurisdictions plus a dedicated cross-cutting insurance pass, deliberately overlapping so the highest-stakes numbers got two independent looks); the verdicts, comparison table and publish map are mine.
- **AUDIT GUIDANCE:** **All regulatory facts are dated 2026-08-17 and reflect what a document said on that date, not what is currently operative law. Regulatory currency decays: every figure must be re-verified against its primary source at publication time, and each published page must carry its verification date.** A future recon must not treat this document as current law and must not promote a `UNRESOLVED` item without new evidence. **The $1.5M / VA CSL resolution status is recorded inside the document (§4)** — do not re-open it as an unknown; do re-check it before publishing. **58 owner questions are pending an OWNER-VERIFIED review pass** (DISC-16); owner answers still require a primary source before anything ships. The DC broker contract term is genuinely `UNRESOLVED` and tracked as DISC-15 — MTM's 2021 re-award was "one year plus four option years", so the DC broker could change before `/providers/dc` ships. Two research passes disagreed about whether MTM publishes a DC figure; that is recorded in §4a as a coverage difference (robots-blocked DOC/PDF assets), not a factual conflict, and needs no re-litigation.

### 2026-08-17 — Task #5: P1a industry lane (five-subject IA teardown, category matrix, draft V2 page inventory)
- **WRONG / BEFORE:** There was no documented picture of what the NEMT industry actually publishes. V2 scope was guesswork: SITE_GROUND_TRUTH §6 P2 carried a *hypothesis* page list ("current 12 public pages + provider resource center + security page + FAQ") with nothing behind it. Nobody had checked which information categories competitors surface for payers, providers, facilities or members, which self-description nouns the industry uses, or where our live V1 site is actually thin.
- **CHANGED:** Created `docs/DESIGN_RESEARCH.md` **part 1** (1,641 lines). Five subjects torn down: **MTM** (`mtm-inc.net`), **Modivcare** (`www.modivcare.com`), **Verida** (`verida.com`, formerly Southeastrans), **SafeRide Health** (`www.saferidehealth.com`), **Roundtrip** (`roundtriphealth.com`) — each domain verified by search before fetching, all five resolved live-as-given. Per subject: identity line with the exact self-description noun, full nav map with verbatim labels, page inventory grouped by audience (≈300 page types enumerated), information categories per audience (326 recorded), artifacts (documents, portals, contact patterns), sign-in pattern, and a copy-their-good / reject-their-bad verdict table (111 verdicts). Then §6 SYNTHESIS: a **43-category matrix** scored across the five subjects plus **NEXO V1** (audited first-hand from this repo) and **NEXO V2** (carry/skip with reasons); provider-resource findings for P1b; sign-in patterns for D3; vocabulary findings mapped against D15; and a **draft 24-page V2 inventory marked DRAFT FOR P2 FREEZE**. Added `scripts/qa/artifacts/research/` to `.gitignore` **before** any capture. Updated `docs/SITE_PROGRESS.json` (P1-A1 done, P1-A split to in-progress, DISC-14 added, currentTask → craft lane) and regenerated the board.
- **VERIFIED:** Every competitor claim carries a **VERIFIED** or **REPORTED** label (971 / 74 occurrences in the document); published marketing numbers are labelled VERIFIED *as published claims*, never as facts about the world. Board validator **green** (7 phases, 22 items, 16 decisions, 14 discoveries) and regenerated (47.8 KB). `npx tsc --noEmit` **TRUE 0**. Research screenshot directory confirmed gitignored and empty at commit time (0 files staged).
- **TESTS ADDED:** none — research and documentation task, no challenge class fixed. The board's JSON validator (the existing D12 guard) was exercised on the progress update and passed.
- **SKILLS USED:** `nexo-brand` §7 (the copy honesty gate, which set the rule that competitor *structure* is freely adoptable but competitor *claims* are not — several subjects publish unsourced statistics that we record as observations and never imitate) and its §7.2 self-description lexicon, against which the vocabulary findings were mapped; `frontend-design` (information-architecture judgment for the category matrix and the page-count recommendation). Honest note: both were applied from principles already established this session rather than re-read this task. Five parallel read-only research subagents did the fetching; the synthesis, matrix scoring and page recommendation are mine.
- **AUDIT GUIDANCE:** **All competitor facts in `docs/DESIGN_RESEARCH.md` are dated 2026-08-17 and carry VERIFIED/REPORTED labels. Competitor sites change; a future recon must re-verify before relying on any specific claim, and must not treat a REPORTED item as established.** The **draft 24-page V2 inventory is NOT frozen** — P2 freezes it, and per the standing rule the frozen list only shrinks. Insurance figures seen on competitor sites are **not** verified requirements: P1b must source DC/MD/VA numbers primarily, and the owner-reported $1.5M and VA CSL $300K-$1M figures remain unverified and unpublishable until then. Do not re-flag the V1 gaps recorded in the matrix as new discoveries — they are the documented input to P2/P4.

### 2026-08-17 — Task #4: law updates bundle (D14-D16, positioning decided in nexo-brand, PRODUCT.md, handoff scaffold)
- **WRONG / BEFORE:** SITE_GROUND_TRUTH was at v1.1 without D14-D16, and its §1 positioning still negated the transportation role ("We do not drive"). The `nexo-brand` skill still carried the stale UNDECIDED operating model, permitted only "technology-first NEMT company" as the self-description, and named `huashu-design` as a design co-lead. No `PRODUCT.md` existed, so the `impeccable` skill halted at setup with `NO_PRODUCT_MD`. No `docs/PLATFORM_DESIGN_HANDOFF.md` existed, so D4's cross-repo design law had no artifact.
- **CHANGED:** Five artifacts. (1) **SITE_GROUND_TRUTH.md → v1.2**: §1 rewritten to the de-negated positioning (transportation IS the business, delivered through a credentialed provider network, we answer for every trip); **D14, D15, D16 appended** to the ledger in append-only order; §6 P1a gained the global-scope anatomical-teardown method sentence; §8 gained the transfer-verdict law (D16) and the positioning language law (D15); §9 rewritten to P0-complete with P1a next. (2) **PRODUCT.md** created at the repo root (register, users, purpose, brand personality, anti-references, design principles, accessibility). (3) **nexo-brand amended in BOTH copies**: intro parenthetical, §7.2 heading and body, and the §7.3 query-lexicon reference now carry the decided identity; §0 precedence updated to frontend-design leading with huashu contributing in scope; a CHANGELOG line added to the header. (4) **docs/PLATFORM_DESIGN_HANDOFF.md** created with the three verdicts and eight seeded rows. (5) **docs/SITE_PROGRESS.json** updated (P0-T4 done, currentTask → P1a, activePhase → P1, D14-D16 mirrored, three discoveries resolved, DISC-13 kept open with its workaround note, session appended) and the board regenerated.
- **VERIFIED:** `npx tsc --noEmit` **TRUE 0**. **nexo-brand copies byte-identical** after the edit: `diff` clean, md5 `529ea338ec0b7e3e946e2f3c409d7ae9` on both, 827 lines / 73,051 bytes each. Stale-text sweep on the amended skill: `undecided` 0, `technology-first` 0, `we do not drive` 0; gate MECHANISM intact (LAUNCH flags, the NEVER list, audit-every-string all still present). Board validator green and the board regenerated (44.2 KB, 7 phases, 21 items, 4 FIXLOG entries). **`impeccable` unblocked**: `node .claude/skills/impeccable/scripts/context.mjs` now prints PRODUCT.md and exits 0 instead of `NO_PRODUCT_MD`.
- **TESTS ADDED:** none — documentation-only task with no challenge class fixed. The existing D12 guard (the board's JSON validator, negative-tested in Task #3) was exercised on every edit of `docs/SITE_PROGRESS.json` and passed.
- **SKILLS USED:** `nexo-brand` (the amendment target; its own SYNC RULE governed the two-copy edit). No design skills were needed or used: nothing visual was produced. Honest note — `impeccable` was executed only as a verification probe (to prove the PRODUCT.md block lifted), not consulted for design guidance.
- **AUDIT GUIDANCE:** The **operating model is DECIDED** (D1 + D15). Future recons must NOT flag "undecided" positioning anywhere except as historical record in this FIXLOG and in `src/lib/launch.ts` comments (the code comment is a separate, still-open cleanup — the law now lives in SITE_GROUND_TRUTH §1, which wins). **`impeccable`'s PRODUCT.md block is lifted** — do not re-flag it. **The huashu precedence question is settled** (frontend-design leads; huashu contributes in scope) — do not re-open it. Do not re-flag the absence of D14-D16 from the progress JSON or the missing handoff doc; both now exist. `PLATFORM_DESIGN_HANDOFF.md` is append-only: changed verdicts get a new dated row referencing the old, never an edit in place.

### 2026-08-17 — Task #3: the board (D13) — progress JSON truth, board generator, eye-passed render
- **WRONG / BEFORE:** There was no visual status truth. Progress lived only in prose docs (SITE_GROUND_TRUTH, FIXLOG, SKILLS_REGISTRY), so "where are we" required reading three files and holding the answer in your head. Nothing showed phase progress, open discoveries, gates, or the rejected/deferred history at a glance.
- **CHANGED:** Created `docs/SITE_PROGRESS.json` — the committed status truth (meta, 7 phases P0-P6, 20 items, 13 decisions with D7 carrying its reversal chain, 13 discoveries, 4 gates, skills summary with the 14 canonical picks, sessions), populated with real data from SITE_GROUND_TRUTH.md, FIXLOG.md and docs/SKILLS_REGISTRY.md (no placeholders). Created `scripts/board.mjs` — zero-dependency, Node-only, Windows-safe: it validates the JSON, parses the newest 5 FIXLOG entries, and emits `docs/SITE_BOARD.html`, one self-contained file with inline CSS and **zero network requests** (verified: 0 matches for http/img/script-src/@import), openable from `file://`. Sections in order: YOU ARE HERE banner, phase bars, plan ledger, decisions (D7 rendered as a WAS -> NOW chain with its why), latest FIXLOG, gates dashboard, skills, discovery inbox, THE GRAVEYARD, sessions, footer. Palette values are MIRRORED from `src/app/globals.css` as inline constants with a comment saying so. Added `"board": "node scripts/board.mjs"` to package.json and `docs/SITE_BOARD.html` to .gitignore (the HTML is a local render; the JSON is the truth).
- **VERIFIED:** `npx tsc --noEmit` TRUE 0. `npm run board` writes 40.8 KB / 7 phases / 20 items / 3 FIXLOG entries. Render measured in-browser: document 6,540 px tall, **0 horizontal overflow, 0 page errors**, all 9 sections present, 3 ledger "done" rows, 13 decision cards (1 reversal card), 4 gate tiles, 12 discovery rows, 5 graveyard cards, 14 canonical picks. **Human eye pass performed on the committed artifact** (`scripts/qa/artifacts/skills-audit/board-v1.png`, re-shot after the final JSON edit so evidence and truth match): banner legible on ink, P0 bar fully jade at 3/3, status dots coloured and each paired with a written label, nothing overlapping or clipped.
- **TESTS ADDED (D12):** The board's `validate()` guard is the test — malformed truth must fail loudly, never render a silently-wrong board. **Negative-tested three ways, all exit 1 with a precise message naming the fault:** (1) missing required top-level key -> `missing required top-level key: "gates"`; (2) invalid item status enum -> `item P0-T1 has status "almost-done"; allowed: done, in-progress, todo, blocked, deferred, rejected`; (3) dangling phase->item reference -> `phase P0 lists item "P0-GHOST", which does not exist in items[]`. Positive control passes (exit 0), and the HTML was **not** written by any failing run. A `--check` flag runs validation only, so a future preflight (the deferred GY-4 toolchain check) can call it directly. Temp fixtures deleted.
- **SKILLS USED:** `nexo-brand` (its actual token VALUES from globals.css, mirrored into the board; hierarchy-over-density and colour discipline), `frontend-design` (token plan first, then critique against templated defaults), `emil-design-eng` (restraint: the board ships zero animation because none of it would carry meaning), `frontend-a11y` (AA contrast pairs on the mirrored tokens; **colour is never the only cue** — every status dot is paired with its written label; the phase bar carries an aria-label), `webapp-testing` (Playwright screenshot lane for the eye pass). **Honest scope note:** these principles were applied from the standing set established earlier this session and from the real token file; I did not re-read each SKILL.md during this task. `playwright-cli` was attempted first per the prompt and could not load `file://` (see DISC-13).
- **AUDIT GUIDANCE:** `docs/SITE_BOARD.html` is **gitignored by design** — its absence from a fresh clone is NOT a defect; run `npm run board`. `docs/SITE_PROGRESS.json` is the committed truth and the only file to edit; the HTML is disposable output. Rejected/deferred items (GY-*) are permanent graveyard records, never to be "cleaned up". The board renders `meta.currentTask` verbatim, so each task must advance it: it currently still names Task #3, and the next task's prompt should set it. Do not re-flag the board's system-font stack as a brand deviation — webfonts are deliberately excluded to keep the render network-free.

### 2026-08-17 — Task #2 ADDENDUM v2: repair-first law applied (huashu-design restored, impeccable paths fixed)
- **WRONG / BEFORE:** Owner rulings arrived after the Task #2 commit (`fd51e5d`) was already pushed. The addendum assumed nothing had been committed; it had. Under the original audit, `huashu-design` and `impeccable` were reported as WORKING-with-caveats and their missing/broken files were left as discoveries rather than repaired.
- **CHANGED:** Recorded the **REPAIR-FIRST LAW** in the registry (a skill with missing files gets a repair attempt before any BROKEN verdict). **`huashu-design`: FIXED** — searched five sibling installs (all file-less), every plugin/marketplace cache, and the ECC bundle, then found the GitHub origin `alchaincyf/huashu-design` (`e735935`) and restored `references/` (32), `scripts/` (18), `assets/` (15) into both the repo copy and the user mirror; 43/47 routed paths now resolve (the other 4 are doc placeholders absent upstream). Root cause confirmed upstream: a skills-CLI sync bug (≤1.5.15 synced single files only). The six `assets/bgm-*.mp3` video-export tracks (~26.7 MB) were deliberately excluded with the completion command recorded; net +5.0 MB. **`impeccable`: FIXED-PATH / BLOCKED** — rewrote `.agents/skills/impeccable` → `.claude/skills/impeccable` across `SKILL.md`, five `reference/*.md`, and `scripts/hook-admin.mjs` in both copies (a junction was rejected as machine-local and clone-fragile); `hook-admin.mjs` was included because it *writes* the hook command and would otherwise install a hook pointing at a nonexistent path. Updated CANONICAL PICKS: **general-design lead = `frontend-design`** (+ `emil-design-eng` and the standing set), `huashu-design` contributes where its scope applies, `nexo-brand` remains law above all. Added DISCOVERIES 16 (nexo-brand §0 co-lead vs huashu's scope note, for the queued amendment) and 17 (owner ruling queued as D14: Mobbin-class animated premium, technique + measurement, P1a technique-teardown workstream, new skills via `skill-creator` candidate `nexo-motion`, static-complete SSR retained), and converted discoveries 2 and 4 to RESOLVED.
- **VERIFIED:** `npx tsc --noEmit` TRUE 0. `impeccable` repair proven by executing the now-documented command verbatim — `node .claude/skills/impeccable/scripts/context.mjs` exits 0 and returns `RESOLVED_CONTEXT` (its `NO_PRODUCT_MD` line is the expected, still-open block); `node --check` passes on the edited `hook-admin.mjs`; zero `.agents/skills/impeccable` references remain in either copy. `huashu-design` repair verified programmatically (43/47 routed paths resolve) plus a spot-read of `references/design-styles.md`. The cube was **not** re-run: this addendum changed only `.claude/skills/**` tooling, no site code and no shared chrome, so the Task #2 baseline (234 cells, ALL GREEN, full firefox leg included, not stopped early) stands.
- **TESTS ADDED:** none, and the reason is stated rather than skipped (D12). Both repairs are install/path-integrity classes, not code-regression classes; the durable check is the re-runnable verification in registry §4.1/§4.2 (routed-path resolution count, and executing the documented command). The recommended automation remains the `scripts/qa/toolchain-check.mjs` preflight proposed in the Task #2 entry, which should now also assert skill-companion integrity (routed paths resolve) — deferred to P0 Task 3 or P5 with owner sign-off, since it adds a new harness file outside this task's scope.
- **SKILLS USED:** `nexo-brand` §0 (discovery process); `impeccable` (executed its own `context.mjs` as repair proof); `huashu-design` (target of repair); `webapp-testing` (Tier-1 lane, unchanged).
- **AUDIT GUIDANCE:** Do not re-flag `huashu-design`'s missing companions or `impeccable`'s `.agents/` paths — both are FIXED here; their reappearance would be a **regression**, not a discovery. Do not re-flag the `bgm-*.mp3` exclusion (deliberate, with the completion command on record). `PRODUCT.md` remains legitimately open (DISCOVERY 3, owner-scheduled). The `nexo-brand` amendment now carries **two** items: the stale §7.2 operating-model line (D1) and the §0 co-lead reconciliation (DISCOVERY 16). D14 is queued in DISCOVERY 17 and must be appended to SITE_GROUND_TRUTH.md by the next docs task. **History note:** this addendum was applied as a follow-up commit because `fd51e5d` was already pushed; no history was rewritten and no force-push was used (D7 guardrails).

### 2026-08-17 — Task #2: P0 skills audit (registry, cube baseline, tool fixes)
- **WRONG / BEFORE:** The toolchain was assumed, never audited (D6). No registry existed, so nobody knew which of the ~100 installed skills applied to this site, which overlapped, or which tools actually ran. `playwright-cli` was documented by an installed skill but was NOT on PATH (the npm package `playwright-cli` is deprecated). The V1 QA cube had no recorded full-green baseline on the `v2` branch, and Stage 16.1's `display:"optional"` font fix was still recorded as NOT VERIFIED because its confirming cube run had been stopped early.
- **CHANGED:** Enumerated the whole toolchain from the live filesystem: 103 project skills (= 90 user skills byte-identical + 14 vendored superpowers plugin skills), 89 user skills, 14 plugin skills, 2 MCP servers (`agent-browser`, `mobbin`). Ran Tier-1 functional tests with committed evidence (3 Playwright engines, the full cube, playwright-cli, agent-browser, a live Mobbin MCP query). **FIXED `playwright-cli`** by following the skill's own §Installation: `npm install -g @playwright/cli@latest` (0.1.18, now on PATH, `open` + `screenshot` verified). Classified all 103 skills into Tier 2 (11 standing design skills) and Tier 3 (92) via 11 parallel read-only subagents, then reconciled their circular rulings into one taxonomy. Wrote `docs/SKILLS_REGISTRY.md` (inventory, Tier-1 table, cube baseline, Tier-2 + sync check, Tier-3 by category, canonical picks, MCP division of labor, 14 discoveries).
- **VERIFIED:** `npx tsc --noEmit` TRUE 0 (no TypeScript touched). **THE CUBE: ALL GREEN** — 234 cells (13 routes x 6 profiles x 3 engines), 3,175 invariant checks passed, 0 failures, 0 retries, 0 console errors, 0 `FAIL_*` artifacts; chromium + webkit + firefox each GREEN. Tier-1 evidence committed under `scripts/qa/artifacts/skills-audit/` (5 screenshots, 10.2–11.8 KB each). `nexo-brand` sync check: repo canonical and user mirror are **IDENTICAL** (71,514 bytes, 809 lines, md5 `08d793ac25e63ed0fa4a17784ade7e62`). Agent claims about `impeccable` and `huashu-design` were re-verified by the auditor directly rather than taken on trust.
- **TESTS ADDED:** none in this commit, and the reason is stated rather than skipped (D12). The one thing FIXED was an environment/install gap (a global CLI absent from PATH), not a code-regression class, and the re-runnable Tier-1 procedure in the registry is its durable check. A real guard — a `scripts/qa/toolchain-check.mjs` preflight asserting node/npm/playwright/3 engines/`playwright-cli`/`agent-browser` resolve, wired into `qa:sweep` — belongs in P0 Task 3 or P5 and is **recommended as a follow-up**; it was not added here because this task's authorized staging list covers only the registry, FIXLOG, and the evidence directory.
- **SKILLS USED:** `nexo-brand` §0 (the SKILL DISCOVERY standing process that structures this audit) and §10/§10.1 (the cube law); `dispatching-parallel-agents` / `do` (11-agent parallel classification); `webapp-testing` + `playwright-cli` + `agent-browser` (Tier-1 execution); `mobbin` MCP (functional verification).
- **AUDIT GUIDANCE:** Do not re-flag anything in `docs/SKILLS_REGISTRY.md` §8 as a new discovery — specifically: the stale `nexo-brand` §7.2 operating-model text (D1 supersedes it; amending BOTH skill copies is a future task), `impeccable`'s `.agents/` script-path breakage and its `NO_PRODUCT_MD` setup block, `huashu-design`'s missing `references/`/`assets/`/`scripts/` and its SEO-site self-exclusion, the stale `playwright-cli` skill doc versus its binary, the empty `learned/` skill directory in both trees, the vendored duplicate of the 14 superpowers skills, the generic-skill-versus-nexo-brand doctrine conflicts, and the deprecation of the npm `playwright-cli` package. Also do not re-flag Stage 16.1's font fix as unverified — this task's cube verified it (`/about` x w768 x chromium x I5 now passes; CLS green in all 234 cells). The cube baseline recorded here found ZERO site defects, so any future cube failure is a genuine regression against this baseline, not a pre-existing condition.

### 2026-08-17 — Task #1: found the machine (v2 branch, ground truth, FIXLOG, recon receipt)
- **WRONG / BEFORE:** No document system in the repo. Decisions lived in code comments and lost chat "stage reports." Site positioned as an NEMT company; operating model recorded as UNDECIDED in src/lib/launch.ts.
- **CHANGED:** Created branch v2. Committed SITE_GROUND_TRUTH.md (law, decisions D1–D13), this FIXLOG, and docs/SITE_RECON_2026-08-17.md (full recon receipt: repo inventory, styling system facts, QA cube facts, cross-machine inconsistency candidates C1–C4, doc inventory, session decisions).
- **VERIFIED:** tsc TRUE 0 (no code changed); files present on v2; push accepted; CI check job expected green.
- **TESTS ADDED:** none — documentation-only founding task; no challenge class fixed.
- **SKILLS USED:** none — documentation task.
- **AUDIT GUIDANCE:** Do not re-flag the missing doc system, the UNDECIDED operating-model note, or the C1–C4 candidates as new discoveries; all are on record here and in the recon receipt. C1–C4 are HYPOTHESES awaiting P5 measurement, not confirmed defects.
