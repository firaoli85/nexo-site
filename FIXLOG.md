# FIXLOG — nexo-site

Permanent, append-only, reverse-chronological ledger of every fix, change, and reversal. Newest entry at the top, directly under this header block. Entry format:

### 2026-08-20 — Task #27: D25/T2 atmosphere live — rose+violet system, scoped compensated ink pair, violet ruled weather

- **WRONG / BEFORE:** **D25 was ruled and benched but the site was still cold.** The owner had picked **T2 (rose + violet)**, and the five owner hexes, the exact radial layers and the compensated ink pair all existed only in `docs/` — not one of them was a token, and no page wore the treatment. The bench also left two obligations unmet by construction: **its compensated pair had never been checked against the surfaces the flagship actually uses**, and **its painted-pixel values came from small fragments**, which is not the same measurement as a 1440-wide section.
- **CHANGED:** **`--atmo-rose #f58f9c` / `--atmo-rose-soft #f7929a` / `--atmo-violet #8d6fd3` plus `--atmo-light` and `--atmo-ink`** now live in `globals.css`, carrying the section-8 layers verbatim (rewritten only from `rgba(r,g,b,a)` into the `rgb(r g b / a)` form the file already uses). **Delivery is two utility classes that set a `background-image` on the section itself** — chosen over a `::before` overlay because a section's background already paints beneath every child, so the glow lands under the AmbientMap, the console and the copy **without inventing a z-index or a stacking context**, in a file where `.ambient-map` and `.route-overlay` already contend for that space; it also cannot intercept a pointer, because it is not a box. **No JS, no images, no animation** — a static paint, so reduced-motion has nothing to honour. **Applied to exactly two surfaces: the homepage INK HERO (the flagship) and the FINAL CTA band (the one light band).** The band was chosen over `AudienceTriage` because the tonal map (`nexo-brand` section 6.1) rules that section must stay **CLEAN** — cards and an ambient field fought there once already — and because the final CTA is the last light moment before the ink footer, where warmth reads as the page settling. **Wider rollout is P4 per-page work and is marked as such in both files.**
- **D24 IMPACT CHECK (as supplied, and where it was followed to a different conclusion):** *D23 floors — the ink glow requires the compensated pair (`--ink-surface #192b24` to `#273a32`, `--on-ink-border-strong #5b8275` to `#688f82`); changing `--ink-surface` obligates RE-DERIVING dependents exactly as Task #18 did; I21 re-verified by the sweep; atmosphere never behind alarm UI; hex guard — new colours enter as `globals.css` tokens only; D20 — zero-JS layered CSS radials, zero new assets.* **All of it holds, with one deliberate departure: the compensation is SCOPED to `.atmo-ink`, not global.** The check assumes a global token change. Compensating globally would lift **every** ink surface on the site — nav dropdown panels, the footer, Stop 3 — where there is no glow and therefore no deficit, and it would invalidate Task #18's `--ink-hover` derivation, which was solved against `#192b24`. Custom properties inherit, so the override reaches exactly the glowing subtree and nothing else. **`--ink-hover` needed no re-derivation at all, and the reason is structural rather than lucky: it is nav-only, and the nav is chrome — a sibling of `<main>`, never inside a glowing section.** Its Task #18 math stands untouched (1.59:1 vs the panel, `--on-ink-muted` 4.85:1 on it). **Verified by computed style rather than asserted:** inside the hero `--ink-surface #273a32`, `--on-ink-border #4a6c66`, `--on-ink-border-strong #688f82`; at `:root`, in the nav, in the footer and in the light band all three keep their Task #18 values.
- **THREE TIERS MOVE, NOT THE TWO THE BENCH NAMED — the bench missed one and it was under its own floor.** D23 E2-05 hardened **both** on-ink border tiers, and the bench only modelled the card edge. Measured: the lifted surface drops the DIVIDER tier `--on-ink-border #405e58` to **1.70:1**, beneath the **2.10:1** E2-05 hardened it to. `#4a6c66` restores **2.09:1**. The card edge behaved as predicted (`#5b8275` falls to 2.82, under its 3:1 WCAG 1.4.11 tier; `#688f82` restores **3.36**). **No floor was lowered; floors-only-rise is intact.**
- **VIOLET COLLISION — RULED WEATHER, NOT MEANING.** `--svc-wheel` already owns violet semantically, so this was rendered and **viewed**, not reasoned. In the shipped layout the violet sun sits lower-left and the Wheelchair chip sits in the console at right, so a fair test had to remove the geometry: **the violet sun was dragged directly under the chip at the owner's own alpha**, and the chip still reads unmistakably as a chip — pill shape, light fill, icon, label. **A 3x-alpha control was rendered to prove the test can fail**, and at 3x the violet does read as a coloured field in the right margin. Violet therefore stays in the ink register rather than being held to the light register only.
- **VERIFIED:** **AA re-measured on PAINTED PIXELS on the real pages, not on bench fragments: 57 text runs across both registers and all four console scenes, ZERO below floor.** The four pairings the glow measurably lowered all keep large margin (hero lede 9.66 to 9.27, hero secondary CTA 16.17 to 15.86, band h2 17.28 to 16.81, band lede 7.51 to 7.20, against floors of 4.5 and 3.0). `npx tsc --noEmit` **TRUE 0**; `build:check` green, all 17 routes static, **First Load JS unchanged** (the treatment adds no JS). Full cube **ALL GREEN (`SWEEP_EXIT=0`, chromium + webkit + firefox)** — but **only on the fourth run, and the three red ones are the substance of this task rather than an embarrassment to bury**: run 1 red on I17, run 2 red on I10 after I reused a long-lived server the sweep then adopted (`Reusing server already answering on :3300`), run 3 red on I10 again with a fresh server. **Every one was a harness defect, each proven so by A/B before anything was changed, and no site code was touched to make the cube green.** I19 4/4, I21 16/16 and the I20 zoom leg (worst delta 0.095px against a 1px tolerance) all folded into the exit code. Human-eye pass at **1440 and 390 on both surfaces, viewed**: on light the rose wash right and violet wash lower-left read clearly as atmosphere with the map landmarks intact; on ink the register is quieter, and **noticeably stronger at 390 than at 1440**, because the same percentage radial covers proportionally more of a narrow viewport.
- **ONE MEASURED DEGRADATION, ON RECORD AND NOT PAPERED OVER:** the console panel's **FILL** separation from the field falls **1.14 to 1.00** at the brightest point of the rose sun. It breaches no floor that governs it — **B06 governs `--ink-surface`, not the `--ink-glass` composite** — and the panel's separation is carried by its **EDGE** by documented design (`Hero.tsx` says so in as many words), measuring **4.40 vs the painted field and 3.13 vs the panel interior**, both clear of the 3:1 boundary tier. But at 1.00 the fill contributes literally nothing, so the console now leans entirely on its edge. **Restoring fill separation would mean roughly doubling `--ink-glass` luminance, ending the hero's kin-to-kin doctrine; that trade is the owner's and was not taken unilaterally.**
- **TESTS ADDED / HARNESS FIXED:** **I17 was hardened, and the road to that was the one genuinely surprising part of this task.** The first full cube came back **RED — 3 failing cells**, all WebKit, the only real assertion being `I17 FAIL: Company:open=true/no-indicator` on `/` at w1440, and it **survived the harness retry**. The nav is chrome and sits outside `.atmo-ink`, so a causal link was not obvious, and an early 5-run A/B looked incriminating (atmosphere ON 1/5 pass vs OFF 3/5). **That difference was noise, and running it properly is what showed it:** at N=10 per arm the rate was **IDENTICAL — 9/10 with the atmosphere ON and 9/10 with it OFF**. The diagnostic the harness never printed settled the cause: every failure reported **`all=0`**, meaning the indicator was **not in the DOM at all** at measure time. **Radix mounts the Indicator on open, and `aria-expanded` flips BEFORE that mount lands**, so I17 fixed 340ms wait was racing it, and the failing trigger varied randomly run to run. A **mount-aware** wait passed **10/10 with a worst offset of 0px** across 30 trigger measurements. **The site was never at fault; the probe was.** The wait is now in `scripts/qa/sweep.mjs`. **Detection power was proven preserved rather than assumed:** a 4-leg negative test ran CONTROL plus three sabotages, and the hardened check passes the control and convicts all three — including **S1 FROZEN, the exact Stage-16 Defect B signature, at delta 102px and 208px**. A frozen indicator is mounted with a real width, so the wait returns instantly and the 4px assertion still convicts it; a genuinely absent one still falls through the timeout to the existing no-indicator failure. **A SECOND HARNESS DEFECT surfaced behind the first, and it is the more interesting one.** With I17 fixed the cube came back red again, now on `I10 reduced-motion` with `ERR: Failed to fetch RSC payload for http://localhost:3300/` — **on a different route every run** (`/platform`, then `/solutions/providers`, then `/contact` and `/privacy`). The blamed routes carry no atmosphere, and **the failing prefetch TARGET was always some unrelated page** (`/` or `/apply`), which is the tell: `checkReducedMotion()` drives ALL 13 routes through ONE page and clears its error buffer before each `goto`, but Next prefetches the links in view and **navigating away ABORTS those requests, and WebKit reports each abort as a console error that lands AFTER the next navigation has begun** — so the previous route's aborted prefetch was charged to the current one. **Measured over 12 full sweeps per arm: it fired in 5/12 with the atmosphere ON and 6/12 with it OFF** — marginally WORSE without the change, and with the glow off the failures scattered across seven different routes. **Pre-existing, and not caused by anything this task did.** The fix parks the page on `about:blank` and lets the aborts land there before clearing the buffer and navigating for real. **Verified rather than assumed: 8/8 sweeps clean afterwards, where roughly half had been failing, and a negative leg injecting a genuine console error convicted 13/13 routes**, so the flush blinds nothing — `about:blank` runs no app code. **What is still owed: no painted-pixel invariant exists**, so nothing in the cube would catch a future change that pushes text over the glow below AA. I22 is the natural next addition; it was not built here because the measuring instrument needed five corrections before it was trustworthy, and shipping an unproven probe as law is what D26 forbids.
- **SKILLS USED:** `nexo-brand` as the governing law (section 3 for the violet collision, section 4 glass doctrine for what the console actually is, section 6.1 for the tonal-map ruling that kept the glow off AudienceTriage, section 8 for the mandatory visual loop); `frontend-design` and `emil-design-eng` for the layer construction and the register reasoning; `frontend-a11y` for the painted-pixel discipline. **No subagents** (session policy). `impeccable` was **not** run: this task changed tokens and two section classes rather than authoring a surface, and the eye pass plus the painted-pixel measurement are the checks that fit the change.
- **AUDIT GUIDANCE:** **The compensation is scoped ON PURPOSE — do not "fix" it by hoisting `--ink-surface #273a32` to `:root`.** That would lift the nav panels, the footer and Stop 3 where no glow exists, and it would silently invalidate Task #18's `--ink-hover` derivation. **Any new glowing section must wear `.atmo-ink` (which carries the compensation) rather than hand-rolling a background-image**, or it will glow with uncompensated borders. **The DIVIDER tier is part of the pair now** — three tokens, not two. **`--atmo-*` values are ATMOSPHERE and are semantically silent (D25):** a future change that gives rose or violet a meaning, a state, or an interactive surface is a violation, not a refinement. **Do not re-tune the owner's T2 alphas without a new owner pick** — the values are the owner's, not the implementation's; the one number an implementer may legitimately revisit is the ink rose sun's position, if the console-fill collapse is judged unacceptable. **Painted-pixel measurement is the only valid method here** (D25), and **bench values do not transfer to full-width sections** — section 9 of `COLOR_BENCH.md` records two places where the fragment measurements were wrong in both directions. **I17 races Radix mount by design of the DOM, not by accident — do not "fix" a recurrence by deleting the wait.** Its negative test is the thing to re-run before touching it. **When a probe reports a wall of failures, suspect the probe**: this one manufactured 49 fake failures out of 52 runs before it was corrected, and the five failure modes are written down in that same section so the next author does not rediscover them.

### 2026-08-20 — Task #26: D25 ruled (D2 synthesis, rose-anchored) — receipts + tuned atmosphere bench
- **WRONG / BEFORE:** **The direction was ruled in chat and unrecorded.** D25 existed only as a conversation, the owner's five hexes were uncaptured, and the law that makes the whole direction safe — *warm/pink cannot be an emergency-adjacent signal; incorporate it in backgrounds* — was nowhere in the repo. **No rose-anchored candidates had been rendered at all**, so "how warm" could only be argued. **And the INK register had never been tested for warmth:** the Task #24 bench put glow only on light, which left the site's flagship dark register entirely unexamined.
- **CHANGED:** Ground truth **v1.8** with **D25 inserted into the slot D26 deliberately reserved for it** — nothing existing edited, so append-only holds and the ledger reads D1…D26 in numeric order. **`docs/DESIGN_SUGGESTIONS.md` S-014** captures all five owner hexes with dispositions and credits GitLab per the owner's own take-credit-improve rule. **`docs/color-direction-bench/tuned.html`** is new: three temperatures, four identical fragments each, only the glow layer tokenised, with the Task #24 GROUP-BY comparison mode reused from the start. **`docs/COLOR_BENCH.md` §8** records the exact radial layers, the measurement method, the flagship finding and the pick protocol. **Docs-only: no site code, no token changed, and NO CUBE — nothing the cube covers moved.**
- **VERIFIED:** **THE FLAGSHIP FRAGMENT EARNED ITSELF IN ITS FIRST MEASUREMENT, AND THE FINDING IS ARITHMETIC RATHER THAN TASTE.** **Any usable glow on ink breaks the D23 B06 floor.** Pure ink sits at luminance **0.00651**; the 1.25 floor against `--ink-surface` permits at most **0.00649**. Measured against the shipped `#192b24`: **T1 1.07 · T2 1.10 · T3 1.02** versus 1.25. **The bench FAILS that line rather than excusing it**, because D25 says atmosphere may never push a delta below its floor, and prints the **compensated pair** passing directly beneath: `--ink-surface` **#192b24 → #273a32** (1.32 / 1.34 / 1.25) and `--on-ink-border-strong` **#5b8275 → #688f82** (the lifted surface would otherwise drop the card edge to **2.82**, under its 3:1 tier; #688f82 restores **3.36**), with compensated card text at 10.52 / 6.29 / 6.55. **THE LIGHT REGISTER SHOWS THE EXACT INVERSE and that is the other half of the finding:** the glow darkens the page slightly, so a white card separates **better** than it does today — D23 A06 measured **1.28** against the shipped floor of 1.07. **Light has luminance headroom to spare; ink has almost none.** A bench that tested only light would have shipped this straight into the flagship register. **MEASUREMENT IS BY PAINTED PIXELS**, per D25: the harness renders, **hides the text while keeping its layout**, and takes the darkest painted pixel on light and the lightest on ink. **A trap worth recording: the first sampling pass returned `#4d8ec7` (blue) and `#b27e5c` (orange), colours that exist in no atmosphere here — it was reading subpixel-antialiasing fringes on glyph edges.** Hiding the text is what makes the sample honest. **STATUS ROW VERDICT: alarms hold in all three.** Every pill's text clears AA on its own fill (5.22 / 4.84 / 4.72) and every pill separates from the painted field (tightest case T3: refused 1.16, deadline 1.22). T3 carries the most risk to watch by eye because apricot sits nearest the amber alarm's hue, and that is said in T3's own notes strip rather than buried. Font check (primed): all three families **LOADED**. Screenshots at 1440 and 390 for every temperature, **viewed**. Zero console errors at either width in both group-by modes. `npx tsc --noEmit` **TRUE 0**; board green (52 items).
- **TESTS ADDED:** **None — bench tooling, and there is no challenge class in it.** Recording that rather than inventing one. The bench lives in `docs/` and never enters the app bundle.
- **SKILLS USED:** **`impeccable` ran for real** on the `nexo-site`-scoped variant before handoff; **assessment independence: DEGRADED** (sub-agents not used — session policy restricts the Agent tool — so A and B ran sequentially per the skill's documented fallback). **Four findings: two fixed, two shown to be false positives with measurements rather than dismissed by assertion.** (P1) The card explaining the ink-floor failure sat **after** all three temperatures, so the owner would meet three FAIL lines with no explanation for two screens — the same class as Task #24's protocol placement — **fixed**, moved above them. (P2) `.card-doc` 82ch and `.notes` 80ch exceeded the measure cap — **fixed**, and the eye pass then caught that capping the *width* of a full-bleed strip left the background short with a white gap, so `.notes` is capped by padding instead. (P2) Detector em-dash overuse: of the 16 counted, **6 are true em-dashes and 10 are `--` in CSS custom-property names** written as prose references; the true ones were rewritten and the token names stay, because renaming them to satisfy a cadence heuristic would make the document wrong. (advisory) Detector "numbered section markers, Sequence: 02, 07, 08, 09" — **verified false positive**: the fragment labels are roman numerals, and 02/07/08/09 are gradient alpha stops inside the style block. `frontend-design` + `emil-design-eng` for the temperature design and the hue reasoning; `frontend-a11y` for the painted-pixel discipline; `ui-styling` for the fragments; `nexo-brand` §1 as the law being extended and §3 for the violet collision. No subagents.
- **AUDIT GUIDANCE:** **D25 IS LAW: warmth is atmosphere-only and semantically silent; jade owns interaction and status.** A future proposal that gives warmth a meaning, a state, or an interactive surface is a violation, not a refinement. **The hexes in the bench are candidates, not site tokens**, until the implementation task ships them with a full cube and AA re-verification. **The compensated ink pair (#273a32 / #688f82) is an obligation the implementation inherits**, not an optional nicety — without it the ink card loses its fill separation under any glow. **Do not tune the ink glow to zero to "fix" the floor**: the failure is arithmetic, the compensation is the answer, and deleting the glow would delete the flagship idea. **Measure painted pixels, never composited alphas**, and **hide the text before sampling** or subpixel fringes will lie to you. **The violet in T2 needs a service-level check at implementation** — `--svc-wheel` already owns violet semantically. **The two deep tones (#332750, #522a22) were NOT benched and are unadopted**; they are a separate question from a temperature pick. **Nothing ships from a bench.**

### 2026-08-20 — Task #25: probe retraction + provenance/Syncthing law (D26); FO-3 root cause found and fixed
- **WRONG / BEFORE:** **Both chat-authored field probes were wrong, and their numbers had been cited as evidence.** Probe 1 read `--route-progress` from `document.documentElement`; **the code sets it on `.route-overlay`**, so `parseFloat("")` returned **0 on every machine, every time**, and its `errY` measured only the van's distance along the path. **The Yoga's 2945.6 and the owner's laptop's 2071 were artifacts, not errors** — and they were the basis of the **Task #22 "conviction" narrative**. Probe 2, the supposedly corrected one, threw `getPointAtLength: non-finite` on the owner's machine because Blink serialises `stroke-dashoffset` as **`calc(0.729646px)`** and `parseFloat` of that is **NaN**. **Both were written blind against assumed selectors and assumed serialisation; the owner's visual reports were the only valid field instrument throughout.** Meanwhile the environment itself was unsound: **Syncthing was syncing `.next`, `node_modules` and `.git`** across three machines, a live mechanism for stale and mixed builds that most likely contaminated two earlier field rounds. **And FO-3's real defect had been shipping since the route was built, invisible because two of three engines were being permissive about invalid CSS.**
- **CHANGED:** **`docs/FIELD_OBSERVATIONS.md` FO-3** carries the full retraction of both probes, the reclassification of both artifact JSONs, the correction to the #22 narrative (**architecture stands on its independent merits; the evidence is withdrawn**), the root cause, and a field probe that was **executed in all three engines before publication**. **Ground truth v1.7 with D26** (file sync must never touch build exhaust; field provenance protocol; **probe publication rule**) plus a §8 standing law; **D25 is deliberately left unassigned** and D26 numbered around it so the reserved slot stays open for the colour-direction ruling. **`.stignore` created and committed** at the repo root with `.next`, `node_modules`, `.git`, `out`. **S-013** registers containerization as OWNER-DIRECTIVE/LATER. **THE FIX:** `src/app/globals.css` — `.route-path` now reads `stroke-dasharray: 1px; stroke-dashoffset: calc((1 - var(--route-progress, 1)) * 1px)`. **`scripts/qa/sweep.mjs`:** I20 now parses the computed dashoffset and asserts the line agrees with the variable; the zoom leg gains a **dpr-2 lane**.
- **VERIFIED:** **THE ROOT CAUSE, AND IT WAS NEITHER ZOOM NOR DPR.** As a CSS *property*, `stroke-dashoffset` takes a `<length-percentage>` and **a bare number is not a length**. (`stroke-dasharray` is the lenient one — it genuinely accepts `<number>` — which is why only the offset broke.) Blink coerces and serialises `calc(0.729646px)`; WebKit coerces to `0.72968px`; **Gecko is strict and correct: it drops the declaration, the offset falls back to its initial value 0, and with `dasharray: 1` that paints the ENTIRE path.** So Firefox rendered a **fully-drawn line beside a correctly-placed van** — exactly the reported LINE-FULL/VAN-MID signature. **Measured identical in Gecko at dpr 1, 1.5 AND 2, so the device-pixel-ratio lead that opened the hunt was a red herring; engine strictness was the variable, and I am recording that rather than letting the dpr theory take credit.** **AFTER THE FIX, all three engines at dpr 2 / w1542:** dashoffset serialises `0.618027px` / `0.618117px` / **`0.619808px`** (Gecko fixed), `lineP` equals the variable, head Y equals van Y, **gap 0px**, verdict *OK: line and van agree* at every sampled position. **The retracted probe's own defect was reproduced deliberately:** across 36 samples in three engines, `var(documentElement)` was **empty in every single one**. `npx tsc --noEmit` **TRUE 0**; `build:check` green, 17 routes static; **FULL CUBE RUN because `src/` changed** (see the numbers in the report); board green. **Addendum requirement satisfied from the live DOM:** the element carrying the animated dash is **`path.route-path`, `pathLength="1"`, inside the route `svg`** — and the page contains a **second** dashed element (`line` inside `g.terminus-line`, `dasharray: 5px, 4.5px`) that an assumption-based probe could easily have grabbed instead, which is the concrete reason the publication rule now exists.
- **TESTS ADDED:** **I20 now asserts what the line actually claims.** It previously compared the van against the path *geometry* and never asked what was being *drawn*, which is precisely where the defect lived. It now reads the computed `stroke-dashoffset`, **strips `calc()` before parsing** (the trap that crashed probe 2), fails if the value does not parse, and fails if `1 − dashoffset` disagrees with `--route-progress` by more than 0.02 — with a message that explains the CSS rule rather than just printing a number. **Since Firefox is already a cube engine, this assertion would have caught the defect on the day it shipped.** **Negative-tested by restoring the pre-fix unitless rule in Gecko:** the assertion fires with *"THE LINE DISAGREES WITH THE VARIABLE. dashoffset says the line is drawn to 1 while --route-progress is 0.3243 (raw: 0px)"*, and healthy passes in both engines. A **dpr-2 lane** was added to the I20 zoom leg (10 samples, 5 mid-curve).
- **SKILLS USED:** `systematic-debugging` — this task exists because an instrument lied, and the discipline that broke it was refusing to trust any probe's own claim: measure **what the line actually renders** (computed dashoffset) rather than what the code intends, and identify the dashed element from the live DOM rather than by name. That is also what exposed the second dashed element. `review-animations` for confirming the drawn result across engines. **Honest note: `react-patterns` contributed nothing** — the fix is one CSS declaration and the component was not touched. No subagents.
- **AUDIT GUIDANCE:** **THE TWO ARTIFACT PROBE JSONs (Yoga `errY: 2945.6`, laptop `errY: 2071`) MUST NEVER BE CITED AS EVIDENCE AGAIN.** They measured the van's distance along the path, not an error. **The Task #22 conviction narrative is retracted as evidence; its architecture is not** — do not revert the single-surface design, which stands on separately measured merits. **A field test without the build-generation probe is not evidence** (D26b), and **a probe the harness has not executed is not published** (D26 publication rule) — both prior probes were published blind and both failed. **FO-3's true signature is line-versus-van, measured as dashoffset-versus-transform**, never via the retracted probes. **Do not revert `stroke-dashoffset` to the unitless form** — it is invalid CSS, Gecko is right to drop it, and I20 now fails on it by name. **The dpr-2 lead was a red herring**; if a future report arrives with a dpr number attached, that is not automatically the variable. **`.stignore` is committed deliberately** so every machine inherits it on `git pull`; do not move it to a per-machine setting.

### 2026-08-19 — Task #24: GitLab direction study — dissection, taste receipt, three-direction colour bench
- **WRONG / BEFORE:** **A major owner taste event risked either blind adoption or silent loss.** The owner named GitLab as a colour and starting-design reference, loved the fire gradient, the simplicity and the dark cards, and framed a real positioning thesis: the **warm + technical middle ground** fits a company that is *neither fully medical nor fully technology*. Nothing in the repo could hold that. There was **no rendered comparison** for the colour-direction question, so the choice would have been made from a screenshot of somebody else's site against a memory of ours, and **no way to see the one cost that actually matters** — a warm brand sitting in the same hue neighbourhood as the alarm colours.
- **CHANGED:** **`docs/DESIGN_SUGGESTIONS.md`** gains **S-012**, the taste event, with the owner's reasons preserved and a **three-layer split** (atmosphere / composition / card treatment) so the layers can be ruled on independently. **`docs/DESIGN_RESEARCH.md`** gains **Part 3 (§14a–14f)**, the GitLab dissection in the same five-field method as the Stripe/Linear round. **`docs/color-direction-bench/index.html`** is new: one self-contained page rendering **three directions × four fragments from identical markup**, with live contrast ratios, a **GROUP BY → FRAGMENT** comparison mode, and the D24 impact check written into the page. **`docs/COLOR_BENCH.md` §7** records the bench design, the tuning rationale, the verification and the D25 protocol. **No site code. No token changed. No cube — stated plainly, because this task touches nothing the cube covers.**
- **VERIFIED:** **THE HEADLINE FINDING IS THAT THE THING THE OWNER WANTS IS FREE.** The gradient is **six layered CSS `radial-gradient`s** with alpha falloff, two of them anchored **off-canvas** (`at 104% 55%` and `at 6% 102%`) so the viewer only ever sees falloff, never a hotspot. `canvas` count **0**, `video` **0**, `animation-name: none`, `filter: none`. **Zero bytes, zero JS, no re-paint.** Meanwhile GitLab's page costs **FCP 3812ms, 1434 kB, 250 requests, 804 kB of JavaScript across 136 script requests** — and **none of that is the gradient.** The technique is also **hue-agnostic**, which is why D2 can take the craft without taking the palette. **VERDICTS:** TAKE the gradient technique; ADAPT the hero structure (their h1 tracking of −0.03em independently confirms our bench-measured −0.030em) and the fill-only dark cards, constrained by the D23 E2-05 floor; **REJECT the nav treatment** — they have **no bottom boundary in either state**, which collides with the F1 seam that exists because FO-2 proved the boundary collapsing on real hardware, guarded by I21; REJECT the delivery model against D20. **THE COLLISION IS NOW A NUMBER, NOT AN ARGUMENT:** brand-to-danger hue distance is **164°** for jade and **20°** for the ember warm (**10°** for a rose warm, 25° for GitLab's own orange). The rendered D3 status row shows a second cost I had not anticipated: because the page is warm, the **green "credentials current" pill becomes the visual outlier**, inverting the hierarchy so the quiet good state is the loudest thing in the row. **D23 FLOORS WERE TREATED AS PORTABLE AND BOUND EVERY DIRECTION**, which forced two re-tunes in D3: the warm ink pair from candidates measuring **1.17–1.23 up to 1.27** (jade ships 1.25), and the card edge from **2.48 to 3.49** to clear the 3:1 tier jade clears at 3.47. **A MEASUREMENT CORRECTED MY OWN ANALYSIS MID-BUILD:** the bench first scored text against the theoretical *sun core* and reported D3's lede failing AA at 4.06 — condemning a direction on an assumption. Sampling the **actual painted pixels** in the lede's own column returned **7.37**; the suns are anchored off-canvas so the core is never reachable under text. The bench now reports measured values and demotes the core to an information row. Font check (primed): all three families **LOADED**. Screenshots at **1440 and 390 for every direction, viewed**. All twelve fragment ratio sets pass in both group-by modes; zero console errors. `npx tsc --noEmit` **TRUE 0**; board green (7 phases, 50 items).
- **TESTS ADDED:** **None — this is documentation and bench tooling, and there is no challenge class in it.** Recording that rather than inventing a test to fill the field. The bench is `docs/` tooling and never enters the app bundle.
- **SKILLS USED:** **`impeccable` ran for real against the bench before handoff**, on the `nexo-site`-scoped variant, with **assessment independence recorded as DEGRADED** (sub-agents not used: session policy restricts the Agent tool, so Assessment A and B ran sequentially per the skill's documented fallback). Heuristic average **3.1/4**; snapshot persisted under `.impeccable/critique/`. **Five findings, four fixed:** (P1) the bench's entire job is comparison but it only supported sequential reading, so weighing D1's hero against D3's meant holding the first in memory across two screens — **fixed with the GROUP BY → FRAGMENT mode**, verified at 4 rows / 12 direction blocks / clean restore; (P1) the two-machine reading requirement lived only at the foot of the page — **fixed**, and partially downgraded on review since the core instruction was already in the opening lede; (P2) NOTES strips ran past the 65–75ch cap — **capped at 78ch**; (P2) detector flagged **em-dash overuse, 26 in body copy** — **fixed with 20 sentence-level rewrites** done individually because automatic dash-stripping breaks grammar, **detector now returns `[]`**; (P3) the identical dark-card trio — **accepted, not fixed**, because that fragment exists to render the site's *actual* card system and restyling it would break the comparison it serves. `frontend-design` + `emil-design-eng` for the direction work and the hue-geometry reasoning (D2's warm family is jade's *complement*, chosen so the two harmonise rather than compete). `frontend-a11y` for the ratio discipline in every rendered pairing. `ui-styling` for the fragment construction. `nexo-brand` §1 as the incumbent law under review, and §3 for the `--accent-hover` substitution that the warm field forces. No subagents.
- **AUDIT GUIDANCE:** **THE DIRECTION QUESTION IS OPEN AND NO DIRECTION WAS CHOSEN HERE.** The bench produces **D25** — adopt, synthesize, or hold. **Until D25 the hardened jade palette is law**, and any adoption goes through a full token-implementation task with the cube and AA re-verification, floors-only-rise honoured. **The bench is `docs/` tooling and is never shipped** — it is not site code, it has no invariant, and it must not be imported from. **D23 floors bind every direction**, which is why D3's warm ink and card edge were re-tuned rather than left pretty; a future direction proposal that does not clear the equivalent separations is not a candidate. **The GitLab nav treatment is REJECTED and the reason is a shipped field fix** — do not import "no boundary" from a page whose colour situation is not ours (see §14c and S-005). **`--accent` cannot sit on the warm atmosphere** — measured 4.25 at the worst painted pixel; `--accent-hover` is the designated substitute under `nexo-brand` §3 and holds 5.40. **Credit is given as the owner asked:** the atmosphere technique is GitLab's, read from their CSS and recorded in §14a; what we add is the hue geometry, the AA discipline, and the D23 floors applied to a palette that is not ours yet.

### 2026-08-19 — Task #23: D24 impact-check law + design suggestion register + FO-3 unified-scale evidence
- **WRONG / BEFORE:** **There was no formal impact-check step, and no door for design input.** Suggestions arrived as prose — owner ideas in chat, an external AI audit, Claude's own proposals — with no record of what was decided about any of them or why, and **nothing that checks a proposal against the ledger before it becomes code**. That is not hypothetical: the external audit prescribed *"remove drop shadows, crisp 1px borders only"*, which collides head-on with the **F1 nav seam shipped in Task #20** — a `box-shadow: 0 1px 0` hairline that exists **because the scrolled-nav edge was invisible on the owner's own laptop** (FO-2), guarded by **I21** at four device scale factors. Applied as a rule, a plausible-sounding suggestion would have re-opened a closed field defect and failed an invariant. **And FO-3's work-machine leg was unrecorded**, leaving the defect looking machine-specific when it is not.
- **CHANGED:** Ground truth to **v1.6**: **D24** appended to the §4 ledger (append-only order verified D1…D24) and the impact-check law added to §8. **`docs/DESIGN_SUGGESTIONS.md` created** — schema, six dispositions, and **11 seeded entries**. `docs/FIELD_OBSERVATIONS.md` FO-3 gains the **unified-scale evidence** and a two-machine closing-proof protocol. `docs/SITE_BUILD_PLAN.md` W5 part-2 scope now carries **S-001**. `docs/SITE_PROGRESS.json` mirrors D24, adds DISC-24, and rewrites the owner-check item to the two-machine protocol. **Documentation only — no site code, no cube.**
- **VERIFIED:** **The register earned its existence during the seeding, which is the honest test of a process document.** Of 11 entries only **one** is adopted work (the owner's directive); **two were ALREADY-LAW** — the audit was reading **V1 on `main`**, not v2, and flagged things D21 and D23 had already settled; **four are TEST-FIRST**, routed to a bench rather than to code; **one is a VALID CONCERN** folded into the D23 floors-only-rise watch; **one is LATER** behind D19's photography ban; and **two are rejections** — a blanket rule declined on a collision, and one EVALUATED-REJECTED with a five-point receipt. **THE REJECTION IS THE POINT.** S-011 records the external FO-3 theory (restore `preserveAspectRatio="none"` + `translate(-50%,-50%)`) as EVALUATED-REJECTED, because its prescription is **to restore the exact condition that was present when the defect was first reported**: V1 shipped *with* `preserveAspectRatio="none"` and the FO-1/FO-3 family existed then; Task #19 removed it and pixel-sized the SVG so the CTM is identity, which **I20 asserts by name**; Task #21 measured the stretch mechanism it depends on at **≤32px horizontal** against the **~2900px vertical** actually observed — wrong axis, two orders of magnitude; the field probe printed **`errX: 0.0`**, the opposite of a horizontal stretch (the cited `−12` is the *designed* ±12px lane offset); and the cited `vvScale: 1.236` **appears in no probe output** — ours returned `1`. Its underlying instinct, *one mapping not two*, is honoured and exceeded by Task #22's single-surface architecture. **`npm run board` green** (7 phases, 49 items) and **`npx tsc --noEmit` TRUE 0**. **The board validator rejected my first D24 entry** for using `text` where the schema requires `summary` — the D12 guard doing its job; nothing was written until the JSON was valid.
- **TESTS ADDED:** **None — this is a documentation task and there is no challenge class in it.** Recording that plainly rather than inventing a test to fill the field.
- **SKILLS USED:** `nexo-brand` as the law every disposition was judged against — it is what makes S-005 a *collision* rather than a preference (the flat-aesthetic rule, the §1 tonal law governing register transitions, the §3 colour roles that keep service-level chips distinct from status chips, and the D19 photography ban). `frontend-design` for the honest verdicts, including the two that required saying a suggestion was right about the *problem* and wrong about the *fix* (S-005's shadow is load-bearing; S-010's real issue is already solved by the text-sovereignty rule). **Honest note: no engineering skill contributed** — no code was written, no cube was run, and there was nothing for `react-performance` or `systematic-debugging` to do. No subagents.
- **AUDIT GUIDANCE:** **`docs/DESIGN_SUGGESTIONS.md` IS THE SOLE INTAKE FOR DESIGN INPUT.** Ideas do not go from a conversation into code; they get an S-number, a source, a date, a disposition and an impact note first. **IMPACT CHECKS ARE MANDATORY IN EVERY BUILD PROMPT FROM TASK #24 ONWARD** (D24): map the planned change against the §4 ledger, the FIXLOG AUDIT GUIDANCE lines and the invariant set, name every collision and its resolution, and treat an unresolvable collision as a **STOP-and-surface-to-owner fork — never a silent override**. **THE EXTERNAL FO-3 THEORY IS SETTLED — see S-011 — do not re-open it** without new field evidence from a build ≥ Task #22; five independent measurements stand against it and re-deriving them costs a session. **An external review of the live site is a review of V1**, not of v2: check what a reviewer was looking at before treating an observation as current. **The F1 nav seam is not decoration** — any future "remove shadows" instinct must read S-005 first. **FO-3's closing proof is now a TWO-MACHINE protocol**, and each machine must be tested at *the setting that used to fail it* — Yoga at 100%, work machine at 80% — because testing both at 100% would pass on the work machine for the wrong reason.

### 2026-08-19 — Task #22: FO-3 fixed — the van rides inside the SVG (LUT transform, one raster surface)
- **WRONG / BEFORE:** **The van and the line were two renderers sharing one variable.** The line was an SVG `<path>` scrubbed by `stroke-dashoffset: calc(1 - var(--route-progress))`; the van was an HTML `<span>` beside the svg driven by `offset-distance: calc(var(--route-progress) * 100%)`. **The owner's field probe convicted it.** On the Yoga at browser zoom 100% over Windows 125% scaling (dpr 1.25), build fingerprint `preserveAspect=null` (only true from Task #19 on): `{"progress": 0, "errX": 0.0, "errY": 2945.6, "vvScale": 1, "svgW": "1521px", "winW": 1536}`. The shared variable read **zero**; the LINE rendered that honestly — undrawn, matching the owner's screenshot showing no line — while the VAN sat **2945.6px along the route** holding a value the variable no longer had. `errX: 0.0` means the two agreed perfectly on the horizontal axis and the **entire** disagreement was distance *along* the route. Not paint, not geometry — **state**. The owner's zoom ladder correlates exactly with the *effective fractional* scale: 100%×1.25 = 1.25 large gap, 90%×1.25 = 1.125 small gap, 80%×1.25 = **1.00 perfect** — a rasterisation-boundary signature. And **I20 was blind to it**: the class is unreproducible in lab (Task #21: 51 samples, 4 emulation lanes, 3 engines, all ≤1px).
- **CHANGED:** **ONE RASTER SURFACE.** The van is now a `<g>` **inside** the route svg, in the same user space, positioned per frame by an SVG transform written from the same number in the same frame as the line's dash. **The entire offset-path system is gone** — `offset-path`, `offset-distance`, `offset-rotate`, `offset-anchor`, the imperative `offsetPath` assignment, and the `@supports (offset-path: …)` display gate. Positioning is a **precomputed LUT**: at each geometry change the rendered path is sampled at **N = 256** points into two `Float32Array`s; per frame `update()` interpolates two entries and writes one `transform` attribute. Three nested groups each own exactly one transform so none of them fight: `.route-van` the per-frame translate (a presentation **attribute**, so no CSS rule may set `transform` on it), `.route-van-lane` the ±12px lane, `.route-van-nose` the ±90° facing. `scripts/qa/sweep.mjs`: I20 core + the zoom leg now measure `van.getScreenCTM()`, a **fractional-DPR 1.25 lane** was added, the dead `offsetPath` gates were removed, I15's selector follows the new markup, and the sabotage was retargeted.
- **VERIFIED:** **PER-FRAME COST 4.9µs chromium / 3.7µs webkit / 13.8µs firefox** against a 16 667µs frame — **0.02–0.08% of budget**, with zero per-frame geometry reads and zero layout reads; the scroll handler is still one passive listener, one rAF, one var write. **LUT build 15.3 / 12 / 30ms, paid once per geometry change, never per frame.** **N WAS CHOSEN BY MEASUREMENT, AND THE OBVIOUS CHOICE WAS WRONG:** I started at 512, where the build measured **144ms on firefox** — a visible main-thread stall on a reflow mid-scroll, bought with accuracy nobody can perceive (0.03px). The straight leg interpolates *exactly*, so error exists only in the terminus curve, where chord error is ~`s²/(8R)` = 19²/(8·460) ≈ **0.10px**; measured **0.095 / 0.096 / 0.106px**, 10× under I20's tolerance, at half the cost. **MOTION PARITY VERIFIED IN ALL THREE ENGINES RATHER THAN HOPED:** rotation displaces the van's centre by **0.00px**, the leg flip moves it by exactly **−24.00px**, nose matrices exactly ±90°, `transform-box: view-box` + `transform-origin: 0 0` behaving identically on Blink, WebKit and Gecko. **EYE PASS at zoom 1:** worst delta **0.09px** across 30 slow steps and **0.01px** on hard jumps sampled with no settle — *better* than the Task #19 baseline (0.227 / 0.08); handoff holds at **36px** through the curve, the U-turn, the manual reflow trigger and its removal; I15 gap at the terminus **21px**, unchanged; the van visibly rides the curve head with correct nose and lane. **DOCTRINE RE-VERIFIED ONE BY ONE IN ALL THREE ENGINES:** reduced-motion (progress pinned to 1, van absent, no `data-direction`, spine not armed, line fully drawn), sub-`lg` teardown (zero van/path nodes, overlay `display:none`), the narrow→wide resize-lockout rule, static-complete SSR. `npx tsc --noEmit` **TRUE 0**; `build:check` green, 17 routes static. **A MEASUREMENT SUBTLETY THAT LOOKED LIKE A 12px REGRESSION AND WAS NOT:** `getBoundingClientRect()` on a `<g>` **includes its children's transforms**, so it folded in the deliberate ±12px lane offset; the old HTML parent's border box excluded the child transform for free. I20 now measures the group's **own** origin via `getScreenCTM()` — the point that actually rides the path.
- **TESTS ADDED:** **A fractional-DPR 1.25 lane on the I20 zoom leg**, sampling mid-curve (measured `dx=22.6, p=0.9398, delta=0.095px`). **It is a GUARD, NOT A REPRODUCTION, and the difference is stated in the code:** Task #21 measured this same emulation at ≤1px, so lab emulation demonstrably does not reproduce the field defect; the leg exists so a future regression at fractional DPR cannot pass unseen, and **the owner's field re-test remains the closing proof.** I20's core keeps its meaning — expected-vs-rendered agreement at three scrolled positions, the perturbation leg, and the identity-CTM assertion (still guarding stretch) — retargeted to the new markup. **Negative-tested both:** the core's 7 legs all behave (displaced van fails naming the offset; pre-fix svg sizing fails naming the CTM; stubbed `ResizeObserver` fails naming the stale band; the three deliberate absences pass), and the zoom leg's 3 legs behave with the retargeted sabotage — a CSS `transform` on `.route-van`, which beats the presentation attribute and therefore wins on every frame.
- **SKILLS USED:** `react-performance` — the per-frame budget decided the architecture twice: it is why the LUT exists at all (rather than `getPointAtLength` per frame) and why N is 256 rather than 512. `review-animations` as the parity judge, and it earned it: the ±0.00px rotation-centre and −24.00px lane measurements are the difference between "the motion looks the same" and knowing it is. `react-patterns` for the ref/effect seam — the LUT is built in an effect after commit because `measure()` runs *before* React has committed the new `d` and would sample the previous geometry. `systematic-debugging` for reading the probe as evidence rather than as a symptom, and for isolating a 4000px reading that turned out to be my own cost probe mutating the van transform. `nexo-brand` §5 motion doctrine as law. No subagents.
- **AUDIT GUIDANCE:** **THERE IS NO SECOND COORDINATE SYSTEM AND NO SECOND RENDERER ANY MORE.** Any future van/line desync report must **first verify the build is ≥ this commit** — every FO-1/FO-3 report against an older build describes an architecture that no longer exists, and re-flagging those is rediscovery, not a finding. **THE CHROMIUM-INTERNAL MECHANISM IS DELIBERATELY UNPROVEN.** It was never reproducible in lab, so it was never instrumented, and claiming a diagnosis we could not measure would be worse than saying so plainly. The class was removed instead. **Do not "restore" offset-path** — the whole system was retired on field evidence, and SVG transforms need no `@supports` gate, so the van now renders wherever the svg does instead of being hidden on engines lacking `offset-path`. **Do not set a CSS `transform` on `.route-van`** — it carries a per-frame presentation attribute and CSS would silently beat it (that is exactly how the zoom leg's sabotage works). **Do not measure the van with `getBoundingClientRect()`** — on a `<g>` it includes the deliberate lane offset and will report a phantom 12px error; use `getScreenCTM()`. **N = 256 is a measured trade, not a default** — raising it costs firefox 144ms per rebuild for accuracy below the perceptible floor. **The I20 fractional-DPR leg is a guard, not a reproduction** — a green cube does not mean FO-3 is confirmed dead in the field. **The owner's Yoga re-test at the curve is the closing proof and is pending:** re-run the console probe in `docs/FIELD_OBSERVATIONS.md` FO-3; `errY` should read ~0 at every scroll position and every zoom level, and the zoom ladder should be flat.

### 2026-08-19 — Task #21: FO-3 zoom-class desync — investigated, NOT convicted, I20 zoom leg added
- **WRONG / BEFORE:** **FO-3 (owner field test, Yoga-class laptop):** the van tracks the straight leg and then **ignores the terminus curve**, continuing straight down while the drawn line turns. Build provenance established by **fingerprint** rather than by hash — the owner saw the scrolled-nav seam over light content, which does not exist before Task #20, so the build is at or after `9d2e2e3` and FO-1's fixes were **present**. **And I20 was blind to the whole zoom class:** it only ever sampled at zoom 1, so nothing in the harness could have caught a zoom-dependent divergence in either direction.
- **CHANGED:** **NO FIX SHIPPED FOR FO-3, BECAUSE NOTHING WAS CONVICTED.** What shipped is **coverage**: the **I20 zoom leg**, which asserts van/line agreement **mid-curve** under CSS `zoom` 1.25/1.5 and `Emulation.setDeviceMetricsOverride` 1.25. It is **chromium-only** — zoom is not emulable in the webkit/firefox lanes — and **it says so in its own output**, so a green I20 can never be misread as "zoom verified everywhere". Wired into `run.mjs` beside I19/I21 and folded into the same exit code. `RouteOverlay.tsx` is **unchanged**: the one candidate architecture was tried, measured, and reverted (below).
- **VERIFIED:** **FO-3 DID NOT REPRODUCE — 51 samples, 4 emulation lanes × 3 engines × 3 scroll positions, every one within 1px.** Lanes: `deviceScaleFactor`, `Emulation.setPageScaleFactor` (compositor pinch-zoom), `Emulation.setDeviceMetricsOverride` (closest to real browser zoom), and CSS `zoom` on `<html>`. Worst anywhere: **0.224px** (webkit DSF 1.25), and it was a *constant* offset, not one growing through the curve. **THE PRIME SUSPECT'S OWN PREDICTION FAILED, AND THAT IS THE INFORMATIVE PART.** The reported signature — invisible on the straight leg, appearing at the curve — implies an error **proportional to the path's x**. Every sample recorded `dxFromGutter` alongside the error, so it was directly testable: at `dxFromGutter = 0` and at `dxFromGutter = 172` the error is the same and it is ~zero. `offset-path` was never rejected either; the computed value was read at every sample and was always valid. **`metrics @1.5` is a legitimate skip, not a gap** — the emulated viewport drops below `lg` and the overlay tears down, which is correct behaviour. **THE CANDIDATE FIX WAS MEASURED AND REJECTED, AND I20 IS WHAT REJECTED IT.** `offset-path: url(#route-path)` — pointing the van at the *rendered path element* instead of a copied string — priced beautifully: accepted in **all three engines**, switching a positioned van from `path()` to `url()` moved it **0.00px**, and zero per-frame cost (versus per-frame `getPointAtLength` at **19µs** chromium / **44µs** webkit / **102µs** firefox on a *dev* box — real work on every frame, on the machine class that already struggles, for a defect never reproduced). **It shipped into the cube and I20's perturbation leg failed it: 978px off in chromium, 977.9px in webkit, PERSISTENT through 1500ms; firefox correct.** `url()` has a **stale-reference invalidation bug**: when the referenced path's `d` changes, the van keeps the OLD geometry — geometry updated correctly (path length 6039 → 4839) and the van did not follow. That is a *worse* version of the class it was meant to remove and is precisely the "van on a different path from the line" symptom FO-3 describes. **Reverted.** The shipped `path()` assignment is correct *because* it is re-assigned on every geometry change, which forces the invalidation `url()` skips — so `url()` would need re-assigning too, buying nothing and carrying an engine bug. `npx tsc --noEmit` **TRUE 0**; `build:check` green, 17 routes static; **FULL CUBE ALL GREEN on the reverted build — 234 cells, 3412 checks, 3/3 engines, zero failures**, with I19 (4/4), I21 (16/16) and the I20 zoom leg all printed in-run. **EYE PASS, both lanes.** Zoom 1 (the 100% majority): worst delta **0.227px across 30 scroll steps**, identical to the Task #19 baseline — no regression; the curve frame shows the van sitting exactly at the head of a visibly turning line, with `offsetPath: path("M 146…`. Emulated-zoom lane: the numbers are clean (**0.156px** at p=0.9487, `dxFromGutter` 36.4) but **the screenshot framing under CSS `zoom` did not produce a usable frame of the van** — CSS `zoom` pushes the route to the extreme left edge of the viewport, which is a property of that emulation lane, not a defect. **I am recording that lane as measured-but-not-visually-confirmed rather than claiming a picture I do not have.**
- **TESTS ADDED:** **The I20 zoom leg**, negative-tested in 3 legs: healthy passes; handing the van its **own shifted copy** of the geometry (exactly what the pre-`url()` architecture makes possible) fails naming the zoom context and the offset; restored passes. **Its own negative test exposed two faults in it, both fixed:** (1) it asserted the tolerance only on the *curved* sample, so a **16px error on the straight leg passed** — it now asserts on every sample; (2) its sabotage shifted only the path's `M` command, leaving the **endpoint unmoved**, so samples landing at `p=1` saw no divergence and the sabotage slipped past its own test — it now shifts every coordinate, and the sampler prefers a point genuinely *inside* the curve (`p < 0.999`) over the terminus endpoint.
- **SKILLS USED:** `systematic-debugging` — the entire task is its discipline, and it is what produced the one genuinely valuable result: reproduce first, refuse to force the theory when 51 samples disagree with it, and let the invariant rather than intuition decide whether a change is an improvement. `react-performance` for pricing the per-frame option honestly (measured per-call cost on all three engines rather than asserting "too slow") and for confirming the scroll handler was never touched. `review-animations` for the eye pass at the curve and the U-turn. `nexo-brand` §5 motion doctrine as law. **Honest note: `react-patterns` contributed nothing** — the component ends the task byte-identical to how it started. No subagents.
- **AUDIT GUIDANCE:** **FO-3 IS HELD, NOT FIXED. Do not record it as resolved.** No fix shipped; the one candidate was measured and rejected. **The next instrument is a field console probe, recorded verbatim in `docs/FIELD_OBSERVATIONS.md` FO-3** — run it on the Yoga at the curve; `errorPx` is the whole question. If it is ~0 while the van *looks* wrong, the defect is in what is painted rather than where the element is positioned and the hunt moves to compositing; if it is large, `errX`/`errY`/`dxFromGutter` say which axis and how far into the curve. **DO NOT RE-ATTEMPT `offset-path: url()` without re-testing the reflow case** — it fails I20 at 978px in two of three engines, and the measurement is in FO-3. **The zoom class is now instrumented:** any future field report of a curve-time divergence runs the I20 zoom leg first. **The I20 zoom leg is deliberately chromium-only and says so** — that partial is honest coverage, not an oversight to "fix" by deleting the caveat. **FO-2 is CLOSED in field on both machines** (work: distinct edge; Yoga: subtle edge, as designed) — do not re-open it. **FO-1's van-glue re-test at a verified commit is still owed.** **`RouteOverlay.tsx` is unchanged by this task** — a diff against Task #19 is the correct expectation.

### 2026-08-18 — Task #20: F1 field failure (FO-2) — scrolled-nav edge made scale-proof + I21
- **WRONG / BEFORE:** **The scrolled-nav bottom edge did not exist on the owner's standard laptop** — the OPERATIVE device that set the D23 floors — even though it shipped at `--on-ink-border-strong`, the owner's **own E2-05 floor**, measuring 3.47:1. Two passes in the same session isolated it: card and grid hairlines EXIST (D23 holds) and the `/platform` sticky sub-nav rule reads CRISP. **This was a class the colour floors could not catch and were never able to:** D23 measured *what colour a line must be to be perceptible*, assuming a line would be perceived at all. And nothing anywhere asserted that a chrome edge is actually **painted and visible** — I1–I20 assert structure, contrast tokens, metadata and motion, but no invariant had ever looked at a seam and asked whether a line is there.
- **CHANGED:** **`--nav-seam: rgb(11 21 18 / 0.75)`** in the token block, painted by **`.nav-seam { box-shadow: 0 1px 0 var(--nav-seam) }`**, armed in `Navbar.tsx` by the **same `scrolled` condition** as the border so the page-top no-border design is untouched. It is a hard **0-blur** rule — a hairline, not a glow — so the flat aesthetic holds. **The border token was NOT changed, the D23 floors were NOT softened, and the `/platform` rule was NOT demoted** — all three were confirmed working by measurement in this task. New: `scripts/qa/i21-nav-seam.mjs`, wired into `run.mjs` beside the preflight and I19 and folded into the same exit code.
- **VERIFIED:** **ALL THREE PRIME SUSPECTS WERE ACQUITTED BY DIRECT MEASUREMENT, AND THAT DISAGREEMENT WAS THE FINDING.** The seam was rendered at **DSF 1 / 1.25 / 1.5 / 2 on chromium, webkit and firefox** and pixel-sampled from the PNG (zero-dep decode via `node:zlib`, so physical rows are read rather than inferred). In **all 12 cells** the scrolled state was **armed** (`1px solid rgb(91,130,117)`) and the painted row matched the token at **distance 0**. Not fractional-DPR erasure, not a failure to arm, not the glass swallowing it — **the edge was painted everywhere, the whole time.** **THE ACTUAL MECHANISM IS CONTRAST POLARITY:** a line is seen when it is a **LOCAL EXTREMUM** — different from the nearest different value on *both* sides. Over light content the edge sits **monotonically** between a dark bar (L 0.025) and a bright page (L 0.928) at L 0.195, so the eye folds it into the boundary ramp and reads it as antialiasing — **measured strength 0.00 at every DSF**. Over ink it is a **LOCAL MAX ≈4.0** and always worked, **which is exactly the case the F1 bench fragment tested: that fragment was titled "Nav over ink".** The failure lives in the register F1 never benched. **The owner supplied the control for free:** the `/platform` rule they called CRISP measures as a **LOCAL MIN at 2.36** — an extremum, which is why it reads. One mechanism explains both of their observations. **THE TASK'S FIRST SUGGESTED REMEDY WAS REJECTED ON EVIDENCE:** a 2px border measures **0.00 on the light side at all four DSFs**, because thickness cannot create an extremum that polarity denies; a soft shadow measured 0.00 too. Only the chosen hairline produces an extremum in **both** registers at **every** DSF (light **1.99 / 1.92 / 2.31 / 1.99**, ink 4.27 / 3.19 / 2.16 / 4.26), and its light-side strength is comparable to the `/platform` rule the owner already calls crisp. **Contrast re-verified against the composited nav background:** border vs nav-over-light **3.31:1**, over-tint **3.32:1**, over-ink **4.27:1**; seam vs the page below **7.97:1**; nav text untouched — on-ink **12.35:1**, on-ink-muted **7.38:1**, accent-on-ink **7.69:1**. `npx tsc --noEmit` **TRUE 0**; `build:check` green, 17 routes static; **FULL CUBE ALL GREEN — 234 cells, 3/3 engines, zero failures, zero FAIL artifacts**, with the preflight, I19 (4/4) and I21 (16/16) printed in-run. **EYE PASS** (1440 and 390, DSF 1 and 1.5, screenshots viewed): scrolled over light content the bar now separates from the page as a crisp hairline that reads intentional rather than heavy; at page top there is still **no** edge and the nav merges into the ink hero exactly as designed.
- **TESTS ADDED:** **I21 "the nav edge exists."** It asserts a painted **local extremum** at the seam across **DSF 1 / 1.25 / 1.5 / 2 × w1440 / w390 × light and ink = 16 samples**, floor **1.5** (measured worst on the fixed build is 1.92; the failure mode scores exactly 0). **It deliberately asserts no colour, no token and no border width** — every one of those was perfectly correct while the edge was invisible, which is precisely how FO-2 happened; asserting them would have re-created the blind spot. **Tradeoff stated in the file:** it runs once per sweep like I19 rather than as cube columns, because device scale factor cannot be varied inside an existing cube cell and re-running 234 cells at four DSFs would cost 936 to answer a question about one element; the cost is that it samples only the homepage nav, which is where the reported defect lives. **Self-test, 3 legs, all as specified:** healthy 16/16, sabotage (`border-bottom-color:transparent; box-shadow:none`) fails naming `MONOTONIC, strength 0.00`, restored green. **TWO INSTRUMENT FAULTS ARE RECORDED BECAUSE EACH NEARLY PRODUCED A FALSE FINDING.** (1) `html { scroll-behavior: smooth }` means `scrollTo` animates; frames captured mid-flight show the sticky bar unrepositioned, and the seam appeared **MISSING at three of four scroll positions** until every scroll was made `behavior:"instant"` — I nearly filed that as a site defect. (2) The extremum detector was **wrong twice**: comparing the global minimum against the plateaus fails because at this seam the global minimum IS the bar, so a real 1px dip scored MONOTONIC; comparing against immediate neighbours fails because at DSF 2 a 1px border is two identical rows and neither is a *strict* extremum, so a plainly visible edge scored MONOTONIC. The working version collapses equal runs first. Both lessons are written into the invariant file so the next reader does not repeat them.
- **SKILLS USED:** `systematic-debugging` — the whole task is its thesis: the field report named three suspects, the instruments acquitted all three, and refusing to force the DPR theory onto contrary evidence is what surfaced the real mechanism. `frontend-a11y` for the composited-background contrast re-verification and for the insight that a correct contrast *ratio* and a visible *line* are different claims. `ui-styling` + `frontend-design` for the candidate set and the craft call that a hard 0-blur hairline is a rule rather than a glow, so the flat aesthetic survives. `react-patterns` only lightly — the change is one conditional class on an existing state, and no structure, state or boundary moved; recording that rather than padding. `nexo-brand` §1/§5 as law (the tonal registers are why "over light" and "over ink" had to be measured separately). No subagents.
- **AUDIT GUIDANCE:** **PAINTED-EDGE EXISTENCE IS NOW INSTRUMENTED. Any field report of a missing chrome edge runs I21 FIRST** (`node scripts/qa/i21-nav-seam.mjs`) — it answers in one command whether the line is painted, and at which DSF and register it fails. **Do not re-open the fractional-DPR theory for this edge:** it was measured and acquitted on 3 engines × 4 DSFs, and the record in `docs/FIELD_OBSERVATIONS.md` FO-2 says so with the matrix. **Do not "simplify" the fix by thickening the border and dropping the seam** — 2px measures 0.00 on the light side at all four DSFs; the two elements cover different registers and both are load-bearing. **The D23 floors were NOT softened by this task and the `/platform` rule was NOT demoted** — both were confirmed working; any future change there is a regression, not a follow-through from this one. **The page-top no-border state is deliberate** and the seam is armed on `scrolled` for exactly that reason; do not move it onto `.nav-glass` unconditionally. **Any probe that scrolls this site must use `behavior:"instant"`** — smooth scrolling produces lagged sticky-bar frames that read as missing chrome. **FO-1's van verdict remains PENDING**, not resolved: the owner's 2026-08-18 screenshots showed mode-2-signature divergence but the build provenance was unestablished, so no verdict was recorded either way. **The owner's combined field re-test at a verified commit is the closing proof for BOTH FO-1 and FO-2** — everything here is verified by instrument and by eye on a dev-class machine, not on the laptop that reported the failures.

### 2026-08-18 — Task #19: W5 part 1 — C4 fix (single-truth route geometry + reflow re-measure) + I20
- **WRONG / BEFORE:** **FO-1 was live on V1 and the mechanism had two independent halves.** `geo.d` was consumed by two renderers in two coordinate systems: the line as `<path>` inside an SVG sized by CSS (`h-full w-full`) with `preserveAspectRatio="none"`, and the van via CSS `offset-path: path()`, which reads the same string as **raw pixels with no CTM at all**. Any drift between the host's live width and the measured `geo.w` sheared one against the other. Separately, `measure()` ran on **mount and `window.resize` only** — no `ResizeObserver`, no `MutationObserver` — so any post-mount reflow that never resizes the window (fonts settling under `display:"optional"`, images landing, content toggling) left `geo.h`, `geo.top` and the `docStart`/`docSpan` progress mapping describing a page that no longer existed. **And nothing anywhere asserted that the line and the van agree**, which is why a defect this visible reached a real machine through a 234-cell cube.
- **CHANGED:** **Both halves shipped, because the measurements showed the two failures need different medicine.** **(1) One coordinate system:** the route SVG is now sized in **pixels** — `width`/`height` attributes *and* inline style set to `geo.w`/`geo.h`, `preserveAspectRatio` removed, `viewBox` carrying the same two numbers — so the CTM is forced to **identity** and a stretch is not representable. `h-full w-full` is gone and a comment says it must not come back, because CSS beats presentation attributes and would silently reintroduce the shear. **(2) Geometry currency:** a rAF-debounced `ResizeObserver` on `document.documentElement`, the measured region and the host, alongside the existing mount + `resize` path, with an **equality guard** (`sameGeo`) so an unchanged geometry never re-renders — which also stops the observer feeding itself. **THE SPEC'S `getPointAtLength()` OPTION WAS REJECTED ON A PERF READING, NOT A PREFERENCE:** it would have added a geometry read to **every frame** to fix the *smaller* of the two failures, while pixel-sizing costs **nothing** per frame. The scroll handler is untouched — still one passive listener, one rAF, one CSS var write, zero per-frame layout reads. `scripts/qa/sweep.mjs` gains **I20** and `scripts/qa/i20-negative-test.mjs` is new.
- **VERIFIED:** **REPRODUCED FIRST, ON THE UNFIXED PROD BUILD, WITH NO WINDOW RESIZE** (systematic-debugging law) — then re-measured after. Mode 1, trigger = change the host's rendered width post-mount (what a scrollbar appearing or disappearing does): van-to-drawn-curve **1.52px / 6.08px / 31.67px → 0.00px / 0.00px / 0.00px** at −15 / −60 / −200px, and svg CTM `a` **0.9896 / 0.9583 / 0.8611 → 1.0000**. Mode 2, trigger = inject 1500px of document height post-mount: route end **1536px above the footer seam → 36px**, the intended handoff. Combined: **1.52px / 1536px → 0.00px / 36px**. `npx tsc --noEmit` **TRUE 0**; `build:check` green, 17 routes static; **FULL CUBE ALL GREEN — 234 cells, 3410 checks (up from 3176, exactly +234 = one I20 per cell), 3/3 engines GREEN, zero failures, zero FAIL artifacts.** **A MEASUREMENT CORRECTED FO-1's STATED MECHANISM.** FO-1 reasoned that the non-uniform stretch changes arc-length parameterisation and therefore "expresses vertically — precisely the reported van floating far below the line". It does not. Across every scroll position and width delta, the **vertical** component stayed within **0.09px** while the horizontal component carried **100%** of the divergence, tracking `(1 − ctm.a) × pathX` exactly: the browser parameterises the dash in **user space** and applies the CTM afterwards, so a horizontal-only stretch **cannot** express vertically. Since this path's x never exceeds ~228, mode 1 is bounded near **32px even for a 200px width error** — too small to be the reported symptom. **Mode 2 is therefore the dominant field failure**, and it is the only measured mechanism with vertical magnitude. **DOCTRINE RE-VERIFIED ONE BY ONE:** reduced-motion pins `--route-progress: 1` with `stroke-dashoffset: 0%`, no van, no `data-direction`, spine not armed; sub-`lg` tears down to zero route/van nodes with the overlay `display:none`; narrow→wide resize still initialises (the Stage-1 resize-lockout rule); no-JS renders the overlay shell only. **HUMAN EYE** (1440, prod build): worst line/van delta **0.227px across 30 slow scroll steps** and **0.08px across hard jumps sampled with no settle time**; U-turn leg flips at the terminus and the van sits exactly on the drawn head in its ±12px return lane. **The manual reflow trigger is the one that matters — +1400px injected into the live document with no resize: handoff stayed 36px, delta 0.011px, and the van stayed glued through the reflow, the terminus, and the removal.** I15 cross-check at the terminus: no text overlap, van bottom to footer card **21px**.
- **TESTS ADDED:** **I20 "the van rides the line"** — the D12 receipt for FO-1, shipped in the same task as the fix. **Tolerance 1.0px, calibrated rather than guessed:** measured agreement on the fixed build across chromium/webkit/firefox × 1024/1440/1920 × 5 scroll positions (**45 samples**) was **max 0.11px, p95 0.048px, median 0.003px**, and the smallest divergence reproduced on the unfixed build was **1.52px** — so 1.0px sits ~9× above the worst observed sub-pixel noise and below the smallest real defect. It samples **three** scroll positions including one deep in the terminus curve where any shear is largest; it asserts the **CTM is identity by name**, so a future `h-full`/`w-full` or `preserveAspectRatio` regression fails with an *instruction* rather than a number; and it carries the **perturbation leg** — a post-mount document growth with no window resize, the FO-1 trigger encoded forever. Deliberate absences (interior routes, sub-`lg`, reduced-motion, no `offset-path`) are **skips that assert the absence**, never failures. **`scripts/qa/i20-negative-test.mjs`, 7 legs, all behaving as specified** — it drives the *real* exported check, not a copy of its logic, against a displaced van (fails naming the 40px offset), the pre-fix svg sizing (fails naming the CTM), and a **stubbed-out `ResizeObserver`** that recreates mode 2 exactly (fails naming the stale band) — then confirms green again and confirms all three deliberate-absence skips pass. **TWO SELF-INFLICTED ERRORS THE PROCESS CAUGHT, BOTH WORTH RECORDING.** (a) The negative test found **my own I20 handoff maths mixed coordinate spaces** — I added `root.style.top`, which is host-relative, to a document-relative seam — and it reported a constant ~65px error on a *healthy* build. Without the negative test that would have shipped as a permanently-red or permanently-mis-tuned invariant. (b) **I20 gated one entire cube run invisibly:** the printed column list is hardcoded separately from the pass/fail gate, so I20 was running and could have failed the build while no reader could see it had run. Fixed, the whole cube re-run so the reported result actually shows the column, and the list now carries a comment saying why it must stay in sync.
- **SKILLS USED:** `systematic-debugging` (the reproduce-before-fixing law, which is what turned "the spec says arc-length divergence" into a measurement that disagreed with the spec). `react-performance` for the per-frame budget that decided the architecture — the scroll path was audited to confirm nothing was added to it, and the `ResizeObserver` was rAF-debounced and equality-guarded so reflow work stays off the frame path. `react-patterns` for the functional `setGeo` guard and the observer's cleanup discipline. `review-animations` for the eye pass — the fixed motion still has to *feel* right, which is why the slow/fast scroll and the U-turn were watched rather than only measured. `nexo-brand` §5 motion doctrine as law (transform/opacity only, reduced-motion static end-state, the `@supports` gate) and §8 the visual-verification loop. No subagents.
- **AUDIT GUIDANCE:** **THE LINE AND THE VAN NOW SHARE ONE GEOMETRY. Any divergence report against v2 is a REGRESSION, not a rediscovery — run I20 and `scripts/qa/i20-negative-test.mjs` before anything else.** **Do not re-add `h-full`/`w-full` or `preserveAspectRatio` to the route svg**: that is precisely the pre-fix state, I20 fails on it by name, and the comment above the element says so. **The ±12px lane offset is not drift** — the van is deliberately offset perpendicular to the route by leg (`.route-van-lane`), so a screenshot showing the van beside the line is correct; I20 measures `.route-van`, whose own border box is not moved by the child's transform. **FO-1's arc-length reasoning is superseded by measurement** — the divergence is horizontal, mode 2 dominates; do not restore the old explanation. **The live V1 defect on `main` remains until v2 deploys**, by the recorded default ruling (v2/W5 path, not a `main` hotfix) — **do not re-flag FO-1 field reports made against `main` as new findings.** **W5 IS NOT DONE:** this task delivered the C4 fix and I20 only. The four `nexo-motion` candidates are not built, the skill is not authored, and the motion-safe conversion has not started. **THE FIELD RE-CHECK IS OWED AND I CANNOT SUPPLY IT:** this is verified by reproduction and a green cube on a dev-class machine, not on the Yoga-class machine that showed the defect. Until that machine sees it, the field leg is unverified and is recorded as such.

### 2026-08-18 — Task #18: P3-W4 token hardening (D23) — perception-floor palette, F1/F3, hex guard
- **WRONG / BEFORE:** **C3, the founding wound, was still open: the palette's deltas were set on good screens.** `--bg` sat 1.04:1 from a white card, `--border` 1.17:1 on white, `--ink-surface` 1.10:1 on ink — separations that exist in the token file and not on a standard laptop. A card that does not lift is not a card; a border that vanishes takes the card's SHAPE with it. Both F-section fragments collapsed on both of the owner's machines. And `--on-ink-border-strong`, the tier **named** for card edges with a ≥3:1 brief, measured **2.85:1 against `--ink-surface`** — the surface it actually sits on. It had only ever been checked against flat `--ink` (3.15:1), so the tier had never once met its own floor where it is used. Nothing enforced the token law either (DISC-20): `nexo-brand` §0 has banned hex outside `globals.css` since Stage 1, enforced only by review.
- **CHANGED:** **Twelve token values**, each converted through the bench's own `mix(from, to, k)` at the read rung rather than re-invented — `--bg` `#fafbfc`→`#f5f7f9`, `--surface-hover`→`#e7ebf1`, `--surface-tint`→`#e9f3ef`, `--surface-alt`→`#ebeff5`, `--input-bg`→`#eff3f7` (all A06, 2.0×); `--border` `#ebedf1`→`#c3c9d5`, `--border-strong` `#dcdfe4`→`#969fae`, `--surface-tint-border`→`#93c0ab` (E1-07, 3.0×); `--ink-surface` `#12201b`→`#192b24` (B06, 2.0×); `--on-ink-border`→`#405e58`, `--on-ink-border-strong` `#4a6b60`→`#5b8275` (E2-05, 1.3×); `--ink-hover` `#264035`→`#2e4d40` (**re-derived, see VERIFIED**). All in `src/app/globals.css` — the payoff of the single-source law: twelve values, one file, every route follows. **F1:** the scrolled-nav bottom edge moves from the divider tier to the card tier (one line in `Navbar.tsx`); the page-top no-border state is unchanged by design. **F3:** the rule half shipped to all **15** section and sticky-chrome boundaries (`border-t/-y/-b border-border-strong`) while the **34** card edges stayed on `--border`. **`scripts/qa/static-hex-check.mjs`** is new and wired through `qa:static`, so both seams (the `qa:sweep` prelude and the CI `check` job) pick it up with one edit. Bench re-mirrored; `docs/COLOR_BENCH.md` §6 added; ground truth **v1.5** with D23; `nexo-brand` §3 + §0 synced in both copies.
- **VERIFIED:** `npx tsc --noEmit` **TRUE 0**; `npm run build:check` green, 17 routes static; **FULL CUBE ALL GREEN**, exit 0, **zero** failure artifacts. **THE HARDENING CREATED ONE REAL AA FAILURE AND THE CONTRAST TABLE CAUGHT IT BEFORE IT SHIPPED.** `--ink-hover` is the one token no reading set. Carrying B06's 2.0× onto it gave `#416b58`, dropping `--on-ink-muted` from 5.85:1 to **3.15:1** — because it is a nav-row hover **fill with text on it**, not the raised *card* the B ladder measured. Extrapolating a reading past what it measured is how a hardening task ships an accessibility regression. Re-derived from its own two constraints instead (muted ≥4.5:1; visibly lighter than `--ink-surface`): at 1.30× text is **4.85:1** with margin — not the 4.57:1 edge 1.40× would leave — and separation **RISES** to 1.59:1 from 1.50:1. The hover reads *more* than before while its text got *safer*. **Where a perception floor and AA conflict, AA wins.** Full recomputed table: 30 pairings, every enforced floor met, **zero** AA or 1.4.11 regressions; `--on-ink-border-strong` now clears **3.47:1** on `--ink-surface`, meeting its named floor there for the first time. **THREE FLAGGED FAILURES WERE MY OWN MIS-ASSIGNED TARGETS, NOT DEFECTS:** I initially scored `--border`, `--border-strong` and `--surface-tint-border` against 3:1. WCAG 1.4.11 governs boundary-dependent **controls** — that is `--border-control` (3.48:1, untouched) — not decorative card and section edges. Corrected the targets, not the values. **EYE PASS** (1440 + 390, home + `/platform`, page-top / scrolled / section boundary): cards lift off the page, hairlines exist, the tint band reads as an intentional band rather than a rendering artifact, ink console card edges are now genuinely present, and the scrolled-nav boundary reads. **PREMIUM, NOT CHUNKY — with one honest caveat:** `--border-strong` ruling against pure white (the `/platform` sticky subnav) is now the most assertive line on the site. It is correct, it is what the floor demanded, and it is the single place I would want the owner's eye.
- **TESTS ADDED:** **`scripts/qa/static-hex-check.mjs`** (DISC-20 closed — the deferral condition was "after thresholds exist", and D23 set them). Two rules: hex literals (3/4/6/8 digits with a negative lookahead so route anchors like `#dispatch` are not read as colours) and literal `rgb()`/`hsl()` — while `var()`-composed forms, the sanctioned way to add alpha to a token, are correctly **not** flagged. **Negative-tested in all four directions:** planted hex → exit 1 naming file+line; planted literal `rgb()` → exit 1; `var()`-composed `rgb()` → exit 0; clean tree → exit 0. **SWEEP: 57 files, 0 literals needing migration.** All 37 pre-existing literals sit in the two already-sanctioned email files (`src/lib/email/chrome.ts` 23, `src/app/email-preview/preview-client.tsx` 14) for the documented reason — mail clients strip `<style>` and ignore `var()`, so email MUST hardcode. Zero `rgb`/`hsl` literals exist outside `globals.css`. **The Task #15 preflight guard also earned itself on its first live encounter:** a stale server on :3300 was serving an old build and the sweep **refused to run** rather than emitting phantom failures — exactly the Task #14 debt it was built to prevent. Killed, rebuilt, green.
- **SKILLS USED:** `nexo-brand` (§0 the AA and zero-hex non-negotiables, §1 the tonal map — which is *why* F3's band half was not applied as a blanket swap, §3 the token registers, §8 the visual-verification loop). `frontend-a11y` for the composited-background discipline that caught the `--ink-hover` failure and corrected the three mis-assigned 1.4.11 targets. `webapp-testing` for the Playwright eye pass. **Honest note: `emil-design-eng` and `ui-styling` informed the premium-vs-chunky judgment but produced no change I can attribute to them** — the craft call here was made against the screenshots and the tonal map. Recording that rather than padding the list. No subagents.
- **AUDIT GUIDANCE:** **FLOORS-ONLY-RISE IS LAW (D23).** These deltas were measured on the operative device, not chosen. A future task may RAISE a floor on new device evidence; **softening one because it looks fine on a good screen is a law violation, not a taste call** — flag it as such. **The glass alphas are deliberately unchanged and that is not an oversight:** the read floors (0.75 nav / 0.60 ink card) sit BELOW the shipped values (0.90 / 0.70), so they already clear, and floors-only-rise gives no mandate to move them. Do not "finish the job" by raising them. **F1 was adopted in half, deliberately** — the edge moved, the alpha did not, for the reason above; the bench's 0.96 pane is kept as an unused proposal. **F3's band half was deliberately NOT a blanket swap** — the site already alternates `--bg` against `--surface-tint`, so tinting every `--bg` section would leave every section tinted and destroy the white register §1 is built on; do not "complete" it. **Card edges staying on `--border` while section rules use `--border-strong` is the design, not an inconsistency.** **`--accent` on `--surface-tint` is 4.54:1 — the tightest margin in the palette.** Darkening `--surface-tint` fails accent text on it; recompute before touching. **Bench CURRENT markers now mirror the HARDENED palette with PREVIOUS rows as history** — a reading cited as "A06" predates this commit and refers to the PREVIOUS column. **COLOR_BENCH §4 finding 8 remains open by choice** (section C prints alpha but no composite ratio) — glass was held, so it was not needed to decide anything. **THE CLOSING PROOF IS OWED AND IT IS THE OWNER'S:** every value was derived from readings and verified by arithmetic, a green cube and my eye on *this* machine — but the laptop that set the floor has not yet seen the result. Until the owner scrolls it there, the hardening is verified-by-derivation, not verified-in-the-field.

### YYYY-MM-DD — task name (short commit)
- **WRONG / BEFORE:** what was wrong or absent, or the state we moved from.
- **CHANGED:** what this task did.
- **VERIFIED:** how it was verified (tsc, qa:sweep scope, manual check).
- **TESTS ADDED:** the challenge-to-test law (D12) — tests created for any hard problem fixed, or "none — no challenge class in this task."
- **SKILLS USED:** which installed skills were used, or "none."
- **AUDIT GUIDANCE:** what a future recon should NOT re-flag because of this entry.

Rule for every future recon/audit prompt: read this file first; do not re-flag anything recorded here as fixed unless there is evidence of regression.

---

### 2026-08-18 — Task #17: W8 part 1 — D15 metadata + JSON-LD, hero lede, www→apex 301, I19
- **WRONG / BEFORE:** **All 13 routes served one byte-identical JSON-LD block asserting the pre-D1 identity** — `"Nexo Access is a technology-first NEMT company built for the DMV"` — as the machine-readable description of the entity, on every page, including `/solutions/providers` whose own human-readable copy already said "Join the Nexo Access network". The type was `MedicalBusiness`, a `LocalBusiness` subtype expecting a postal address we deliberately omit. **There was no host 301**: `www` and apex both served byte-identical 200s, so Google's host choice was left to its discretion on every URL forever. Metadata carried carrier language (`The NEMT platform behind Nexo Access`, an `og:image:alt` naming the company **as** the transportation). And the two pinned W8 debts from Task #9 were still owed: D18's payer half had **no** SEO plan behind it, and the internal-linking spec stopped after five bullets.
- **CHANGED:** **PART A:** `docs/SEO_PLAN.md` §6 completes **W8-a** (the payer lane is CATEGORICAL where the provider lane is GEOGRAPHIC; category vocabulary mapped to home / `/platform` / `/solutions/mcos` with title and description implications) and §7 completes **W8-b** (the ordered spine, per-page in-body link counts, the anchor-text table with a never-column, the in-body placement rule, the sibling-plus-hub rule for every state page, and `BreadcrumbList` placement gated on a visible breadcrumb). Both pinned items marked resolved. **PART B:** `next.config.mjs` gains the host-conditional **301**; `src/lib/schema.ts` becomes **`Organization`** with the reasoning in a code comment; `src/lib/seo.ts` carries the D15 identity in the header comment, `HOME_TITLE`, `HOME_DESCRIPTION`, the `/platform`, `/solutions/mcos` and `/about` descriptions, and `OG_IMAGE.alt`; **`HERO_LEDE`** becomes the owner-approved sentence; `src/app/about/page.tsx`'s "What we are" fact card follows; `scripts/qa/i19-host-redirect.mjs` is new and wired into `run.mjs` beside the stale-build preflight, **folded into the exit code**.
- **VERIFIED:** **Old identity phrases in `src/`: 6 → 0.** (5 in `seo.ts` including the `about` description I initially missed, 1 in `about/page.tsx`.) `npx tsc --noEmit` **TRUE 0**; `npm run build:check` green, 17 routes static; **FULL CUBE ALL GREEN** with the preflight and I19 printed in-run: `Build identity OK — :3300 is serving .next-check`, `I19 host redirect: ✓ 4/4: 301 to apex with path+query preserved, apex host unaffected`. **THE INTERPOLATION WAS PRESERVED, NOT IMPROVISED:** the approved sentence contains `built for the DMV` verbatim, which is exactly `SERVICE_AREA_PROSE`'s pre-launch value, so the approved wording and the `LIVE_OPERATIONS` flag mechanism coexist with neither bent — flipping the flag still swaps the verb in the hero and the structured data together. **I14 AND I16 NEEDED NO EXPECTATION CHANGES, and that is a finding rather than a shortcut:** I14 asserts title-non-empty plus apex canonical plus `og:image` presence, and I16 asserts email addresses. Neither encodes an identity string, so there was nothing to update. **THE HUMAN-EYE PASS CAUGHT A REAL DEFECT THE CUBE COULD NOT:** `Hero.tsx` rendered `{HERO_LEDE}` followed by a hardcoded tail, so the new lede's own second sentence ("every claim is checked before it is billed") was immediately repeated by that tail ("every claim is checked automatically before it's billed") — the same claim twice in one paragraph, **7 lines at 1440 and 9 at 390**. Removed; the lede now renders exactly the approved sentence at **5 lines / 6 lines**. Before deleting the tail I checked that its unique clause survives elsewhere on the page: **`CredentialStrip` ("Credential-gated dispatch") and `ProductDemo` twice.** No claim was lost.
- **TESTS ADDED:** **I19 and its negative test.** The redirect ships with its test (D12). **Building it surfaced a trap worth more than the test itself: `fetch()` CANNOT verify a host-conditional redirect.** `Host` is a forbidden header in the fetch spec, so undici silently drops it, the request arrives as `Host: localhost:3300`, the rule does not match, and the server returns 200 — **indistinguishable from a broken redirect.** My first I19 used `fetch` and **reported a false failure against a redirect that was working correctly**; raw `curl -H "Host: www.nexoaccess.com"` returned `301 → https://nexoaccess.com/platform` and settled it. Rewritten on `node:http`, which can set `Host`. That is exactly why the Task #8 spec called this "the raw-request lane", and the reason is now recorded in the file. I19 also carries a **no-loop control** (apex Host must return 200, not redirect) — without it a rule matching everything would pass the positive cases and ship an infinite loop. **Negative-tested twice:** expecting 308 fails naming actual-versus-wanted; using the apex as the trigger host fails on the control. One expectation of mine was also wrong and the test caught it: root redirects to `https://nexoaccess.com` slashless, which is *this repo's own canonical form* (sitemap and `rel=canonical` both omit it), so the expectation was corrected, not the code.
- **SKILLS USED:** `nexo-brand` §7 (the copy honesty gate — every changed string audited: the identity claim is D15-permitted, no operational claim was added that the launch flags do not back, no "serving" verb appears pre-`LIVE_OPERATIONS`, and the §6.8/§7.3 query-lexicon law is why the *category* may still be named in a title while the *operator* self-description may not). `coding-standards` for the comment discipline on the three non-obvious decisions (why 301 and not `permanent: true`, why `Organization` and not `MedicalBusiness`, why the interpolation is load-bearing). **Honest note: `react-patterns` contributed nothing** — the only component change was deleting a duplicated sentence from JSX; no structure, state or boundary changed. Recording that rather than padding the list. No subagents.
- **AUDIT GUIDANCE:** **Identity strings in metadata and schema are D15-canonical as of this commit. Any reappearance of "technology-first" or "NEMT company" as a Nexo self-description in `src/` is a REGRESSION**, not a rediscovery — the grep is 6 → 0 and `_seed/` plus `docs/`/`FIXLOG` occurrences are deliberate historical record. **`Organization` is a ruling, not a guess** — do not "correct" it back to `MedicalBusiness`; the reasoning is in a code comment at `schema.ts`. **The 301 must stay `statusCode: 301`** — `permanent: true` emits 308, and every DNS and deploy note in this repo says 301. **The host literals in `next.config.mjs` are the first outside `site.ts:15` and are deliberate**: `.mjs` cannot import the TS constant. **THE OG IMAGE ASSET STILL CARRIES THE OLD POSITIONING — by scope, not by oversight.** Only its `alt` text changed here; new artwork is **W6** design work. **Body copy, section headings and solutions-page prose were deliberately untouched: that is P6 COPY_DECK, not missing work** — the hero eyebrow chip still reads "Non-emergency medical transportation", which is correct for now. **PRODUCTION VERIFICATION IS OWED AND I19 CANNOT SUBSTITUTE FOR IT.** I19 proves the compiled rule matches the www Host, answers 301 not 308, preserves path and query, and leaves the apex alone. **It cannot prove that DNS points `www` at this app, or that the Coolify/Traefik proxy forwards `Host` unmodified.** Those are deployment facts. At the v2 deploy the owner must check `https://www.nexoaccess.com/platform` really 301s to the apex; until then that leg is unverified and is recorded as such.

### 2026-08-18 — Task #16: C4 observed — field evidence FO-1, named desync mechanism, W5 fix + I20 spec
- **WRONG / BEFORE:** **C4 was a hypothesis.** It had sat on the consistency-defects register since the recon as "unbudgeted animation cost" — a suspicion nobody had seen fail, awaiting P5 measurement. The route line and the livery van desynchronising on real hardware was **not a known defect at all**, its mechanism was unknown, and **there was no place in the repo to record a defect seen in the wild**. The cube runs on a dev-class machine and had never caught it, which is exactly why a field-observation record was missing rather than merely empty.
- **CHANGED:** Created **`docs/FIELD_OBSERVATIONS.md`** with **FO-1** — date, machine class, the exact build observed (**live production, `main` @ `f0de3c9`**), the symptoms, and the named mechanism with its verification. Promoted the **C4 line in `SITE_GROUND_TRUTH.md` §9** from hypothesis to **OBSERVED (FO-1)**, leaving C1–C3 untouched. Appended a **W5 addendum** to `docs/SITE_BUILD_PLAN.md`: a two-option fix spec and the **I20 "the van rides the line"** invariant spec. Logged FO-1 as a scheduled discovery and pointed `currentTask` at the same Yoga-class machine for the owed C3 colour readings.
- **VERIFIED:** **The mechanism was re-verified against source, not transcribed on trust — and the source corrected the chat-side diagnosis on the point that matters.** First, `git diff f0de3c9..HEAD -- src/components/home/RouteOverlay.tsx` is **empty**, so the live V1 code and current v2 code are identical for this component and the diagnosis needs no translation between branches. Confirmed in source: **(a)** one `geo.d` string serves two renderers — the line as `<path d={geo.d} pathLength={1}>` inside `viewBox="0 0 geo.w geo.h"`, the van via `van.style.offsetPath = path("${geo.d}")` at `RouteOverlay.tsx:232`, which CSS consumes as **raw pixels with no viewBox to scale it**; **(b)** `measure()` runs on **mount and `window.resize` only** — the component's own header comment says so, and **grep confirms there is no `ResizeObserver` and no `MutationObserver` in the file**; **(c)** `docStart`/`docSpan` are captured inside `measure()` (lines 138–139) and consumed from the **cached** geometry by the scroll handler, so they go stale with the page. **(d) "fast machines mask it" is labelled INFERENCE, not observation** — it follows from the mechanism and is consistent with the cube never catching it, but nobody measured it. **THE CORRECTION:** the chat read said the SVG "scales with its box" while the van "never rescales" — true but incomplete. The SVG carries **`preserveAspectRatio="none"`**, and the host's axes are governed **differently**: **height is FROZEN** (inline `height: geo.h`) while **width is LIVE** (`w-full`). That splits FO-1 into **two separable failures**: the **desync itself** is an **arc-length divergence** — a live-width change (cheaply, a scrollbar appearing as document height changes) stretches the path **non-uniformly**, and non-uniform stretching changes arc-length parameterisation, so the line's head (`stroke-dashoffset` on the stretched `pathLength=1` path) and the van (`offset-distance` on the raw path) are **different points**, expressing **vertically** on a long mostly-vertical route; while the **frozen height** separately leaves the line in a **stale band** with a stale progress mapping, which is what produces the frame with **a van and no line nearby**. Board validator green (7 phases, 42 items, 66.5 KB); `npx tsc --noEmit` **TRUE 0**.
- **TESTS ADDED:** none in this task, deliberately. **I20 is SPECIFIED and BOUND to the W5 fix task per D12** — an invariant written before the fix exists would assert against the broken behaviour. It is recorded in the W5 addendum with its skip conditions (`@supports (offset-path)`, reduced-motion, sub-`lg`) so those are **skips, not failures**.
- **SKILLS USED:** none, honestly. **This was source reading and evidence discipline, not design or research work** — the whole value was refusing to transcribe a diagnosis I had not checked, and the correction to it came from reading `preserveAspectRatio` and the host's inline style rather than from any skill. Recording that rather than listing skills I did not use. No subagents.
- **AUDIT GUIDANCE:** **C4 is OBSERVED, not open for re-hypothesising.** A future recon must not list it among the unmeasured C-record suspicions, and must not "discover" it again from a field report matching FO-1 — that is the same defect, already named and scheduled. **The live V1 site currently carries this defect and that is known**, not an oversight: the owner ruling on a `main` hotfix is **recorded as pending in FO-1 with the default being the v2/W5 path**, and the entry must be updated when the ruling is relayed rather than silently resolved. **I20 MUST SHIP IN THE SAME TASK AS THE W5 FIX — without it the fix is unverifiable on the machines that exhibit the bug, and per D12 an unverified fix is incomplete.** **The fix must address BOTH failures**, not just the one that is easier to reach: a change that re-measures geometry but leaves `docStart`/`docSpan` stale will still produce the van-with-no-line frame. **Which of the two dominates on that machine is NOT established** — it needs measurement on the machine, and reasoning from source cannot settle it. **The bluish panel cast reported alongside is C3-class and panel-level: a screenshot cannot capture it**, so it is not evidence and must not be treated as such; the colour-bench readings on that machine remain the instrument and are still pending.

### 2026-08-18 — Task #15: stale-build preflight guard + W4 colour hardening bench
- **WRONG / BEFORE:** The stale-server gotcha was written down in `nexo-brand` §8 and **enforced by nothing** — it cost Task #14 a full cube run and 185 phantom "failures" on a site that was fine. Separately, **C3 ("subtle palette on cheap monitors") had no measuring instrument**: it had sat on the defect register since the recon as an untested suspicion, and the cheap-panel leg of the C3 protocol had never been exercised — every screen used across both type-bench rounds was a good one.
- **CHANGED:** **PART A:** `scripts/qa/preflight-build-identity.mjs`, wired into `scripts/qa/run.mjs` **at the server-reuse seam — the exact line Task #14 broke through.** **PART B:** `docs/color-bench/index.html` — six generated ladders (68 specimens) plus text ramps with computed ratios and three danger-pair reproductions, each with **one** hardened proposal — and `docs/COLOR_BENCH.md` recording the step-delta design, the danger inventory with current values, and the reading protocol.
- **VERIFIED:** **The guard uses TWO signals because each catches what the other misses:** a **BUILD_ID probe** (`/_next/static/<diskBuildId>/_buildManifest.js` — asks the server directly whether it is running *this* build; one request, no HTML parsing, nothing to rot when markup changes) and a **CSS-asset probe** (parse the served HTML's stylesheet href and confirm it resolves — the exact Task #14 signature, which catches a torn build where the BUILD_ID directory survived but the assets did not). Either disagreeing is fatal. **NEGATIVE-TESTED WITH A REAL REPRODUCTION, not a synthetic one:** built, left the server running, rebuilt underneath it so the identities genuinely diverged (`08rKY2wIdtQDHn4zHuk4A` → `nEfCzgjuuXnuFOdF42ezN`), ran the preflight → **exit 1 naming both identities and the kill-and-rerun fix**; then killed the server and ran the wired path clean → **`Build identity OK — :3300 is serving .next-check (BUILD_ID nEfCzgjuuXnuFOdF42ezN)` → GREEN, exit 0.** **BENCH:** 3/3 fonts loaded under the Task #12 amended priming check; 68 specimens + 8 ramp rows + 3 pairs; 6 CURRENT markers all matching the real token values; zero overflow at 390/768/1440/1920; zero console errors. **Arithmetic re-verified after the blocker fix: 48 rungs, ZERO mismatches, max printed ratio 8.690:1** (inside the 21:1 physical ceiling). Screenshots viewed at 1440 and 390 — and viewing them is how I caught that the first section-a crop was mid-paint, which the programmatic check then disproved (12 rungs, 3 rows, all with real height).
- **TESTS ADDED:** **the preflight guard and its negative test.** This is the second law this session to stop being prose and become a gate (after Task #14's static type guard). **The reproduction is recorded in full in `SITE_PROGRESS.json` so it can be re-run**, because a guard whose negative test cannot be reproduced is a guard nobody will trust enough to keep.
- **CUBE EXEMPTION, STATED RATHER THAN ASSUMED:** **no cube run this task, and that is deliberate.** The regression law binds changes to shipped surfaces; **Part A touches only `scripts/qa/` (harness) and Part B only `docs/` (tooling) — no file under `src/`, `tailwind.config.ts` or `next.config.mjs` was modified**, so no rendered output can have changed. The harness change is covered by its own negative test, which is a stronger check than a cube pass would be (a cube pass would only show the guard did not fire; the negative test proves it fires **and** that it clears). `npx tsc --noEmit` **TRUE 0** and the board validator is green. **If a future task touches `src/` it owes a full cube regardless of how small the change looks.**
- **SKILLS USED:** `nexo-brand` **§1 read first, as instructed** — the tonal map is the thing being hardened, and reading it is why the bench tests *registers* (light staircase, ink staircase, both text ramps, both border tiers) rather than a flat swatch grid; §0's AA floor and 1.4.11 3:1 boundary rule set what the ramps print. `frontend-a11y` for the contrast maths and for the ruling that `--text-disabled` is **WCAG 1.4.3 EXEMPT** rather than a failure. `frontend-design` + `nexo-site:emil-design-eng` + `ui-styling` for the instrument's craft. **`impeccable` RAN FOR REAL** (two independent assessments, per the owner-facing-deliverable rule) and **materially changed the artefact** — see below. **Honest note: no subagents beyond the two impeccable assessments.**
- **AUDIT GUIDANCE:** **The bench is `docs/` tooling and is NEVER shipped.** **Its CURRENT markers mirror `globals.css` as of this commit — if a token changes, RE-MIRROR the bench or its markers become lies**; the file says so in its own header comment. **The instrument's output is the OWNER'S READINGS, which do not exist yet** — the bench is a question, not an answer, and no screenshot substitutes for it. **The DISC-20 hardcoded-hex guard lands with the IMPLEMENTATION task after readings, not here** — building it before the thresholds exist would encode the wrong thresholds. **Three impeccable findings are recorded NOT DONE with reasons and must not be re-flagged as oversights:** composite worst-case ratios for the glass ladders (correct, needs lightest/darkest-pixel compositing, deferred to the implementation task rather than half-built), a `prefers-reduced-motion` block (colour-only transitions, non-vestibular, internal tooling), and the sub-13px bench chrome (10.5–11px — **the most ironic finding in the set, on the page built for cheap screens**; worth fixing before the cheap-panel session). **Two findings are worth remembering as failure classes, not just as fixed bugs:** the bench was **mislabelling its own steps** (unclamped mix fed to the ratio function while the hex was clamped, printing ratios up to 71.39:1 against a 21:1 physical ceiling) and its **canvas was the exact value of the flagship CURRENT specimen**, so the one thing it most needed to measure had no ground to separate from. Both were found by the critique, not by me. **A measuring instrument is only as good as its own arithmetic, and it must be checked like any other claim.**

### 2026-08-18 — Task #14: D21 modular type implementation (first code on v2)
- **WRONG / BEFORE:** Type was scattered and unenforced. Families lived in `layout.tsx` and were re-declared with duplicate fallback chains in `tailwind.config.ts` and again in the `globals.css` body rule; weights were inline Tailwind (`font-bold` on every heading, so the hero and an h3 were the same weight); there was **no tracking system at all** — every display heading used the same blanket `tracking-tight` regardless of scale; and **nothing enforced any of it**, so a component could name a font family and no gate would notice. `nexo-brand` §2 still described the pre-D21 system and its blanket "display tracking ≥ -0.04em" contradicted the bench-measured values D21 had just locked.
- **CHANGED:** **TOKEN BLOCK** in `globals.css` is now the single type source — families (wired to the next/font `*-src` variables), `--weight-hero: 800` / `--weight-heading: 700`, and the four bench-measured tracking tokens. **LOADING**: IBM Plex Mono added via `next/font` at **400/500 only**, both genuinely used (eyebrow pills are `font-medium` 500; ProductDemo identifiers inherit 400); `display:"optional"` kept consistent with the pair with a comment recording that **C1 is deliberately untouched** as P5's measurement question. **Bricolage already loaded 500/600/700/800, so no widening was needed and it contributed zero payload delta** — the task anticipated a possible widening; there was none to do. **TAILWIND** maps family/weight/tracking **once**. **APPLICATION SWEEP: 53 edits across 19 files.** **STATIC GUARD** `scripts/qa/static-type-check.mjs` added and wired. **`nexo-brand` §2** rewritten to the D21 system in **both copies**.
- **VERIFIED:** `npx tsc --noEmit` **TRUE 0**. `npm run build:check` green, 17 routes static. **FULL CUBE ALL GREEN — 234 cells (13 routes × 6 profiles × 3 engines), zero failures, zero FAIL artifacts, all three engines reported GREEN.** **THE FIRST CUBE RUN WAS INVALID AND WAS DISCARDED, NOT EXPLAINED AWAY.** It produced **185 failing cells** and every page rendered **completely unstyled**. I did not treat that as a type regression: I opened a FAIL screenshot, saw raw unstyled HTML, then fetched the served CSS link and got **HTTP 400 for hash `667effaa182fb03f` while `.next-check` on disk held `709e3d23b1dd55e6`** — a hash mismatch proving a **stale `next start` from an earlier session was still holding :3300** and `run.mjs` had reused it (it logs "Reusing server already answering on :3300"). That is the **recorded never-rebuild-under-a-running-server gotcha — an ENVIRONMENT failure, not a site regression.** Killed the server, cleared 212 bogus artifacts, rebuilt into a fresh `.next-check` with nothing running, re-ran: **1 flaky cell** (chromium/w1920/home, passed in isolation on retry), then a **third full run came back CUBE ALL GREEN with zero failures**. I ran it twice rather than accept the single amber cell. **GUARD NEGATIVE-TEST**: a planted `fontFamily` violation produced **exit 1 naming `src/components/ui/Button.tsx:4`** with the correct rule and offending text; plant removed, **exit 0** restored, `git diff` on that file clean. **HUMAN-EYE PASS** (prod-local, reduced-motion, viewed): computed values confirm the system — **h1 `font-weight: 800`, `letter-spacing: -1.8px` at 60px = exactly `-0.030em`; h2 700 at `-0.022em`; 9 mono elements on home, 7 on `/solutions/providers`, 5 on `/platform`.** `nexo-brand` copies **byte-identical, md5 `b7974062637979b7a5f60c1259f6588d`**, 75,089 bytes.
- **TESTS ADDED:** **`scripts/qa/static-type-check.mjs` + its negative test — the D12 receipt.** D21's Modular Type Law was a sentence in a document; it is now a gate that fails the build. Zero dependencies, names file + line + rule. **Wired in TWO places, deliberately:** as a **fail-fast prelude to `qa:sweep`** (so a violation costs seconds, not the 30+ minutes the cube takes) **and into the CI `check` job before typecheck** (so it catches on every push without needing the cube). **Sanctioned exemptions are principled, not convenience:** `globals.css` (the token block itself), `layout.tsx` (next/font loading), and **`src/lib/email/**` + `src/app/email-preview/**` — because mail clients strip `<style>` and ignore `var()`, so an inline literal font stack there is mandatory craft; without that exemption the guard would fail correct code.**
- **SKILLS USED:** **`nexo-brand` FIRST**, as instructed — I read §2's existing text before amending it, which is why the amendment *neutralises* the superseded blanket `-0.04em` clause instead of leaving two contradictory tracking rules in one section. §0 (WCAG AA and zero-CLS as the gates the cube enforces) and §8 (the visual-verification loop) governed the verification ladder. `react-performance` informed the font-loading decisions: loading only the two mono cuts actually used, and **measuring the payload delta rather than assuming it** — which is how I found that IBM Plex Mono costs 64 KB across 10 files precisely because it is *not* a variable font, unlike Bricolage (3 files) and Hanken (4). `ui-styling` for the weight/tracking application. `frontend-design` for the eye pass. **Honest note: `react-patterns` contributed nothing** — no component structure changed, only class strings and one font import; recording that rather than padding the list. No subagents.
- **AUDIT GUIDANCE:** **Font families outside `src/app/globals.css` and `src/app/layout.tsx` are violations that the guard now catches — auditors run `npm run qa:static` (or `qa:sweep`, which runs it first), they do not grep.** A grep will show ~16 hits in `src/lib/email/` and `src/app/email-preview/`; **those are sanctioned and correct**, because HTML email cannot use CSS custom properties. Do not "fix" them and do not re-flag them. **`display:"optional"` is deliberately unchanged** — whether slow machines should keep the metric-matched fallback is **C1, a P5 measurement question**, not a defect. **Mono breadth beyond eyebrow/kicker labels and ProductDemo data labels is deliberate P4 page scope, not an omission** — the accent layer is a *label* voice and must not be applied by reflex to anything that looks technical. **The hero lede still carries the pre-D15 "technology-first NEMT company" copy on purpose: that rewrite is W8's (`SEO_PLAN` §3.2), and this task's scope law forbade copy changes.** **One observed consequence worth knowing before W4:** the mono eyebrow pill is wider than the Hanken original and wraps to two lines at 390 — contained, readable, and the overflow invariant passes, but it is a real visual change, not an accident. **NO hardcoded-HEX guard exists** (DISC-20) — the type guard proves the pattern; W4 is its natural home since W4 owns colour. **`nexo-brand` §2 now describes and the token block defines**; if they ever disagree, the token block is what ships and §2 is the bug.

### 2026-08-18 — Task #13: D21/D22 ledger lock + owner-doc tier annotations
- **WRONG / BEFORE:** The type ruling and the private-evidence tier existed **only in chat**. `W3` sat open in the board with the finalists chosen but no decision recorded, `nexo-brand` §2 was still the operative type law, and nothing in the repo stated that the owner's modularity requirement was binding. Separately, `docs/PROVIDER_REQUIREMENTS_RESEARCH.md` carried entries marked `UNRESOLVED` and `OWNER-REPORTED` that **the owner's own historical documents had already settled internally** — so the research doc was understating what we actually knew, while the evidence itself could not be brought into a public repo.
- **CHANGED:** `SITE_GROUND_TRUTH.md` → **v1.4**, with **D21** (type ruling: System B, bench-measured tracking as law, **MODULAR TYPE LAW**, commit-and-close) and **D22** (**OWNER-DOC-VERIFIED** private evidence tier and its handling law) appended to the §4 ledger, a matching §8 standing law (*"no component may declare a font family; type flows from the token block only"*), and §9 rewritten. `docs/PROVIDER_REQUIREMENTS_RESEARCH.md` gained the **OWNER-DOC-VERIFIED tier definition** in the SOURCE LAW legend, annotations on the **OR-3 row** and on **both Sexual-Abuse-and-Molestation entries**, and a new **LAYER-MODEL CONFIRMATION** block at the head of the publish map. `docs/DESIGN_RESEARCH.md` gained **§13e D21 DECISION**. `.gitignore` now excludes `.impeccable/critique/` — **it is tool working state, not a receipt; FIXLOG is the record of what was found and what changed**, and Task #12's findings are already written into `TYPE_BENCH.md` and this log. `docs/SITE_PROGRESS.json` mirrors D21/D22, closes `W3`, resolves the owner-packet discovery, and annotates the `P1-B1` gate.
- **VERIFIED:** **PRIVACY LAW AUDIT — the central check of this task, and it was run two ways.** A line-level grep of the staged diff **for the owner-supplied watchlist of insurance-figure and broker-name patterns** (the watchlist is deliberately not reproduced here — enumerating it would itself put broker names into the repo, which is the exact harm D22 guards against) returned **three matches on added lines**. I did **not** stop there, because a line-level grep cannot distinguish new text from pre-existing text carried along when a whole table row is rewritten. I re-ran the check at **word level** (`git diff --cached --word-diff=porcelain`, extracting only genuinely-added word-runs): **9,804 characters of newly-added text, ZERO forbidden tokens.** All three line-level matches are **pre-existing Task #6 public-research content** — the Modivcare/SAM row, the OR-3 row, and the `P1-B1` note — each of which has a matching `-` line with identical figures, and each of which was rewritten only to append a tier annotation. **The annotations themselves contain no figure, no broker name, and no contract term**; they name only a tier, a date, and a memo reference (`E2/E3`, `E4/E5`). **No insurance figure, broker name, or contract term entered any repo file in this task.** Ledger append-only order verified **D20 → D-pre21 → D21 → D22**. `.gitignore` confirmed effective via `git check-ignore` (`.gitignore:49`). Board validator green (7 phases, 40 items, 61.5 KB); `npx tsc --noEmit` **TRUE 0**.
- **TESTS ADDED:** none — documentation-only task, no site code touched, no cube run. **The word-level privacy audit is worth reusing** as the standard check whenever private-tier material is annotated into a public repo: a line-level grep over-reports on any rewritten line and would have produced a false alarm here.
- **SKILLS USED:** `nexo-brand` §7 and its D17 publish boundary — which is the reason the OWNER-DOC-VERIFIED tier is defined as **internal-verification only** and explicitly cannot promote anything to publishable. **Honest note: no design or research skill was used, because nothing visual or investigative happened here** — this task is a ledger lock and a set of annotations, and the work was precision in wording plus the privacy audit. No subagents; the two-pass privacy check was mine.
- **AUDIT GUIDANCE:** **OWNER-DOC-VERIFIED facts are internally SETTLED — do not re-open them as unknowns.** OR-3 and the two SAM-coverage entries have been confirmed against the owner's historical documents; a future recon must not list them among the open internal questions. **But they remain NON-PUBLISHABLE until an independent public primary source exists (D17), and their public-tier labels were deliberately left unchanged** — that is not an oversight, it is the tier working as designed. **The evidence memo and its source documents are intentionally absent from this repository; their absence is NOT a gap and must never be "fixed" by importing them.** They live in Claude project knowledge only, per D22's handling law, and chat-side memo creation was the deliberate exception to the Claude-Code-creates-docs rule because private material cannot pass through a public repo. **The LAYER-MODEL CONFIRMATION in the publish map is publishable-class precisely because it states a structure and no number** — it explains *why* the NEMT-specific provider minimums were never findable in public sources (the operative floor lives in a private broker agreement), which closes that gap as *understood* rather than merely open. **D21 closes the type question: `nexo-brand` §2 is superseded on families/weights/tracking, and commit-and-close (D-pre21b) means type is not re-litigated mid-build.** The **cheap-panel leg of the C3 protocol is still owed and belongs to W4** — every screen used across both bench rounds was a good one — and it must not be treated as a reason to reopen W3. `.impeccable/critique/` being gitignored is a ruling, not neglect.

### 2026-08-17 — Task #12: finalist context bench (System A vs System B) + type process rulings
- **WRONG / BEFORE:** The two finalists had been chosen **on specimens only** — isolated faces at hero/heading/body/label scale. That cannot answer the owner's own insight that *a font alone makes no site premium; the combination does*, because judging a face in isolation is not judging a combination. Three process questions were also unrecorded: whether to buy a premium face, what stops the type search from re-opening forever, and whether the J3 contradiction page may name vendors. And the specimen-bench task had closed with `impeccable` named in its brief but **not actually invoked** — recorded honestly at the time, still outstanding.
- **CHANGED:** **PART A:** appended **§13d TYPE BENCH REACTIONS** to `docs/DESIGN_RESEARCH.md` — finalists (#06 Sora system, #02 Bricolage-Heavy system), the owner's self-discovery that fonts are inherently hard to satisfy him (accepted as a working constraint), the insight that combination not face makes a site premium, and **the testing gap stated rather than hidden: both devices used were good screens, so the cheap-panel leg of the C3 protocol is still owed.** Appended **D-pre21** to the `SITE_GROUND_TRUTH.md` §4 ledger (premium purchase declined with a P6 one-time-licence door; **commit-and-close law**; J3 names no vendors). Added the premium-face door to the `SITE_BUILD_PLAN.md` ROADMAP, mirrored the rulings into `SITE_PROGRESS.json`, and logged the **Healthway credentialing-packets** discovery. **PART B:** built **`docs/type-bench/context.html`** — one set of markup rendered twice through a single toggle across an ink hero, a light content section and an ink terminus, plus **two simultaneous 390px pairs** and a live font-load readout.
- **VERIFIED:** **6/6 families loaded at both 1440 and 390; 14/14 elements swap family across all three fragments with no fragment left un-toggled**; zero horizontal overflow at 390/768/1024/1440/1920; zero console errors and zero failed requests; every contrast pairing clears AA on **real composited** backgrounds (hero h1 16.17:1, lede 9.66:1, chip on composited `--ink-glass` 8.80:1, light body 8.06:1). **THE STANDING FONT CHECK CAUGHT A REAL DEFECT ON ITS FIRST RUN:** Hanken Grotesk reported **FALLBACK (Δ0.0)** — not a CDN failure, because the CSS contained it. **The page opens in System A, so Hanken had zero on-screen usage, and a browser never downloads a webfont no element is using**; Bricolage and IBM Plex escaped only because the 390 strip and the chrome labels used them unconditionally. Fixed by priming with `document.fonts.load()` before measuring. **`$impeccable critique` WAS RUN FOR REAL** — two independent assessments that never saw each other, score **22/40**, deterministic detector **0 findings and verified non-vacuous** (the same detector fired 4 findings on the sibling specimen bench). **12 findings, all addressed**; screenshots re-shot and viewed after the fixes.
- **TESTS ADDED:** none new — the Task #10 standing check was reused. **But the standing check is AMENDED, and the amendment is the durable lesson: prime every family with `document.fonts.load()` before measuring.** A family declared in CSS but not currently rendered reports a **false FALLBACK**, because browsers never fetch an unused webfont. Recorded in `TYPE_BENCH.md` §3 and in the bench's own source comment.
- **SKILLS USED:** **`impeccable` — INVOCATION EXPLICITLY CONFIRMED THIS TASK.** Setup ran (`context.mjs` resolved PRODUCT.md, register **marketing**), the `critique` command reference was loaded and followed, both mandated assessments ran as independent sub-agents, and the snapshot was persisted to `.impeccable/critique/`. This closes the Task #10 gap where it was named but not run. **`nexo-site:emil-design-eng`** (the directory-scoped variant, which governs files under `nexo-site/`) for motion and interaction craft — custom easing, `@media (hover: hover)` guards, `:active` press feedback. `frontend-design` for the fragment architecture and the decision that the hard comparison (body + mono) must come before the easy one (display). `ui-styling` for specimen craft. `nexo-brand` §0 (reduced motion non-negotiable — the bench had **no guard at all**, now fixed), §1 tonal registers, §2 type law, §3 tokens (21/21 colours were verbatim; three drift points found and fixed: the easing curve, a raw hex, and a border tier), §8 the render-and-view loop.
- **AUDIT GUIDANCE:** **The context bench SUPERSEDES the specimen bench for the D21 decision.** `docs/type-bench/index.html` remains as the record of how seven became two and must not be deleted, but it is no longer the instrument — note it carries 4 detector findings that are **not** worth fixing precisely because it is superseded. **Both benches stay `docs/`-only and never enter the app bundle; their CDN font loading is bench-only and must not be copied into the app** — production remains `next/font` self-hosted with `display:"optional"`, decided at implementation under C1. **D21 is PENDING THE OWNER'S A/B PICK; `nexo-brand` §2 remains law until it lands**, and per **D-pre21(b) commit-and-close, once D21 is set the type question is CLOSED until post-launch** — a future audit must not re-open it mid-build. **Three impeccable findings were deliberately NOT adopted and must not be re-flagged as oversights:** pinning the hero to an equal line count (the 3-lines-in-A vs 2-in-B difference is **genuinely caused by the faces** — Sora sets ~12% wider — so it is labelled in the control bar rather than suppressed); defaulting to System B (the incumbent is now disclosed in words, which fixes the substance without introducing an opposite ordering bias); and splitting into three independent role toggles (a good idea that would let the owner reach Bricolage + Instrument Sans + IBM Plex Mono, but **it changes the question he was asked** — recorded as an open question for the D21 conversation). **The single most important fix came from the critique, not from me: the bench never disclosed that System B is what the site already ships**, so it presented a symmetric choice when B costs nothing and A costs 13 pages re-typeset plus re-running the CLS work that `layout.tsx` documents. **The cheap-panel leg of the C3 protocol is still owed and lands in W4** — every device used so far has been a good screen, which is the opposite of the condition C3 exists to catch. `.impeccable/critique/` is left **untracked** (it is the tooling backlog `$impeccable polish` reads); whether to track or ignore it is an owner call, not one I made silently.

### 2026-08-17 — Task #11: J2/J3 resolved from primary sources (MD 16+ tier corrected; contradiction-page jurisdiction attributed)
- **WRONG / BEFORE:** Two internal **VERIFIED-vs-VERIFIED** contradictions sat unresolved in our own research, recorded as judgment calls J2 and J3 at `docs/SITE_BUILD_PLAN.md` §0. **J2:** two cells in `PROVIDER_REQUIREMENTS_RESEARCH.md`, both labelled VERIFIED and sourced differently, disagreed on whether the Maryland PSC "16 or more passengers" insurance tier includes a `$100,000` property-damage component — and the frozen plan had ruled that row **UNPUBLISHABLE** as a result. **J3:** the circulating "$100,000 general liability / 25-50-25 automobile" figure was attributed to **Maryland** by the provider research (with a DO NOT PUBLISH order) and to **Virginia** by the SEO plan, which had built the planned flagship insurance-contradiction trust page on it. A page whose entire premise is correcting a jurisdictional error could not be written while its own jurisdiction was contested.
- **CHANGED:** Appended **"J2 RESOLUTION — 2026-08-17"** and **"J3 RESOLUTION — 2026-08-17"** to `docs/PROVIDER_REQUIREMENTS_RESEARCH.md` (~200 lines), each carrying the ruling, primary-source citations with access dates, verbatim operative fragments, the corrected cell marked VERIFIED with its new pinpoint citation, and a reversal receipt. **The wrong cell was annotated in place — struck through, cause recorded, kept visible — never deleted**, and the Maryland-attribution passage that fed J3 was cross-referenced rather than rewritten, because that observation was accurate and only its inference was wrong. Annotated the **J2 and J3 rows in `docs/SITE_BUILD_PLAN.md` §0** with their resolutions (annotation, not scope growth). Marked **`P4-prep` done** in `SITE_PROGRESS.json`.
- **VERIFIED:** **J2 — settled by text, and I fetched the primary myself rather than relying on agents.** `COMAR 20.95.01.18B(3)`, quoted verbatim from `regs.maryland.gov` (HTTP 200, 2026-08-17): *"(3) The minimum per accident insurance required for each vehicle with a seating capacity of 16 passengers or more is: (a) $75,000 for injury to any one person, $400,000 for injuries to two or more persons, **and $100,000 for property damage**; or (b) $500,000 combined single limit."* Independently confirmed on `law.cornell.edu` (HTTP 200) and restated in the PSC's **own Form-28 Carrier Application Packet**; `regulations.justia.com` returned **HTTP 403** and forms no part of the ruling. **Cell B (§4c, the Cornell mirror) was RIGHT; Cell A (§2e, sourced to `regs.maryland.gov`) was WRONG.** Cause: a **mid-clause elision in our own transcription** — the third conjunct dropped while the `; or` connector that follows it was kept. Proof it was not a source defect: **the host Cell A cites returns the figure today**, and across six sources addressing the tier (both hosts, PSC Form 28 Rev. 9-26-23 and Rev. 7-1-22, the PSC Vehicle Addition/Deletion Packet, the PSC FAQ rev. 9-8-22) **all six carry the $100,000**. The superseded PSC FAQ rev. 2-1-18 is excluded as the culprit because it omits the **entire** tier, not one element. **The citation was independently validated** — Title 20 PUBLIC SERVICE COMMISSION → Subtitle 95 TRANSPORTATION → Chapter 20.95.01 General → Reg .18 "Insurance or Bond for Motor Vehicles Carrying Passengers"; §B is Liability Insurance, §C is the separate bond provision — with two refinements adopted: pinpoint to `B(3)(a)`, and drop the period before the subsection letter per COMAR house style. **The two lower tiers were re-checked against the same text and are correct as written**; only the 16-or-more row was corrupted. **J3 — `BOTH-GENERIC`.** `25/50/25` matches **neither** state: **Maryland is 30/60/15** (MVA, HTTP 200) and **Virginia is 50/100/25** for policies effective on or after Jan 1 2025, `30/60/20` before that (Virginia DMV and Code of Va. § 46.2-472, both HTTP 200) — all fetched by me directly. Virginia's pre-2022 `25/50/20` is the closest historical match and is **still wrong on the third leg**, and dead since January 1, 2022. **No Maryland or Virginia government or broker primary document states `$100,000` for general liability**: the Cardinal Care SFY2026 contract yielded **zero occurrences of "general liability" across 1.83M extracted characters**, and the DMAS FFS NEMT requirement documents contain zero dollar figures. **MediRoutes publishes the identical uncited sentence on its Maryland, Virginia, Texas and California pages (all fetched, HTTP 200)** and on roughly ten states total — four states whose insurance law is not remotely alike cannot share one statutory minimum. Board validator green (7 phases, 40 items, 58.5 KB); `npx tsc --noEmit` **TRUE 0**.
- **TESTS ADDED:** none — research micro-task, no site code touched.
- **SKILLS USED:** `nexo-brand` §7 (the copy honesty gate, which is the whole reason this task exists and why the four publication conditions in the J3 resolution are stricter than the research finding alone would require) and its D17 publish boundary. **Honest note: the substantive value here came from primary-source discipline rather than from any skill** — the decisive J2 quote and both states' personal-auto figures were fetched by me with `curl` and parsed locally, because the Maryland and Virginia hosts had defeated earlier passes and I wanted the raw text under my own control rather than through a summarizer. A 6-agent workflow ran the parallel primary-source hunts (COMAR primary + independent secondary read, personal-auto minimums, general-liability hunt, in-the-wild vendor survey) and an adversarial adjudicator; **the adjudicator materially improved the result twice** — it rejected one input's root-cause theory for J2 as not fitting the artifact, and it caught an overstatement in another input's J3 evidence (a "six vendors publish this for both states" claim that the quoted pages do not support). Both corrections are recorded in the resolutions.
- **AUDIT GUIDANCE:** **The corrected cells SUPERSEDE; the struck cells are historical record and must never be deleted or "cleaned up."** The J2 correction is `COMAR 20.95.01.18B(3)`: `$75,000` per person / `$400,000` two or more persons / **`$100,000` property damage**; or `$500,000` CSL. **TWO TRAPS, both live, both recorded in the resolution:** (1) **the 16-or-more per-person figure is `$75,000` — IDENTICAL to the 8-to-15 tier, not escalated.** The row is genuinely non-monotonic; anyone "repairing" it by symmetry would inflate it to `$100,000` and publish a fabricated regulatory figure. (2) **`20.95.01.18B` governs PSC-permitted for-hire vehicles and the 16+ tier is the LEAST likely to bind our readers** — a typical NEMT van seats under 16, so B(1) or B(2) is operative; TNO vehicles fall under PUA §10-405 at different, lower figures and must not be cross-applied. **Do not re-open J2 or J3 as unknowns.** **The contradiction page is UNBLOCKED ON JURISDICTION ONLY — P1-B1 and D17 still apply independently**, and the page must be **RE-PREMISED**: its original thesis ("Virginia says X but the law says Y") is false in its premise because Virginia never said X. The defensible thesis is that a figure circulating as ten states' minimum is no state's minimum. **The DO NOT PUBLISH order on the figure itself STANDS — `BOTH-GENERIC` does not lift it.** **Do not repeat the six-vendor claim**; the evidence supports MediRoutes across ~10 states plus ZeitRide for Virginia only. **Vendor naming is an unresolved OWNER decision** — §7's institution-name law does not literally cover software vendors, but naming competitors as publishers of dangerous misinformation is trade-libel exposure the owner has not signed off on; the recommended form names no vendor. **THE LARGEST GAP IS STILL OPEN AND MUST NOT BE PAPERED OVER: the NEMT-specific provider insurance minimums for DC, MD and VA have still not been located.** Everything verified here is either a personal financial-responsibility minimum or a for-hire carrier minimum; what actually binds a Medicaid NEMT provider comes from the state Medicaid provider agreement and, in practice, the broker contract. **Until those are retrieved, no page may state a specific NEMT provider insurance minimum for any of the three jurisdictions** — and publishing a personal-auto figure near the phrase "NEMT insurance requirements" would substitute one dangerous misstatement for another. **DC was not covered by this pass at all.**

### 2026-08-17 — Task #10: P3-W3 type-specimen bench + freeze ratification
- **WRONG / BEFORE:** The type direction was **unresolved and unresolvable by argument**. `DESIGN_RESEARCH` §8.5/§11b found light display weight (300) correlating across Stripe and ElevenLabs as "the single strongest premium signal in this sample"; §13b T2 records the owner's own words as **"not bold enough."** The corpus flagged that tension and explicitly declined to settle it, and no document could — it is an eye judgment, and there was nothing for the eye to judge. Separately, the six freeze judgment calls J1–J6 were **my calls, unratified**, and the three not-shippable routes had no unblock conditions. The two W8 obligations surfaced by the freeze (D18's payer-half query classes, the unfinished internal-linking spec) and the J2/J3 research were recorded in prose only, where they could quietly be lost.
- **CHANGED:** **PART A:** appended the **owner ratification line** to `docs/SITE_BUILD_PLAN.md` §0 — J1–J6 stand, with unblock conditions for the three not-shippable routes (`/faq` unblocks only on real questions and **invented FAQs are banned**; `/notices` is laid out by us but authored only from counsel/authority sources; **`/providers/credentialing` merges into `/providers/requirements` at P4** unless the owner pass yields distinct content). Pinned three items into `SITE_PROGRESS.json`: **W8-a** (D18 payer-half SEO query classes), **W8-b** (internal-linking spec beyond the five bullets), and **P4-prep** (resolve J2 against the COMAR 20.95.01.18.B primary text and J3's jurisdiction attribution before the contradiction page is built). **PART B:** built **`docs/type-bench/index.html`** — one self-contained file, zero build step, opens from `file://` — with a sticky numbered legend, **seven display blocks** carrying identical real site copy across five rows each (hero at two weights, section-heading scale, 68ch body, eyebrow + mono accent, and a **dark-on-ink strip**, because our pages end on ink and a face must survive both registers), plus a final **A/B stack** of all seven heavy cuts. Block 01 is the incumbent as shipped; **block 02 is the control that matters — the same Bricolage pushed to 700/800, testing whether *weight* rather than *family* was the wound**; blocks 03–07 are Archivo, Space Grotesk, Satoshi, Sora and Fraunces. Two body candidates (Public Sans, Instrument Sans) are swapped in against the Hanken incumbent, and three mono accents rotate across blocks, each labelled. Wrote **`docs/TYPE_BENCH.md`** with the candidate table (license and variable-status per face, one-line rationale and one-line risk each), the verification method and result, and the decision protocol.
- **VERIFIED:** **The bench renders its own faces — proven, not assumed.** The failure mode guarded against is specific: a silent CDN failure makes every block fall back to one system font, the page still looks beautiful, and **the bench lies**. Three independent checks: (1) **width-delta against each family's OWN fallback chain** — `system-ui` for the sans faces, `Georgia, serif` for Fraunces, `ui-monospace` for the monos — rather than one generic sentinel, because a generic sentinel only proves "not monospace", which a system-font fallback would also pass; (2) `document.fonts.check()`; (3) enumeration of the `document.fonts` registry and its per-face status. **Result: 12/12 families render distinct from their own fallback, and 7/7 blocks produce distinct display-face widths for the same string** — no two blocks collided, which is the direct proof none silently shared a fallback. **The two narrowest margins are stated rather than buried: Space Grotesk (4.9px) and Satoshi (13.8px)**, each confirmed by all three checks and visibly distinct in the screenshot. Zero failed requests and zero console errors at 1440 and 390. Screenshots were **viewed, not merely captured** (A/B stack and representative blocks, both viewports); every block renders all five rows and the 390 layout wraps cleanly. Artifacts confirmed gitignored via `git check-ignore` (`.gitignore:45`) with **zero PNGs stageable**. Board validator green (7 phases, 40 items, 58.1 KB). `npx tsc --noEmit` **TRUE 0** — the bench is plain HTML outside the TS project, so nothing should change, and nothing did.
- **TESTS ADDED:** **The font-loading verification method is recorded as the standing check for any future specimen bench** (D12 spirit — a hard problem solved once becomes a permanent check), written into `TYPE_BENCH.md` §3: verify every family against **its own fallback chain**, never a generic sentinel; confirm **no two blocks share a rendered width**; and **never cite `getComputedStyle().fontFamily` as evidence a font loaded** — it returns the *declared* stack, not the *used* font. That last item is recorded because it caught me: an earlier pass reported `resolved=Archivo` from computed style, which proved nothing at all, and the check had to be rebuilt against real fallbacks before its result meant anything.
- **SKILLS USED:** `frontend-design` and **`nexo-site:emil-design-eng`** (the directory-scoped variant, which is the one that governs files under `nexo-site/` — the bare skill emitted a system-reminder pointing at it) led the craft: generous whitespace, restraint so nothing competes with the type itself, custom `cubic-bezier(0.23, 1, 0.32, 1)` on the only interactive element, and hover gated behind `@media (hover: hover) and (pointer: fine)`. `nexo-brand` §2 supplied the incumbent type law the bench is judged against and the ban on Inter/Roboto/Arial/system stacks that removed the obvious challengers; §3 supplied the mirrored color tokens; §8 supplied the mandatory render-and-view loop. `ui-styling` for specimen craft (true hero scale via `clamp()`, real 68ch measure, per-row labelling). `webapp-testing` for the Playwright verification lane. **Honest note: `impeccable` was named in the task to critique the bench and I did not invoke it** — I ran the critique myself against the emil/nexo-brand criteria and the rendered screenshots. That is a real deviation from the task's skill list and is recorded rather than glossed; a future pass may still run it. No subagents this task.
- **AUDIT GUIDANCE:** **The bench is `docs/` tooling and is NEVER shipped site code** — it lives outside the app, is not imported by anything, and must never enter the bundle. **Its CDN font loading is bench-only and must not be copied into the app**; this is stated in a comment at the top of the file and in `TYPE_BENCH.md`, because it is exactly the kind of thing a future reader lifts by accident. Production loading remains `next/font` self-hosted with `display:"optional"` and is a **separate decision tied to C1, made at implementation**. **The type ruling is PENDING THE OWNER and becomes D21 in a follow-up task — `nexo-brand` §2 remains law until D21 supersedes it**, so do not treat any candidate in this bench as chosen, and do not re-flag the incumbent pair as "unresolved" while D21 is outstanding. **The owner must react on at least two different machines/monitors (C3 protocol)**: a face that reads confident on a calibrated display can read thin and grey on a cheap panel, which is the same hazard W4 exists to measure, so a single-screen verdict is not a verdict. **Screenshots are deliberately not committed** (`scripts/qa/artifacts/research/` is gitignored, established Task #5); their absence is not a defect. **Space Grotesk caps at 700** — if the owner's answer to "not bold enough" is 800+, that face structurally cannot supply it, which is a selection constraint and not a bench bug. Satoshi is the one candidate **not on Google Fonts** (Fontshare), so adopting it means a separate self-hosted download and a different license file kept on record.

### 2026-08-17 — Task #9: P2 freeze (SITE_BUILD_PLAN.md) + ground truth v1.3 (D18–D20) + P1-C1 gate closed
- **WRONG / BEFORE:** Scope existed only as a **draft** buried inside a research document — `DESIGN_RESEARCH.md` §6e carried a 24-page inventory explicitly marked "DRAFT FOR P2 FREEZE, not frozen", and nothing bound it. There was no per-page content specification, no workstream list, no roadmap, and no mechanism preventing scope growth. Three owner rulings were undocumented: **national legibility** (payer evaluators contract nationally, and the site read as a local-only shop), **imagery strategy** (T3 was an open P2 decision, and the hero was on record as hard to grasp), and the **P2 technical ratifications** (the performance bar, the decline-don't-degrade rule, the motion-safe architecture, and the four `nexo-motion` candidates were all still proposals). The homepage indexing question was open pending an owner reading.
- **CHANGED:** **PART A:** `SITE_GROUND_TRUTH.md` → **v1.3**. Appended **D18** (broad legibility, local operations — refines D2, does not reverse it), **D19** (imagery strategy, closing T3 — animation and custom illustration primary, **stock people-photography permanently banned**, real photography deferred until real operations exist, hero gets a new signature animated centerpiece), and **D20** (technical ratification: perf bar as ceiling, decline-don't-degrade as standing law, motion-safe architecture, four `nexo-motion` candidates approved for P3). §9 CURRENT STATE rewritten. **PART B:** created **`docs/SITE_BUILD_PLAN.md` (FROZEN)** — the FREEZE LAW verbatim, the SafeRide information-model ruling, the gates table, **24 pages B1–B24** each with route / audience / one-line purpose / REQUIRED CONTENT checklist / deliverables / gates / source citations, **10 workstreams W1–W10**, **15 roadmap items**, **6 judgment calls (J1–J6)**, and **12 recorded grounding gaps**. **PART C:** `SITE_PROGRESS.json` — P2 phase done, three P2 items closed, D18–D20 mirrored, W1–W10 added as phased items, `activePhase` → P3, `currentTask` → "P3-W3: type-specimen bench". **ADDENDUM APPLIED MID-TASK — the P1-C1 gate is CLOSED** by the owner's Search Console reading: `SEO_PLAN.md` gained **§2.7** with the verbatim readings, `SITE_GROUND_TRUTH.md` §9 now lists only P1-B1 as open, the build plan's gates table cites P1-C1 historically as CLOSED, and `SITE_PROGRESS.json` marks the gate done and logs the Sitemaps observation as a low-priority discovery.
- **VERIFIED:** **The P1-C1 closure, verbatim (OWNER-RELAYED, URL Inspection, 2026-08-17):** `https://nexoaccess.com/` — **"URL is on Google"**, page **is indexed**, last crawl **Aug 9, 2026, 11:14 PM** (Googlebot smartphone), crawl allowed, fetch successful, indexing allowed, user-declared canonical `https://nexoaccess.com/` and **Google-selected canonical = Inspected URL (Google agrees with the apex)**. `https://nexoaccess.com/platform` — **"URL is on Google"**, indexed, last crawl **Aug 5, 2026, 6:44 PM**, Google-selected canonical = Inspected URL, **referring pages include the www variant, consistent with correct alternate handling**. Minor: homepage inspection showed Sitemaps **"Temporary processing error"**; `/platform` showed **"No referring sitemaps detected"** — neither blocking, both logged. **This confirms the Task #8 adversarial refutation by outcome:** the www rows were canonicalization *succeeding*, and the honest UNRESOLVED verdict was correct precisely because there was no defect to find. **Every one of the 24 page entries cites its research source** by document and section. Ledger append-only order verified **D1 → D20** after the edit. Board validator **green on the first run** (7 phases, 37 items, 57.7 KB). `npx tsc --noEmit` **TRUE 0**. `git status` showed only the intended files.
- **TESTS ADDED:** none — documentation task, no site code touched, no cube run. **`I19` remains specified, not built** (W9 builds it in P5 alongside the redirect it guards).
- **SKILLS USED:** `frontend-design` for the IA judgment — specifically the per-page content architecture, the audience-to-route mapping, and the decision to keep segmented payer sub-audiences inside one page rather than splitting into new routes until there is distinct proof per segment. `nexo-brand` as the lens every content spec was passed through: §7 (the honesty gate, which is why `/security` is scoped to four repo-verifiable facts instead of matching competitors' SOC/HITRUST posture, and why `/providers/faq` can honestly answer only about half its inherited question list), §7.2 (which is why `/about` carries only prohibitions), §7.4 (admin excluded from every public surface), and §5/§8 (the W2 constraints box). **Honest note:** a 5-agent workflow extracted the grounding — category matrix, provider publish map, SEO targets and schema assignments, craft verdicts and themes — plus an adversarial completeness critic; **the critic's findings materially changed the plan** (it caught all six contradictions and the twelve gaps). The judgment calls, the scoping decisions, and the rulings on which pages must not ship yet are mine.
- **AUDIT GUIDANCE:** **THE PLAN IS FROZEN. Any future addition to Section 1 (pages) or Section 2 (workstreams) is a freeze violation and belongs in Section 3 (ROADMAP).** Auditors must flag B-item scope growth as a violation, not as an improvement. **Six judgment calls J1–J6 are recorded with reasons and are reversible only by an owner ruling** — do not silently re-decide them: **J1** the owner-question count is **58**, not the 70 in `PROVIDER_REQUIREMENTS_RESEARCH.md` §7's own summary (row count settles it: DC 24 + MD 16 + VA 18); **J2** the **Maryland 16+ passenger insurance tier is UNPUBLISHABLE** because two cells inside one document are both labelled VERIFIED and disagree, and that document itself calls a wrong insurance minimum "actively harmful"; **J3** the "$100,000 GL / 25-50-25" figure set is attributed to **Maryland by one document and Virginia by another**, so the flagship insurance-contradiction page cannot be written until the jurisdiction is resolved; **J4** the matrix headline arithmetic is wrong and the cells win (8 full / 4 partial / 2 gated / 29 absent); **J5** `gated` is a **deploy state, not a content gap**, so the sign-in surface is a flag flip (W7), never build work; **J6** the three state pages are **provider-only**. **Three routes are deliberately NOT shippable yet and their thinness is not a defect to fix by inventing content:** `/providers/credentialing` carries a "CARRY (priority)" label with **zero publish-map rows** behind it and an unmakeable duration claim in its query targets; `/faq` has **no question list anywhere in the corpus** and waits for real inbound; `/notices` is a **legal instrument with no legal source**, so we lay it out and do not author it. **Two obligations are owed by W8 and must not be lost:** D18's payer half has **no SEO query classes behind it**, and the internal-linking spec stops after five bullets. **Never re-flag "homepage not indexed" without a fresh owner Search Console reading; as of 2026-08-17 both inspected URLs are indexed with agreeing canonicals.** Note that only **2 of 13 routes** were inspected, so the other eleven remain unread — that is an unknown, not a defect. The www→apex 301 stays in W8 as **durability work, never rescue** (`rel=canonical` is a hint Google may override); do not drop it because indexing is fine, and do not re-justify it as an indexing fix.

### 2026-08-17 — Task #8: P1c SEO plan (live audit, indexing diagnosis, D15 rewrite specs) + owner taste receipts (§13)
- **WRONG / BEFORE:** The homepage indexing issue was **undiagnosed** — `SITE_GROUND_TRUTH.md:79` recorded "homepage indexing issues open; www→apex 301 redirect planned; canonical/OG currently carry the OLD 'NEMT company' positioning" with no evidence behind any of the three, and no spec for fixing them. Nobody had fetched the live site to establish what was actually true. The provider resource center was identified as the organic engine in Task #5/#6 but had no target queries, no internal-linking design, and no assessment of whether the incumbents were beatable. Separately, the owner's design taste existed only in voice-to-text messages and would have been lost.
- **CHANGED:** **PART A:** appended **§13 OWNER TASTE RECEIPTS** to `docs/DESIGN_RESEARCH.md` (102 lines) — seven references with the owner's stated reasons in an explicit **OWNER-STATED** evidence tier, five themes **T1–T5** (combination over cloning; fonts are a wound; the imagery gap is real user feedback; borderless full-bleed; keep our creativity), and what P2/P3 owe against each. Added a **P2 ratification agenda** item and **DISC-17** to `docs/SITE_PROGRESS.json`. **PART B:** created **`docs/SEO_PLAN.md`** — live current-state audit, adversarially-reviewed diagnosis, four fix specs (www→apex 301; metadata/JSON-LD rewrite to D1/D15; provider-resource-center organic architecture; E-E-A-T posture), and a measurement plan. **PART C:** `P1-C` done, **`P1-C1` added as a blocking owner gate**, phase **P1 → done**, `activePhase` → P2, `currentTask` → "P2: sitemap freeze + ratification session".
- **VERIFIED:** All audit facts are `curl` readings of the live production site on **2026-08-17**. **Apex and www both return HTTP 200 with IDENTICAL `ETag "13x53aqr78w4r74"` and identical 222,048-byte bodies, with zero redirects between hosts** — a true duplicate-host state. **But the cross-host `rel=canonical` is CORRECT on both hosts, including deep pages** (`www/platform` → `https://nexoaccess.com/platform`), and both serve an apex-consistent `robots.txt` and the same 13-URL apex sitemap. **A redirect layer already exists and deliberately preserves host:** `http://www.nexoaccess.com/` → **302** → `https://www.**nexoaccess**.com/` (to www, not to apex), so host normalisation must be added on purpose. **SSR is GREEN, tested rather than assumed** — every `<script>` block was stripped from each response and 6,056 / 1,851 / 2,019 characters of real body text plus 17 / 10 / 9 genuine headings survived, so the App-Router "content only lives in the RSC flight payload" failure mode does **not** apply; a Googlebot user-agent returned byte-identical markup (no cloaking). **DISC-7 CONFIRMED and worse than recorded:** the stem **"manage" appears ZERO times** across the entire indexed metadata surface (title, description, every og/twitter tag, JSON-LD, h1) of all three audited pages, and **one byte-identical `MedicalBusiness` JSON-LD block asserts "Nexo Access is a technology-first NEMT company built for the DMV" on all 13 routes** — the old framing is stated in machine-readable form site-wide from a single string, contradicting `/solutions/providers`'s own correct human-readable "Join the Nexo Access network". Board validator green after two guard rejections (below); board regenerated 54.1 KB, 7 phases, 26 items. **`npx tsc --noEmit` TRUE 0.** `git status` showed only the four intended files.
- **TESTS ADDED:** none — research and planning task, no site code changed and no challenge class fixed. **A new harness invariant is SPECIFIED but not built: proposed `I19`** (assert `301` on `www` → apex with path and query preserved), which must issue a raw request with an explicit `Host:`/`X-Forwarded-Host` header because the sweep addresses `localhost:3300` by port and cannot otherwise exercise a host rule. It is built in P4 alongside the redirect it guards. **The D12 guard fired twice on real input and was right both times** — first rejecting `status: "scheduled"` on the new gate item (six legal values named in the error), then rejecting `DISC-17` for using `title`/`summary` when the discovery schema requires `text`. Both were my errors, corrected; nothing was written until the JSON was valid.
- **SKILLS USED:** `nexo-brand` §6.8/§7.3 (the query-lexicon law, which is what resolves the central tension in the rewrite spec: "NEMT" stays in titles because it is what searchers type, while the *descriptions* carry the management-organization model — naming the service category is not the banned claim, describing *the company* as the operator is) and §7/§7.2 (the copy honesty gate, which is why `HERO_LEDE` is flagged as a COPY GATE event rather than a metadata edit, and why the whole E-E-A-T section is built from signals we can ship honestly). `frontend-design` for the information architecture of the provider center and the internal-linking spine. `webapp-testing` for the fetch-and-inspect method. **Honest note:** `coding-standards` was consulted for the redirect and schema specs but contributed nothing beyond what `nexo-brand` and the existing code conventions already dictate — recording that rather than padding the list. A 13-agent workflow did the parallel live audit, three-jurisdiction query research, competitor SEO teardown, and E-E-A-T research; **the four adversarial verify agents materially changed the conclusion** (below); the diagnosis, specs and synthesis are mine.
- **AUDIT GUIDANCE:** **NEVER re-flag the five `www` "Alternate page with proper canonical tag" rows in Search Console as a defect.** Google's own documentation defines that status as *"This page correctly points to the canonical page, which is indexed, **so there is nothing you need to do**"* (support.google.com/webmasters/answer/7440203, fetched 2026-08-17). They are the documented signature of canonicalization **succeeding**. **One correction to the brief's own phrasing is recorded in SEO_PLAN §1.6:** the task described the "Validate Fix" click as failing "by design", but the verified substance is that the status is *informational*, sits in the "Why pages aren't indexed" table rather than under Errors, and has **nothing to validate** — a stronger statement than "validation fails". Whether the UI exposes the affordance at all is UNRESOLVED (no Search Console access); the operational instruction is identical either way — **leave those rows alone**. **Do not "fix" the diagnosis into something more confident than it is:** the adversarial pass **REFUTED** the intuitive www-causes-indexing hypothesis (crawl budget does not apply at 13 URLs; the cited evidence is the success signature) and rated the authority hypothesis **OVERSTATED** because site age and backlinks are unmeasured. **The honest verdict is CAUSE UNRESOLVED**, and it collapses to a diagnosis with one owner reading — tracked as **`P1-C1`**, which blocks the P4 SEO work. **The www→apex 301 is still specified and still worth shipping, but for durability, not rescue:** `rel=canonical` is a hint Google may override, and a 301 removes that discretion permanently and cannot be silently lost by a future code change. Do not let a future audit either drop the redirect ("the canonical handles it") or over-claim it ("it will fix indexing") — both were tested and both are wrong. All SEO and competitor facts are dated **2026-08-17** and describe live third-party surfaces that change; **MTM is the one to watch** — it runs genuine state-scoped recruitment SEO and currently has zero DC/MD/VA provider pages, so if it templatises, the §3.3 window narrows. Provider pages remain **BLOCKED** under D17 by `P1-B1` (58 owner questions plus OR-1/OR-2/OR-3). **No rankings are promised anywhere in `SEO_PLAN.md`, and no volume, difficulty, or traffic numbers appear in it** — we have no keyword tool and no Search Console API, and any future edit adding such a number must cite a real source or omit it.

### 2026-08-17 — Task #7: P1a craft lane (Stripe/Linear dissection, performance bar, nexo-motion candidates) + D17 publish boundary
- **WRONG / BEFORE:** The craft half of P1a was unstarted. D14 had committed us to a "Mobbin-class animated premium" V2 and to a technique-teardown workstream (animation library, what is animated, loading strategy, degradation strategy), but nothing in the repo recorded *how* an exemplar site is actually built. The specific open question was **how Stripe's WebGL gradient hero degrades**, and the honest answer in the repo was "nobody has looked." Separately, `docs/PROVIDER_REQUIREMENTS_RESEARCH.md` carried owner-supplied figures with no label distinguishing them from primary-source findings, and no law governed what a resource page may say.
- **CHANGED:** **PART A (law):** appended **D17** to the `SITE_GROUND_TRUTH.md` §4 ledger — the resource center states **requirements, never advice**; business-formation, ownership, legal and financial guidance are banned ("form an LLC" and its relatives); every resource page carries an informational-only disclaimer and its verification date; nothing UNRESOLVED or merely REPORTED/OWNER-REPORTED is publishable. Added the matching §8 standing-law bullet. Added a **§5b OWNER-REPORTED block** to the provider research (OR-1 WMATC scope, OR-2 DC workers' comp above 3 workers, OR-3 DC GL $1.5M–$2M) plus a D17 governance banner on its §6 publish map, so owner recollection is a distinct evidence tier and not silently promoted to VERIFIED. **PART B (research):** appended **part 2** to `docs/DESIGN_RESEARCH.md` (523 lines, §7–§12) — Stripe full anatomy (13 homepage sections, nav system, footer system, `/enterprise` deep page, typography) and Linear **motion only**, each dissected on PURPOSE / STRUCTURE / TECHNIQUE / DEGRADATION / VERDICT; a performance bar; and a four-part synthesis (nav+footer verdicts, typography direction, four `nexo-motion` candidates, Maze and ElevenLabs paragraphs).
- **VERIFIED:** Everything in part 2 is a **machine reading of the live page on 2026-08-17**, not recall: headless Chromium at 1440×900, each subject loaded normally and under `prefers-reduced-motion: reduce`, with a `getContext` interceptor installed before page scripts. **The priority question is answered with evidence, and the premise needed correcting: stripe.com's hero is not WebGL today.** `.hero-wave-animation` contains **0 canvas, 0 svg, 0 video, 1 img** — an art-directed `<picture>` with three width-gated sources whose assets **Stripe itself names `wave-fallback-desktop.png` / `wave-fallback-tablet.png` / `wave-fallback-mobile.png`** (WebP, `q=60`, `alt=""`), inside a wrapper Stripe classes `__static`. The **capability gate** was captured on the page's one real canvas (`squeezy-carousel__canvas`, document Y ≈ 11,676): `getContext("webgl", {…, failIfMajorPerformanceCaveat: true, powerPreference: "high-performance"})` → **null**, then `experimental-webgl` → **null**, then `2d` → **granted**. A **control canvas created in the same document** proves the flag is the discriminator, not WebGL absence: plain `getContext("webgl")` → **true**, with `failIfMajorPerformanceCaveat: true` → **false**. Stripe therefore *declines* WebGL on a software rasterizer rather than rendering slowly. **Four limits are stated in §7 rather than papered over:** the harness has software WebGL only (SwiftShader/ANGLE), so Stripe's accepted-GPU path was never observed; **Stripe's stylesheets are cross-origin and unreadable, so no claim is made about its reduced-motion CSS in either direction**; transfer/request counts are single-run with an uncontrolled cache and are labelled indicative, not measurement; and section enumeration is selector-dependent, so pages that collapsed (`/enterprise`, ElevenLabs) are reported as probe limits, not as one-section pages. Board validator green after a status correction (below); board regenerated 50.8 KB, 7 phases, 24 items. `npx tsc --noEmit` **TRUE 0**. No screenshots committed — `scripts/qa/artifacts/research/` was already gitignored from Task #5 and 0 files staged.
- **TESTS ADDED:** none — research and documentation task, no challenge class fixed. **The D12 guard earned its keep:** the progress update initially used `status: "scheduled"` for the new gate item, `npm run board` **refused to write** and named the offending item and the six legal values, and the status was corrected to `todo` (the owner can act on it now; what it *blocks* is P4, which the item note states). The guard behaved exactly as designed, on real input, unprompted.
- **SKILLS USED:** `nexo-brand` throughout as the measuring stick — §0 (reduced-motion non-negotiable, which is what made Linear's posture a **failure to record** rather than a technique to copy), §2 (sequential headings, which is what made Stripe `/enterprise`'s 20 `<h1>` elements a defect and produced the measure clarification), §5 (motion vocabulary and ceilings, which the four `nexo-motion` candidates extend rather than replace), and §1 (the tonal map, against which Stripe's single dark chapter in 13 sections was read). `webapp-testing` for the Playwright instrumentation pattern. Honest note: no subagents this task — every measurement was taken directly, because the whole value of the priority question was in controlling the experiment (the first two runs produced an ambiguous "animating vs static" result that was a **measurement artifact** of screenshotting an off-viewport, not-yet-painted canvas; it was discarded and re-run properly rather than reported).
- **AUDIT GUIDANCE:** Do not re-flag the WebGL hero question as open — it is answered in §8.0 **with the correction that the hero is not WebGL at all**, and the standing rule for any future canvas/WebGL work on this site is recorded there (gate the GPU request behind `failIfMajorPerformanceCaveat: true`, ship a named art-directed static asset as the resting state, never put the gamble in a hero). **Do not treat part 2 as design decisions:** every `nexo-motion` candidate is a proposal for P2/P3 and the performance bar is proposed for P2 ratification; nothing here is approved or published. **Do not "fix" the divergences from Stripe** — the non-sticky header, the light 85-link footer, and the absent sub-nav on a 14,184px page were all examined and deliberately rejected, with reasons, in §11a and §8.4; they are logged as chosen. **Two exemplar defects are recorded as what-not-to-copy and must not be read as endorsements:** Stripe `/enterprise` ships 20 `<h1>` elements (including the logo and duplicated nav-panel labels) and its homepage hero ships two; Linear ships **no first-party `prefers-reduced-motion` CSS** (1 rule of 1359, belonging to the third-party `sonner` toast library) and still runs **103 animations** under reduced motion — measured on same-origin, fully readable stylesheets, so that is a real absence and not an inspection limit. In both cases **our existing law is stronger than what the exemplar ships**. The ElevenLabs `motion-safe:` inversion (opt **in** to motion at the utility level rather than opting out) is flagged in §11d as the strongest adoption candidate in the sample and is a P2 question. All observations are dated 2026-08-17 and describe live third-party sites that will change; re-observe before relying on any specific measurement.

### 2026-08-17 — Task #6: P1b provider requirements research (primary sources DC/MD/VA, comparison table, publish map)
- **WRONG / BEFORE:** Provider requirements were known only from owner memory and from what competitors publish about themselves. Two specific figures were in play and unverified: a **$1.5M insurance minimum** attributed to a payer/broker, and a **Virginia CSL of $300K–$1M**. Task #5's audit guidance had explicitly blocked both from publication pending primary sourcing. Nothing in the repo distinguished state licensure from Medicaid enrolment from broker credentialing.
- **CHANGED:** Created `docs/PROVIDER_REQUIREMENTS_RESEARCH.md` (814 lines). Three jurisdiction chapters (DC, Maryland, Virginia), each covering program structure, the ordered path to becoming a provider, vehicle requirements by class, driver requirements, insurance minimums, ongoing compliance, and open questions written for the owner to answer from experience. Plus: a dedicated **insurance cross-check** section, **verdicts on both contested figures**, a **cross-state comparison table** (the skeleton of `/providers/requirements`), and a **publish map** marking each content block PUBLISHABLE or BLOCKED with its condition. **351 requirements captured, 58 owner questions, 128 sources consulted, 47 sources recorded as unreachable or blocked.**
- **VERIFIED:** Every requirement carries **VERIFIED** / **REPORTED** / **`UNRESOLVED`** with URL and access date (201 / 68 / 82 occurrences). **$1.5M: CONFIRMED** as MTM Health's published DC broker requirement (its DC NET Provider FAQ, live and text-extracted 2026-08-17, states "no less than $1,500,000 each" for general and automobile liability) — **but kept BLOCKED from our site** because the document is undated in substance and **MTM's own archived DC-titled handbook contradicts it at $500,000 CSL**; two independent alternative origins were also verified (Virginia's 7-to-15-passenger tier at $1.5M; WMATC restricted authority at $1.5M CSL). **VA CSL: CORRECTED** — the actual state floor is **$350,000** for a 1-to-6-passenger vehicle (Code of Virginia § 46.2-2053 via Virginia DMV); $350K falls inside the recalled range but matches neither endpoint, and **no Virginia primary source uses the term "combined single limit"**. Board validator green (7 phases, 22 items, 16 decisions, 16 discoveries), board regenerated 49.9 KB. `npx tsc --noEmit` **TRUE 0**.
- **TESTS ADDED:** none — research and documentation task, no challenge class fixed. The board's JSON validator (the D12 guard) was exercised on the progress update and passed.
- **SKILLS USED:** `nexo-brand` §7 (the copy honesty gate, which is why the DC $1.5M figure is CONFIRMED as a finding yet BLOCKED from publication — a verified quotation from an undated, self-contradicted document is not a publishable claim). Honest note: no design skills were used or needed; nothing visual was produced. Four parallel read-only research subagents did the primary-source fetching (three jurisdictions plus a dedicated cross-cutting insurance pass, deliberately overlapping so the highest-stakes numbers got two independent looks); the verdicts, comparison table and publish map are mine.
- **AUDIT GUIDANCE:** **All regulatory facts are dated 2026-08-17 and reflect what a document said on that date, not what is currently operative law. Regulatory currency decays: every figure must be re-verified against its primary source at publication time, and each published page must carry its verification date.** A future recon must not treat this document as current law and must not promote a `UNRESOLVED` item without new evidence. **The $1.5M / VA CSL resolution status is recorded inside the document (§4)** — do not re-open it as an unknown; do re-check it before publishing. **58 owner questions are pending an OWNER-VERIFIED review pass** (DISC-16); owner answers still require a primary source before anything ships. The DC broker contract term is genuinely `UNRESOLVED` and tracked as DISC-15 — MTM's 2021 re-award was "one year plus four option years", so the DC broker could change before `/providers/dc` ships. Two research passes disagreed about whether MTM publishes a DC figure; that is recorded in §4a as a coverage difference (robots-blocked DOC/PDF assets), not a factual conflict, and needs no re-litigation.

### 2026-08-17 — Task #5: P1a industry lane (five-subject IA teardown, category matrix, draft V2 page inventory)
- **WRONG / BEFORE:** There was no documented picture of what the NEMT industry actually publishes. V2 scope was guesswork: SITE_GROUND_TRUTH §6 P2 carried a *hypothesis* page list ("current 12 public pages + provider resource center + security page + FAQ") with nothing behind it. Nobody had checked which information categories competitors surface for payers, providers, facilities or members, which self-description nouns the industry uses, or where our live V1 site is actually thin.
- **CHANGED:** Created `docs/DESIGN_RESEARCH.md` **part 1** (1,641 lines). Five subjects torn down: **MTM** (`mtm-inc.net`), **Modivcare** (`www.modivcare.com`), **Verida** (`verida.com`, formerly Southeastrans), **SafeRide Health** (`www.saferidehealth.com`), **Roundtrip** (`roundtriphealth.com`) — each domain verified by search before fetching, all five resolved live-as-given. Per subject: identity line with the exact self-description noun, full nav map with verbatim labels, page inventory grouped by audience (≈300 page types enumerated), information categories per audience (326 recorded), artifacts (documents, portals, contact patterns), sign-in pattern, and a copy-their-good / reject-their-bad verdict table (111 verdicts). Then §6 SYNTHESIS: a **43-category matrix** scored across the five subjects plus **NEXO V1** (audited first-hand from this repo) and **NEXO V2** (carry/skip with reasons); provider-resource findings for P1b; sign-in patterns for D3; vocabulary findings mapped against D15; and a **draft 24-page V2 inventory marked DRAFT FOR P2 FREEZE**. Added `scripts/qa/artifacts/research/` to `.gitignore` **before** any capture. Updated `docs/SITE_PROGRESS.json` (P1-A1 done, P1-A split to in-progress, DISC-14 added, currentTask → craft lane) and regenerated the board.
- **VERIFIED:** Every competitor claim carries a **VERIFIED** or **REPORTED** label (971 / 74 occurrences in the document); published marketing numbers are labelled VERIFIED *as published claims*, never as facts about the world. Board validator **green** (7 phases, 22 items, 16 decisions, 14 discoveries) and regenerated (47.8 KB). `npx tsc --noEmit` **TRUE 0**. Research screenshot directory confirmed gitignored and empty at commit time (0 files staged).
- **TESTS ADDED:** none — research and documentation task, no challenge class fixed. The board's JSON validator (the existing D12 guard) was exercised on the progress update and passed.
- **SKILLS USED:** `nexo-brand` §7 (the copy honesty gate, which set the rule that competitor *structure* is freely adoptable but competitor *claims* are not — several subjects publish unsourced statistics that we record as observations and never imitate) and its §7.2 self-description lexicon, against which the vocabulary findings were mapped; `frontend-design` (information-architecture judgment for the category matrix and the page-count recommendation). Honest note: both were applied from principles already established this session rather than re-read this task. Five parallel read-only research subagents did the fetching; the synthesis, matrix scoring and page recommendation are mine.
- **AUDIT GUIDANCE:** **All competitor facts in `docs/DESIGN_RESEARCH.md` are dated 2026-08-17 and carry VERIFIED/REPORTED labels. Competitor sites change; a future recon must re-verify before relying on any specific claim, and must not treat a REPORTED item as established.** The **draft 24-page V2 inventory is NOT frozen** — P2 freezes it, and per the standing rule the frozen list only shrinks. Insurance figures seen on competitor sites are **not** verified requirements: P1b must source DC/MD/VA numbers primarily, and the owner-reported $1.5M and VA CSL $300K-$1M figures remain unverified and unpublishable until then. Do not re-flag the V1 gaps recorded in the matrix as new discoveries — they are the documented input to P2/P4.

### 2026-08-17 — Task #4: law updates bundle (D14-D16, positioning decided in nexo-brand, PRODUCT.md, handoff scaffold)
- **WRONG / BEFORE:** SITE_GROUND_TRUTH was at v1.1 without D14-D16, and its §1 positioning still negated the transportation role ("We do not drive"). The `nexo-brand` skill still carried the stale UNDECIDED operating model, permitted only "technology-first NEMT company" as the self-description, and named `huashu-design` as a design co-lead. No `PRODUCT.md` existed, so the `impeccable` skill halted at setup with `NO_PRODUCT_MD`. No `docs/PLATFORM_DESIGN_HANDOFF.md` existed, so D4's cross-repo design law had no artifact.
- **CHANGED:** Five artifacts. (1) **SITE_GROUND_TRUTH.md → v1.2**: §1 rewritten to the de-negated positioning (transportation IS the business, delivered through a credentialed provider network, we answer for every trip); **D14, D15, D16 appended** to the ledger in append-only order; §6 P1a gained the global-scope anatomical-teardown method sentence; §8 gained the transfer-verdict law (D16) and the positioning language law (D15); §9 rewritten to P0-complete with P1a next. (2) **PRODUCT.md** created at the repo root (register, users, purpose, brand personality, anti-references, design principles, accessibility). (3) **nexo-brand amended in BOTH copies**: intro parenthetical, §7.2 heading and body, and the §7.3 query-lexicon reference now carry the decided identity; §0 precedence updated to frontend-design leading with huashu contributing in scope; a CHANGELOG line added to the header. (4) **docs/PLATFORM_DESIGN_HANDOFF.md** created with the three verdicts and eight seeded rows. (5) **docs/SITE_PROGRESS.json** updated (P0-T4 done, currentTask → P1a, activePhase → P1, D14-D16 mirrored, three discoveries resolved, DISC-13 kept open with its workaround note, session appended) and the board regenerated.
- **VERIFIED:** `npx tsc --noEmit` **TRUE 0**. **nexo-brand copies byte-identical** after the edit: `diff` clean, md5 `529ea338ec0b7e3e946e2f3c409d7ae9` on both, 827 lines / 73,051 bytes each. Stale-text sweep on the amended skill: `undecided` 0, `technology-first` 0, `we do not drive` 0; gate MECHANISM intact (LAUNCH flags, the NEVER list, audit-every-string all still present). Board validator green and the board regenerated (44.2 KB, 7 phases, 21 items, 4 FIXLOG entries). **`impeccable` unblocked**: `node .claude/skills/impeccable/scripts/context.mjs` now prints PRODUCT.md and exits 0 instead of `NO_PRODUCT_MD`.
- **TESTS ADDED:** none — documentation-only task with no challenge class fixed. The existing D12 guard (the board's JSON validator, negative-tested in Task #3) was exercised on every edit of `docs/SITE_PROGRESS.json` and passed.
- **SKILLS USED:** `nexo-brand` (the amendment target; its own SYNC RULE governed the two-copy edit). No design skills were needed or used: nothing visual was produced. Honest note — `impeccable` was executed only as a verification probe (to prove the PRODUCT.md block lifted), not consulted for design guidance.
- **AUDIT GUIDANCE:** The **operating model is DECIDED** (D1 + D15). Future recons must NOT flag "undecided" positioning anywhere except as historical record in this FIXLOG and in `src/lib/launch.ts` comments (the code comment is a separate, still-open cleanup — the law now lives in SITE_GROUND_TRUTH §1, which wins). **`impeccable`'s PRODUCT.md block is lifted** — do not re-flag it. **The huashu precedence question is settled** (frontend-design leads; huashu contributes in scope) — do not re-open it. Do not re-flag the absence of D14-D16 from the progress JSON or the missing handoff doc; both now exist. `PLATFORM_DESIGN_HANDOFF.md` is append-only: changed verdicts get a new dated row referencing the old, never an edit in place.

### 2026-08-17 — Task #3: the board (D13) — progress JSON truth, board generator, eye-passed render
- **WRONG / BEFORE:** There was no visual status truth. Progress lived only in prose docs (SITE_GROUND_TRUTH, FIXLOG, SKILLS_REGISTRY), so "where are we" required reading three files and holding the answer in your head. Nothing showed phase progress, open discoveries, gates, or the rejected/deferred history at a glance.
- **CHANGED:** Created `docs/SITE_PROGRESS.json` — the committed status truth (meta, 7 phases P0-P6, 20 items, 13 decisions with D7 carrying its reversal chain, 13 discoveries, 4 gates, skills summary with the 14 canonical picks, sessions), populated with real data from SITE_GROUND_TRUTH.md, FIXLOG.md and docs/SKILLS_REGISTRY.md (no placeholders). Created `scripts/board.mjs` — zero-dependency, Node-only, Windows-safe: it validates the JSON, parses the newest 5 FIXLOG entries, and emits `docs/SITE_BOARD.html`, one self-contained file with inline CSS and **zero network requests** (verified: 0 matches for http/img/script-src/@import), openable from `file://`. Sections in order: YOU ARE HERE banner, phase bars, plan ledger, decisions (D7 rendered as a WAS -> NOW chain with its why), latest FIXLOG, gates dashboard, skills, discovery inbox, THE GRAVEYARD, sessions, footer. Palette values are MIRRORED from `src/app/globals.css` as inline constants with a comment saying so. Added `"board": "node scripts/board.mjs"` to package.json and `docs/SITE_BOARD.html` to .gitignore (the HTML is a local render; the JSON is the truth).
- **VERIFIED:** `npx tsc --noEmit` TRUE 0. `npm run board` writes 40.8 KB / 7 phases / 20 items / 3 FIXLOG entries. Render measured in-browser: document 6,540 px tall, **0 horizontal overflow, 0 page errors**, all 9 sections present, 3 ledger "done" rows, 13 decision cards (1 reversal card), 4 gate tiles, 12 discovery rows, 5 graveyard cards, 14 canonical picks. **Human eye pass performed on the committed artifact** (`scripts/qa/artifacts/skills-audit/board-v1.png`, re-shot after the final JSON edit so evidence and truth match): banner legible on ink, P0 bar fully jade at 3/3, status dots coloured and each paired with a written label, nothing overlapping or clipped.
- **TESTS ADDED (D12):** The board's `validate()` guard is the test — malformed truth must fail loudly, never render a silently-wrong board. **Negative-tested three ways, all exit 1 with a precise message naming the fault:** (1) missing required top-level key -> `missing required top-level key: "gates"`; (2) invalid item status enum -> `item P0-T1 has status "almost-done"; allowed: done, in-progress, todo, blocked, deferred, rejected`; (3) dangling phase->item reference -> `phase P0 lists item "P0-GHOST", which does not exist in items[]`. Positive control passes (exit 0), and the HTML was **not** written by any failing run. A `--check` flag runs validation only, so a future preflight (the deferred GY-4 toolchain check) can call it directly. Temp fixtures deleted.
- **SKILLS USED:** `nexo-brand` (its actual token VALUES from globals.css, mirrored into the board; hierarchy-over-density and colour discipline), `frontend-design` (token plan first, then critique against templated defaults), `emil-design-eng` (restraint: the board ships zero animation because none of it would carry meaning), `frontend-a11y` (AA contrast pairs on the mirrored tokens; **colour is never the only cue** — every status dot is paired with its written label; the phase bar carries an aria-label), `webapp-testing` (Playwright screenshot lane for the eye pass). **Honest scope note:** these principles were applied from the standing set established earlier this session and from the real token file; I did not re-read each SKILL.md during this task. `playwright-cli` was attempted first per the prompt and could not load `file://` (see DISC-13).
- **AUDIT GUIDANCE:** `docs/SITE_BOARD.html` is **gitignored by design** — its absence from a fresh clone is NOT a defect; run `npm run board`. `docs/SITE_PROGRESS.json` is the committed truth and the only file to edit; the HTML is disposable output. Rejected/deferred items (GY-*) are permanent graveyard records, never to be "cleaned up". The board renders `meta.currentTask` verbatim, so each task must advance it: it currently still names Task #3, and the next task's prompt should set it. Do not re-flag the board's system-font stack as a brand deviation — webfonts are deliberately excluded to keep the render network-free.

### 2026-08-17 — Task #2 ADDENDUM v2: repair-first law applied (huashu-design restored, impeccable paths fixed)
- **WRONG / BEFORE:** Owner rulings arrived after the Task #2 commit (`fd51e5d`) was already pushed. The addendum assumed nothing had been committed; it had. Under the original audit, `huashu-design` and `impeccable` were reported as WORKING-with-caveats and their missing/broken files were left as discoveries rather than repaired.
- **CHANGED:** Recorded the **REPAIR-FIRST LAW** in the registry (a skill with missing files gets a repair attempt before any BROKEN verdict). **`huashu-design`: FIXED** — searched five sibling installs (all file-less), every plugin/marketplace cache, and the ECC bundle, then found the GitHub origin `alchaincyf/huashu-design` (`e735935`) and restored `references/` (32), `scripts/` (18), `assets/` (15) into both the repo copy and the user mirror; 43/47 routed paths now resolve (the other 4 are doc placeholders absent upstream). Root cause confirmed upstream: a skills-CLI sync bug (≤1.5.15 synced single files only). The six `assets/bgm-*.mp3` video-export tracks (~26.7 MB) were deliberately excluded with the completion command recorded; net +5.0 MB. **`impeccable`: FIXED-PATH / BLOCKED** — rewrote `.agents/skills/impeccable` → `.claude/skills/impeccable` across `SKILL.md`, five `reference/*.md`, and `scripts/hook-admin.mjs` in both copies (a junction was rejected as machine-local and clone-fragile); `hook-admin.mjs` was included because it *writes* the hook command and would otherwise install a hook pointing at a nonexistent path. Updated CANONICAL PICKS: **general-design lead = `frontend-design`** (+ `emil-design-eng` and the standing set), `huashu-design` contributes where its scope applies, `nexo-brand` remains law above all. Added DISCOVERIES 16 (nexo-brand §0 co-lead vs huashu's scope note, for the queued amendment) and 17 (owner ruling queued as D14: Mobbin-class animated premium, technique + measurement, P1a technique-teardown workstream, new skills via `skill-creator` candidate `nexo-motion`, static-complete SSR retained), and converted discoveries 2 and 4 to RESOLVED.
- **VERIFIED:** `npx tsc --noEmit` TRUE 0. `impeccable` repair proven by executing the now-documented command verbatim — `node .claude/skills/impeccable/scripts/context.mjs` exits 0 and returns `RESOLVED_CONTEXT` (its `NO_PRODUCT_MD` line is the expected, still-open block); `node --check` passes on the edited `hook-admin.mjs`; zero `.agents/skills/impeccable` references remain in either copy. `huashu-design` repair verified programmatically (43/47 routed paths resolve) plus a spot-read of `references/design-styles.md`. The cube was **not** re-run: this addendum changed only `.claude/skills/**` tooling, no site code and no shared chrome, so the Task #2 baseline (234 cells, ALL GREEN, full firefox leg included, not stopped early) stands.
- **TESTS ADDED:** none, and the reason is stated rather than skipped (D12). Both repairs are install/path-integrity classes, not code-regression classes; the durable check is the re-runnable verification in registry §4.1/§4.2 (routed-path resolution count, and executing the documented command). The recommended automation remains the `scripts/qa/toolchain-check.mjs` preflight proposed in the Task #2 entry, which should now also assert skill-companion integrity (routed paths resolve) — deferred to P0 Task 3 or P5 with owner sign-off, since it adds a new harness file outside this task's scope.
- **SKILLS USED:** `nexo-brand` §0 (discovery process); `impeccable` (executed its own `context.mjs` as repair proof); `huashu-design` (target of repair); `webapp-testing` (Tier-1 lane, unchanged).
- **AUDIT GUIDANCE:** Do not re-flag `huashu-design`'s missing companions or `impeccable`'s `.agents/` paths — both are FIXED here; their reappearance would be a **regression**, not a discovery. Do not re-flag the `bgm-*.mp3` exclusion (deliberate, with the completion command on record). `PRODUCT.md` remains legitimately open (DISCOVERY 3, owner-scheduled). The `nexo-brand` amendment now carries **two** items: the stale §7.2 operating-model line (D1) and the §0 co-lead reconciliation (DISCOVERY 16). D14 is queued in DISCOVERY 17 and must be appended to SITE_GROUND_TRUTH.md by the next docs task. **History note:** this addendum was applied as a follow-up commit because `fd51e5d` was already pushed; no history was rewritten and no force-push was used (D7 guardrails).

### 2026-08-17 — Task #2: P0 skills audit (registry, cube baseline, tool fixes)
- **WRONG / BEFORE:** The toolchain was assumed, never audited (D6). No registry existed, so nobody knew which of the ~100 installed skills applied to this site, which overlapped, or which tools actually ran. `playwright-cli` was documented by an installed skill but was NOT on PATH (the npm package `playwright-cli` is deprecated). The V1 QA cube had no recorded full-green baseline on the `v2` branch, and Stage 16.1's `display:"optional"` font fix was still recorded as NOT VERIFIED because its confirming cube run had been stopped early.
- **CHANGED:** Enumerated the whole toolchain from the live filesystem: 103 project skills (= 90 user skills byte-identical + 14 vendored superpowers plugin skills), 89 user skills, 14 plugin skills, 2 MCP servers (`agent-browser`, `mobbin`). Ran Tier-1 functional tests with committed evidence (3 Playwright engines, the full cube, playwright-cli, agent-browser, a live Mobbin MCP query). **FIXED `playwright-cli`** by following the skill's own §Installation: `npm install -g @playwright/cli@latest` (0.1.18, now on PATH, `open` + `screenshot` verified). Classified all 103 skills into Tier 2 (11 standing design skills) and Tier 3 (92) via 11 parallel read-only subagents, then reconciled their circular rulings into one taxonomy. Wrote `docs/SKILLS_REGISTRY.md` (inventory, Tier-1 table, cube baseline, Tier-2 + sync check, Tier-3 by category, canonical picks, MCP division of labor, 14 discoveries).
- **VERIFIED:** `npx tsc --noEmit` TRUE 0 (no TypeScript touched). **THE CUBE: ALL GREEN** — 234 cells (13 routes x 6 profiles x 3 engines), 3,175 invariant checks passed, 0 failures, 0 retries, 0 console errors, 0 `FAIL_*` artifacts; chromium + webkit + firefox each GREEN. Tier-1 evidence committed under `scripts/qa/artifacts/skills-audit/` (5 screenshots, 10.2–11.8 KB each). `nexo-brand` sync check: repo canonical and user mirror are **IDENTICAL** (71,514 bytes, 809 lines, md5 `08d793ac25e63ed0fa4a17784ade7e62`). Agent claims about `impeccable` and `huashu-design` were re-verified by the auditor directly rather than taken on trust.
- **TESTS ADDED:** none in this commit, and the reason is stated rather than skipped (D12). The one thing FIXED was an environment/install gap (a global CLI absent from PATH), not a code-regression class, and the re-runnable Tier-1 procedure in the registry is its durable check. A real guard — a `scripts/qa/toolchain-check.mjs` preflight asserting node/npm/playwright/3 engines/`playwright-cli`/`agent-browser` resolve, wired into `qa:sweep` — belongs in P0 Task 3 or P5 and is **recommended as a follow-up**; it was not added here because this task's authorized staging list covers only the registry, FIXLOG, and the evidence directory.
- **SKILLS USED:** `nexo-brand` §0 (the SKILL DISCOVERY standing process that structures this audit) and §10/§10.1 (the cube law); `dispatching-parallel-agents` / `do` (11-agent parallel classification); `webapp-testing` + `playwright-cli` + `agent-browser` (Tier-1 execution); `mobbin` MCP (functional verification).
- **AUDIT GUIDANCE:** Do not re-flag anything in `docs/SKILLS_REGISTRY.md` §8 as a new discovery — specifically: the stale `nexo-brand` §7.2 operating-model text (D1 supersedes it; amending BOTH skill copies is a future task), `impeccable`'s `.agents/` script-path breakage and its `NO_PRODUCT_MD` setup block, `huashu-design`'s missing `references/`/`assets/`/`scripts/` and its SEO-site self-exclusion, the stale `playwright-cli` skill doc versus its binary, the empty `learned/` skill directory in both trees, the vendored duplicate of the 14 superpowers skills, the generic-skill-versus-nexo-brand doctrine conflicts, and the deprecation of the npm `playwright-cli` package. Also do not re-flag Stage 16.1's font fix as unverified — this task's cube verified it (`/about` x w768 x chromium x I5 now passes; CLS green in all 234 cells). The cube baseline recorded here found ZERO site defects, so any future cube failure is a genuine regression against this baseline, not a pre-existing condition.

### 2026-08-17 — Task #1: found the machine (v2 branch, ground truth, FIXLOG, recon receipt)
- **WRONG / BEFORE:** No document system in the repo. Decisions lived in code comments and lost chat "stage reports." Site positioned as an NEMT company; operating model recorded as UNDECIDED in src/lib/launch.ts.
- **CHANGED:** Created branch v2. Committed SITE_GROUND_TRUTH.md (law, decisions D1–D13), this FIXLOG, and docs/SITE_RECON_2026-08-17.md (full recon receipt: repo inventory, styling system facts, QA cube facts, cross-machine inconsistency candidates C1–C4, doc inventory, session decisions).
- **VERIFIED:** tsc TRUE 0 (no code changed); files present on v2; push accepted; CI check job expected green.
- **TESTS ADDED:** none — documentation-only founding task; no challenge class fixed.
- **SKILLS USED:** none — documentation task.
- **AUDIT GUIDANCE:** Do not re-flag the missing doc system, the UNDECIDED operating-model note, or the C1–C4 candidates as new discoveries; all are on record here and in the recon receipt. C1–C4 are HYPOTHESES awaiting P5 measurement, not confirmed defects.
